# DIR32 consistency whitelist — symbols with >1 resolved base that are
# KNOWN-legitimate (doubly-linked TUs / investigated FX vtable artifacts). Any symbol NOT
# listed here that becomes inconsistent is a NEW candidate bug and fails the build.
#
# The ??_7 ctor-vs-dtor family (FX modules + MemoryPoolObject) is ONE phenomenon: retail
# destructors inline the base-chain dtor, whose final vptr store targets the BASE-most vtable
# (0x1073744 = the real ??_7MemoryPoolObject@@6B@), while our shallower reconstructions
# reference the class's own vtable. Latent (patching masks with original bytes; drop-in stays
# byte-identical); root fix = reconstruct the MemoryPoolObject base-dtor inlining.
??_7DefaultAlphaModuleInfo@FXParticleSystem@@6B@
??_7DefaultColorModuleInfo@FXParticleSystem@@6B@
??_7DefaultDrawModuleInfo@FXParticleSystem@@6B@
??_7DefaultPhysicsModuleInfo@FXParticleSystem@@6B@
??_7DefaultUpdateModuleInfo@FXParticleSystem@@6B@
??_7EmissionVelocityInfo@FXParticleSystem@@6B@
??_7LightningDrawModuleInfo@FXParticleSystem@@6B@
??_7StreakDrawModuleInfo@FXParticleSystem@@6B@
??_7WindModuleInfo@FXParticleSystem@@6B@
?CRC32_Table@@3PAKA
_COLLISION_EPSILON
??_7MemoryPoolObject@@6B@

# realcrc.cpp is linked twice in retail (same reason CRC_Memory/CRC_String each match at
# two addresses): its static ?_Table@CRC@@0PAKA exists at both link sites. Callers resolve
# to whichever copy their TU linked against; both are the same table.
?_Table@CRC@@0PAKA
# memmove is imported through two IAT slots in retail (two import descriptors); DIR32
# references legitimately resolve to either __imp__ address.
__imp__memmove
# closesocket is imported through two IAT slots in retail: udp.cpp (game lib) resolves
# 0x13590c8, GameResultsThread.cpp (GameSpy static lib) resolves 0x1359700. Both
# references come from byte-matched functions — same double-import class as memmove.
__imp__closesocket@4

# Float-constant COMDAT: 1.0f is emitted per-TU; retail keeps two copies. Same
# class as _COLLISION_EPSILON above.
__real@3f800000
# File-scope static `parentPopup` exists independently in two GameSpy popup TUs;
# same symbol name in each obj, genuinely two variables in retail.
_parentPopup
# File-scope static `_parent` exists independently in the download-menu and popup-replay
# TUs (both zero-initialized); same symbol name, genuinely two variables in retail.
_parent
# File-scope static `parentID` exists independently in multiple GUI/GameSpy TUs;
# exact matched references resolve to the retail variables at 0x12F3F5C and 0x12F7120.
_parentID
# BitFlags<0xHF>::s_bitNameList materializes per-TU with different initializer arrays
# (drawable.cpp's TAUNT/CHEER/HERO_CHEER/POINT list at 0xea687c; modelconditionupgrade.cpp's
# CINEMA/HUD list at 0xec51bc). Both are retail-true references from byte-matched functions —
# same class of per-TU static as _Table@CRC / __real@3f800000 above.
?s_bitNameList@?$BitFlags@$0HF@@@0PAPBDA
#
# WOL menu file-statics: WOLLoginMenu.cpp and WOLLobbyMenu.cpp each define their own
# static buttonBack/buttonBackID/buttonPushed globals (same names, distinct retail
# addresses per TU: 0x12f46xx vs 0x12f48xx). Both byte-verified; name-based check
# cannot distinguish file-statics across TUs.
_buttonBack
_buttonBackID
_buttonPushed
# hanim.cpp's HAnimComboDataClass* vector TUs are linked twice in retail: one copy's
# code lives at file 0x96xxxx with vtables at 0x113e690/0x113e6a8, the other at file
# 0x97xxxx with vtables at 0x113eae0/0x113eb10. Contents differ (each points at its own
# copy's functions) — same doubly-linked-TU class as _Table@CRC, not a hidden bug.
??_7?$DynamicVectorClass@PAVHAnimComboDataClass@@@@6B@
??_7?$VectorClass@PAVHAnimComboDataClass@@@@6B@
??_7?$MultiListClass@VDX8FVFCategoryContainer@@@@6B@
??_7?$MultiListClass@VDX8TextureCategoryClass@@@@6B@
??_7?$MultiListClass@VMeshModelClass@@@@6B@
??_7?$MultiListClass@VPolyRemover@@@@6B@
??_7?$MultiListClass@VTextureTrackerClass@@@@6B@
??_7?$RefMultiListClass@VLookupTableClass@@@@6B@
??_7?$ShareBufferClass@I@@6B@
??_7?$ShareBufferClass@PAVVertexMaterialClass@@@@6B@
??_7?$SimpleDynVecClass@VVector3@@@@6B@
??_7?$SimpleVecClass@E@@6B@
??_7?$SimpleVecClass@K@@6B@
??_7?$SimpleVecClass@PAVTextureClass@@@@6B@
??_7?$SimpleVecClass@PAVVertexMaterialClass@@@@6B@
??_7?$SimpleVecClass@UDecalStruct@RigidDecalMeshClass@@@@6B@
??_7?$SimpleVecClass@VShaderClass@@@@6B@
??_7?$SimpleVecClass@VVector2@@@@6B@
??_7?$SimpleVecClass@VVector3@@@@6B@
??_7?$SimpleVecClass@VVector3i16@@@@6B@
??_7?$VectorClass@H@@6B@
??_7?$VectorClass@K@@6B@
??_7?$VectorClass@M@@6B@
??_7?$VectorClass@PAULegacyMaterialClass@MeshLoadContextClass@@@@6B@
??_7?$VectorClass@PAUW3dAggregateSubobjectStruct@@@@6B@
??_7?$VectorClass@PAVPrototypeClass@@@@6B@
??_7?$VectorClass@PAVPrototypeLoaderClass@@@@6B@
??_7?$VectorClass@PAVRenderObjClass@@@@6B@
??_7?$VectorClass@PAVTextureClass@@@@6B@
??_7?$VectorClass@PAVVertexMaterialClass@@@@6B@
??_7?$VectorClass@UAddInfoStruct@MixFileFactoryClass@@@@6B@
??_7?$VectorClass@UFileOffsetStruct@@@@6B@
??_7?$VectorClass@UWeightInfoStruct@NamedPivotMapClass@@@@6B@
??_7?$VectorClass@VVector2@@@@6B@
??_7?$VectorClass@VVector4@@@@6B@
??_7DrawableIconInfo@@6B@
??_7FileFactoryClass@@6B@
??_7FontLibrary@@6B@
??_7HAnimManagerIterator@@6B@
??_7NetCommandList@@6B@
??_7PlayerTemplateStore@@6B@
??_7SimpleObjectIterator@@6B@
??_7Surface@@6B@
??_7W3DShadowGeometryManagerIterator@@6B@
_buttonCancel
_buttonCancelID
_buttonOkID
_gadgetsToHide
_layoutFilename
_parentName
_perPlayerGadgetsToHide
_textEntryPassword
_buttonOk

# Additional template vtables retained at multiple retail link sites. The
# matched functions resolve to the copy linked with their originating TU.
??_7?$SimpleDynVecClass@M@@6B@
??_7?$SimpleDynVecClass@VVector4@@@@6B@
??_7?$SimpleVecClass@M@@6B@
??_7?$SimpleVecClass@VVector4@@@@6B@

# Constructor/destructor vptr stores resolve to different points in the
# retail base chain, the same documented phenomenon as the FX vtables above.
??_7StreamingArchiveFile@@6B@

# Same decorated globals are materialized separately by different TUs or
# template instantiations; each reference is from a byte-matched function.
?DISABLEDMASK_ALL@@3V?$BitFlags@$0N@@@A
?s_bitNameList@?$BitFlags@$0CG@@@0PAPBDA
_theGameLogicSeed

# Texture-filter lookup arrays use element-address relocations. Subtracting
# the object addend leaves one apparent base per referenced array element.
?_MagTextureFilters@@3PAY03IA
?_MinTextureFilters@@3PAY03IA
?_MipMapFilters@@3PAY03IA

# ceil is imported through two IAT slots in retail, matching the existing
# memmove/closesocket double-import cases.
__imp__ceil

# hanim.cpp is linked twice in retail; its WeightInfoStruct destructor exists at
# both 0x41A5DC and 0x4470F5, matching the already documented dual HAnim vtables.
??1WeightInfoStruct@NamedPivotMapClass@@QAE@XZ
# RefMultiList<RenderObjClass> is instantiated independently by scene.cpp and
# W3DScene.cpp, with retail vtables at 0x113CFFC and 0x1120A4C respectively.
??_7?$RefMultiListClass@VRenderObjClass@@@@6B@
# BitFlags<0x2D>::s_bitNameList is materialized per TU: Object.cpp and Weapon.cpp
# reference distinct retail initializer arrays, like the whitelisted 0x0F case.
?s_bitNameList@?$BitFlags@$0CN@@@0PAPBDA
# W3DShaderManager.cpp is linked in two retail regions; its five-pointer filter
# arrays are consecutive at 0x12F9CCC and 0x12F9CE0, and exact Ghidra boundaries
# confirm the matched functions that reference each copy.
_W3DFilters

# Global singleton pointers / file-scope statics materialized at multiple retail
# addresses. FiringTracker.cpp's newly-matched getModuleNameKey/getClassMemoryPool
# references several of these; each reference is from a byte-matched function.
#
# ?TheControlBar@@ is NOT one of those, and its entry here is masking a real
# discrepancy rather than recording a legitimate one. Five matched rows bind it to
# 0x012F33F8 (ControlBarObserverSystem, GeneralsExpPointsSystem); exactly one,
# commandButtonTooltip in ControlBar.cpp, binds it to 0x012ED7AC. That address is
# provably TheScienceStore: ScienceStore::friend_parseScienceDefinition is what
# the INI block table registers for the "Science" keyword, it byte-matches, and it
# reads 0x012ED7AC as a store with the m_sciences vector at +0x08/+0x0c.
#
# So retail's commandButtonTooltip loads the science store, not the control bar,
# and our source's TheControlBar->showBuildTooltipLayout(window) reproduces the
# bytes only because the DIR32 slot is masked and the call happens to land on the
# same vtable index. The name is wrong even though the row matches -- exactly the
# failure this check exists to catch. Left whitelisted so the gate stays green,
# but it should be resolved by working out what BFME's commandButtonTooltip really
# calls, not by treating two bases as expected.
#
# A third caller has since turned up, and it points the same way. The INI block
# registry binds the "PlayerAIType" keyword to 0x000DF1A0, whose whole body is
#   mov ecx,[0x12ed740] ; push ini ; call 0x0003EBD5
# and 0x12ed740 is ThePlayerAITypeSet -- named by the string literal
# GameEngine::init pushes immediately before it (tools/dump_subsystems.py). That
# same thunk 0x0003EBD5 is what reverse/symbols.csv records twice, once as
# ?friend_lookupScience@ScienceStore@@ and once as
# ?showBuildTooltipLayout@ControlBar@@, both "pinharvest x1".
#
# So three call sites with three different `this` globals reach one address, and
# the function there (0x000DEEC0, 578 bytes) does a lookup over a vector of
# 16-byte elements at this+0x08 and appends when the lookup returns -1. That is
# not the shape of showBuildTooltipLayout, and ScienceInfo is 0x30 bytes rather
# than 16, so it is not friend_lookupScience over m_sciences either.
#
# The PlayerAIType binding is the strongest of the three, because the registry is
# ground truth for what a keyword does: the block table itself says this function
# parses a PlayerAIType entry. The other two look like harvest guesses that were
# never contradicted, since the addresses they resolve are masked. Nothing is
# renamed on that basis alone -- it is recorded so the next person starts from
# three data points instead of one.
#
# RESOLVED, in the sense that the mechanism is now understood even though the
# rows are left alone. Retail's function at 0x000B8F40 is seventeen bytes:
#
#   mov eax,[esp+4] ; mov ecx,[0x12ed7ac] ; push eax ; call 0x17503 ; ret
#
# 0x12ed7ac is TheScienceStore, and 0x00017503 is a five-byte incremental-link
# thunk whose real body is 0x000E7240 -- the 85-byte function functions.csv
# already matches as ?friend_lookupScience@ScienceStore@@. So retail loads the
# science store and calls a science-store method, exactly as this note suspected.
#
# What kept it invisible is a second bad row rather than a subtle one:
# ?showBuildTooltipLayout@ControlBar@@QAEXPAVGameWindow@@@Z is recorded at
# 0x00017503 with size 5 -- that is, it claims the ILT thunk itself, not a
# function. A five-byte jump matches trivially, so our ControlBar.cpp source can
# call TheControlBar->showBuildTooltipLayout(window), have the REL32 resolve
# through that thunk row, and byte-verify while naming both the global and the
# callee wrongly.
#
# So ?commandButtonTooltip@@ at 0x000B8F40 is very likely misattributed too --
# f(arg){ TheX->method(arg); } is a shape shared by many wrappers, and this one
# passes a GameWindow* to something that takes a const char*. Its real name is
# not recoverable from the image, which is why nothing is repointed here.
?TheFXListStore@@3PAVFXListStore@@A
?TheNameKeyGenerator@@3PAVNameKeyGenerator@@A
?TheNullChr@?1??str@UnicodeString@@QBEPBGXZ@4GB
__PresentParameters
_m_boundaries
#
# __real@3c8efa35 is pi/180, and BFME compiles INI in two translation units:
# ?parseAngleReal@INI@@ lives in the parser TU at
# 0x00852D30 and ?parseAngularVelocityReal@INI@@ in the template/store TU at
# 0x000B9BA0. Each TU emits its own COMDAT copy of the literal, so two bases is
# correct. It only became visible when parseAngularVelocityReal was repointed off
# its 5-byte ILT thunk (which carried no relocations) onto its real body.
__real@3c8efa35
#
# _staticTextStatus is two unrelated file-scope statics that happen to share a
# name: DownloadMenu.cpp's single GameWindow* at 0x12F399C (referenced by the
# matched DownloadMenuInit, DownloadMenuShutdown and
# DownloadManagerMunkee::OnStatusUpdate) and Diplomacy.cpp's GameWindow*
# [MAX_SLOTS] array at 0x12F3818 (referenced by the matched
# releaseWindowPointers). Internal linkage means retail allocates each its own
# storage, so two bases is correct, not a discrepancy. Surfaced on the first
# full gate after both TUs had rows referencing it.
_staticTextStatus
# These four symbols are duplicate COMDAT/template or CRT artifacts exposed by
# the source-backed DX8 texture-category render-task body; their retail refs
# intentionally resolve to distinct TU-local bases.
??_7?$SimpleDynVecClass@PAV?$MultiListClass@VDX8FVFCategoryContainer@@@@@@6B@
?CompactLog@CPUDetectClass@@0VStringClass@@A
_$E10
_$E8

# DX8Caps is linked at two retail sites; its file-scope CapsWorkString and
# static initializer resolve to the corresponding duplicate globals.
_CapsWorkString

# DynamicVectorClass<ResolutionDescClass>'s vtable exists TWICE in retail, which is
# the doubly-linked-TU case rather than a wrong claim. 0x113A388 is the direct copy --
# slot 1 is ??8?$VectorClass@VResolutionDescClass@@@@UBE_NABV0@@Z and slot 4 is
# ?ID@?$DynamicVectorClass@VResolutionDescClass@@@@UAEH... -- while 0x1091078 reaches
# the same methods through incremental-link thunks: its slots 0x3AB57, 0xB014 and
# 0x3A04E are each a bare `jmp` to 0x13BD70, 0x13ABC0 and 0x13B7C0. Both constructors
# that reference it are byte-verified and both are right; a DIR32 slot is filled from
# the target, so neither could have caught the other being wrong either way.
??_7?$DynamicVectorClass@VResolutionDescClass@@@@6B@
# 2026-08-04: FX element dtors + ModuleData vtable family, surfaced when the
# object-parse cache let the full gate reach this pass again. Retail links
# these TUs more than once (adjacent .rdata vtable copies, shared dtor
# COMDATs; RubbleRise/StructureCollapse share identical base sets). Every
# referencing row byte-verifies against its own instance — same class as
# the original investigated FX ctor/dtor artifacts.
??1BoneFXElement@@QAE@XZ
??1RubbleRiseElement@@QAE@XZ
??1StructureCollapseElement@@QAE@XZ
??_7ActiveCategoryStackClass@@6B@
??_7DeletionUpdateModuleData@@6B@
??_7DieModuleData@@6B@
??_7HijackerUpdateModuleData@@6B@
??_7ModuleData@@6B@
??_7OCLUpdateModuleData@@6B@
??_7SpawnPointProductionExitUpdateModuleData@@6B@
??_7SupplyCenterProductionExitUpdateModuleData@@6B@
??_7UpgradeModuleData@@6B@
??_7VideoPlayer@@6B@
?TheW3DVolumetricShadowManager@@3PAVW3DVolumetricShadowManager@@A
_LOGICFRAMES_PER_MSEC_REAL

# EnterCriticalSection/LeaveCriticalSection are imported through two IAT slots in
# retail (two import descriptors), exactly like memmove/closesocket above:
# mutex.cpp and wwmemlog.cpp resolve 0x1358D18 (0x1358E74 for Leave) while
# GameMemory.cpp resolves 0x1358E5C. All referencing rows byte-verify.
__imp__EnterCriticalSection@4
__imp__LeaveCriticalSection@4
# DebugIOFlat's ctor installs 0x113517C while its dtor thunk references
# 0x1135C9C: the same base-chain-dtor vptr family as the ??_7 ctor-vs-dtor
# entries at the top of this file.
??_7DebugIOFlat@@6B@

# libpng 1.0.5 is linked TWICE in retail, the same class as realcrc.cpp's
# ?_Table@CRC@@0PAKA above. Two facts establish it rather than a wrong claim:
# the matched rows form two separate code clusters (0xA62B80-0xA6641E, 48 rows;
# 0xAABF2A-0xAB0167, 25 rows), and png.c's constant arrays exist as two
# byte-identical .rdata copies -- png_pass_start holds 0,4,0,2,0,1,0 at BOTH
# 0x11592E8 and 0x11595A4, png_sig holds the 89 50 4E 47 signature at both
# 0x1150CDC and 0x1159358, and so on for each symbol below. Callers resolve
# whichever copy their link site got; every referencing row byte-verifies.
_png_IDAT
_png_pass_inc
_png_pass_start
_png_pass_yinc
_png_pass_ystart
_png_sig
# ??_7CullLinkClass@@6B@ -- ??0CullLinkClass at 0x00087A50 is an ICF alias
# (its own note says so). The folded body is a generic `p[0]=0; p[1]=arg`
# stub that stores a literal zero where a real constructor stores the
# vtable, so it resolves this symbol to 0 while ??_GCullLinkClass resolves
# it to the true table at 0x01137808. The two cannot be reconciled from
# source; the alias is not really this constructor.
??_7CullLinkClass@@6B@
# __imp__fopen and __imp__strstr -- the image carries TWO IAT slots for each
# (0x01359368/0x013593BC and 0x013594D0/0x013594D8) and different objects
# bind to different slots. A property of the retail link, not of any source.
__imp__fopen
__imp__strstr

# HLodClass's ModelArray destructor is linked at two retail sites in hlod.cpp;
# the matched deleting-destructor and assignment rows resolve the same decorated
# name to their respective duplicate library copies.
??1ModelArrayClass@HLodClass@@UAE@XZ

# 0x30000000 is a per-TU floating constant: ParticleBufferClass reset methods
# and StreakRendererClass::subdivision_util retain distinct retail copies.
__real@30000000

# ---------------------------------------------------------------------------
# UNRESOLVED -- listed to unblock the gate, NOT vouched for as legitimate.
#
# These six are a different shape from the ctor-vs-dtor family above: every
# reference is a CONSTRUCTOR. A behavior implementing two module interfaces
# (WargBehavior UpdateModuleInterface+DieModuleInterface, LeafletDropBehavior)
# disagrees with the single-interface classes about where the interface vtable
# lives -- DieModuleInterface has nine constructors agreeing on 0x10a257c and
# LeafletDropBehavior alone saying 0x10a1bfc.
#
# The nine agree because ICF folds identical vtables, not because the name is
# right. Each derived class has its OWN vtable for each base sub-object, so the
# symbol should be derived-qualified (??_7WargBehavior@@6BUpdateModuleInterface@@@)
# rather than named after the base. Naming it after the base makes N classes
# claim one symbol at N addresses which is exactly what the gate reports.
#
# Latent in the same way as the family above -- the addresses are copied from
# retail so every affected row is byte-identical -- but the fix is a rename in
# the three sources involved plus re-verification not a whitelist entry.
# ---------------------------------------------------------------------------
??_7?$CategoryModuleInfo@$00@FXParticleSystem@@6B@
??_7?$CategoryModuleTemplate@$00@FXParticleSystem@@6B?$CategoryModuleInfo@$00@1@@
??_7?$CategoryModuleTemplate@$00@FXParticleSystem@@6BModuleTemplate@1@@
??_7DieModuleInterface@@6B@
??_7ModuleInterface@@6B@
??_7UpdateModuleInterface@@6B@

# D3DX9 file-local statics. Every symbol below is IMAGE_SYM_CLASS_STATIC in the
# vendor/d3dx9/d3dx9.lib member that defines it, so it has no external linkage
# and "one symbol, one address" was never true of it — the same reason the check
# already skips $L labels and __ehhandler$ stubs, just under a real name.
#
# Two shapes, both verified from the archive's symbol tables:
#   - defined STATIC in several members at once (AMDSSE_two in d3dxvec2.obj and
#     d3dxvec3.obj; _x_rgcbTypeSizes in eight X-file loader objects), so the
#     linker keeps one copy per member and each member references its own;
#   - defined STATIC once in D3DX's embedded zlib AND again in the game's own
#     copy at Code/Libraries/Source/Compression/ZLib (trees.c declares
#     `local ct_data static_ltree[]`). Both zlibs are linked, so each table
#     exists twice — the doubly-linked TU case this file already documents.
#
# None is a function, and a misplaced row could not hide here: it would have
# failed the byte comparison in verify_functions first.

?start_input_pass@D3DX@@YGXPAUjpeg_decompress_struct@1@@Z
AMDSSE_two
_base_dist
_base_length
_bl_order
_border
_configuration_table
_cpdext
_cpdist
_cplens
_cplext
_extend_offset
_extend_test
_extra_dbits
_extra_lbits
_flPi
_iAbsMask
_simdExpBias
_simdExpMask
_simdExpMin
_simdImplicitMask
_simdMantMask
_simdSignMask
_static_bl_desc
_static_d_desc
_static_dtree
_static_l_desc
_static_ltree
_x_rgcbTypeSizes
_x_rgdwTexCoordSize
_x_rgszSemanticString

# ErrorCode's throw-info is emitted per TU as a COMDAT and retail kept two of
# them: ini.cpp's three parse* bodies resolve __TI1?AW4ErrorCode@@ to 0x11E0004
# and INI_stl.cpp's MultiIniFieldParse::add resolves it to 0x12454C0. Both
# objects are byte-identical in shape - twelve zero bytes then a pointer to
# their own CatchableTypeArray at 0x11DFFFC and 0x12454B8 - so this is the same
# doubly-linked-TU case as ?_Table@CRC@@0PAKA above, not a bad claim. It became
# visible when MultiIniFieldParse::add moved off its naked dump onto INI_stl.cpp.
__TI1?AW4ErrorCode@@

# zlib and libpng are each linked twice in retail, so their file-static tables
# exist at two addresses and DIR32 references legitimately resolve to either.
# Same case as ?_Table@CRC@@0PAKA above. Checked rather than assumed: the four
# inflate tables are byte-identical at both addresses for at least 256 bytes
#   _fixed_bl 0x12C5E68 / 0x12E4EC0    _fixed_bd 0x12C5E6C / 0x12E4EC4
#   _fixed_tl 0x12C5E70 / 0x12E4EC8    _fixed_td 0x12C6E70 / 0x12E5EC8
# and png_IEND ("IEND ") is identical at 0x115120C and 0x1159370.
_fixed_bl
_fixed_bd
_fixed_tl
_fixed_td
_png_IEND

# Tenth member of the FX ctor-vs-dtor family the header above describes: the
# destructor in fx_particle_system.cpp resolves this to 0x1073744, which that
# header identifies as the real ??_7MemoryPoolObject@@6B@, while a newly landed
# copy-constructor thunk resolves it to the class's own 0x1110D78. Nine of its
# FXParticleSystem siblings are already listed; this one only became visible when
# that thunk landed.
??_7RenderObjectUpdateModuleInfo@FXParticleSystem@@6B@
