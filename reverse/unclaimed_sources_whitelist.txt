# Sources under Code/ that deliberately carry no matched functions.csv row.
#
# The pre-commit hook rejects a staged source with zero matched rows, because
# normally that means someone committed a body they never verified. This file is
# the exception list, and an entry needs a reason that says what the source is
# for instead.
#
# Format: one repo-relative path per line, with the reason on the line above it.

# Win32BIGFileSystem::init, 109 of 111 bytes. The class identification is the
# point of the file rather than the body: TheArchiveFileSystem points at this
# class, its constructor at 0x009CC350 chains to ArchiveFileSystem's and installs
# vtable 0x01143B40, and the "*.big" literal at 0x0107532C is what names it. The
# body differs only in the scheduling of one instruction pair, and the header
# records everything already ruled out, so the next attempt starts two bytes from
# done. Claim it and delete this entry.
Code/GameEngineDevice/Source/Win32Device/Common/Win32BIGFileSystem_init.cpp
