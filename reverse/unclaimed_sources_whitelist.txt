# Source files allowed to exist with ZERO matched functions.csv rows.
#
# Everything else in Code/ MUST own at least one byte-verified matched row:
# source presence is not progress, and a file of present-unmatched markers
# must not be committable as if it were work.
# The build fails on any unlisted zero-claim source AND on any stale entry
# (file deleted, or file gained matched rows). Every entry needs a reason.

# Parked reconstruction with recorded evidence: closeWindows structural fix +
# logicMessageDispatcher whale recon. Carries an explicit status=unmatched pin
# row (0x00396950) that call-site resolution depends on; promoting it to
# matched is the open task, deleting it would lose the evidence trail.
Code/GameEngine/Source/GameLogic/ScriptEngine/game_logic_dispatch.cpp

# Landed verbatim; its only locatable function (??0Matrix4@@QAE@_N@Z COMDAT) is
# claimed by Code/Libraries/Source/WWVegas/WW3D2/projector.cpp after dedup (one row per address). Kept for
# its present-unmatched drift inventory until more of its functions match.
# Same situation: its located functions were COMDAT-deduped to other files'
# rows; carries drift markers pending reconciliation.
Code/GameEngine/Source/GameLogic/Object/Behavior/GrantStealthBehavior.cpp

# BEYOND-ZH probe: reconstructed Handicap::readFromDict from binary. 360/362
# bytes matched — 2-byte diff is MSVC codegen ordering (push-before-store vs
# store-before-push). See reverse/re_attempts.log for full analysis.
Code/GameEngine/Source/Common/RTS/Handicap.cpp
Code/GameEngine/Source/GameClient/water_transparency.cpp
# Third beyond-ZH probe target from r1s4; drafts parked with attempt evidence.

# The subsystem loader (0x000BB310): looks a subsystem up in SubsystemLegend by
# name and INI::loads every file its "LoadSubsystem" block lists. 181/214 bytes;
# the residue is the same MSVC unwind-esp/EH-state ordering divergence parked on
# Handicap.cpp above (record-then-load vs load-then-record). Retail's home for it
# is ini.cpp — same arena, same EH funcinfo run as INI::parseThingTemplate et al
# — but that file is still a ZH-tree port whose AsciiString inlines its release
# path, so this lives in a native sibling until ini.cpp is converted.
Code/GameEngine/Source/Common/INI/ini_subsystem.cpp

# GameEngine::init (0x00079060) reconstruction. Emits 4026 of retail's 6689 bytes
# with ~660 agreeing; the 60-subsystem registration sequence is faithful and comes
# from reverse/gameengine_init_subsystems.tsv, but retail does un-named setup
# allocations before the first registration, our operator new resolves elsewhere,
# and ~1700 bytes of init are not registrations and are not reversed yet. Kept for
# the registration order and the evidence trail; 0x00079060 still belongs to the
# MASM dump. Its stub classes carry provisional names — see the header.
Code/GameEngine/Source/Common/System/game_engine_init.cpp
