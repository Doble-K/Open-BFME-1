# Work packet: retail `0x000C4240`

- 45 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 95.6% of the bytes outside relocation sites

## Reference source leads

- `?canFireWeaponAtLocation@ActionManager@@QAE_NPBVObject@@PBUCoord3D@@W4CommandSourceType@@W4WeaponSlotType@@0@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/RTS/ActionManager.cpp` (95.6% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
   c4240:	8b 44 24 04          	mov    eax,DWORD PTR [esp+0x4]
   c4244:	85 c0                	test   eax,eax
   c4246:	74 20                	je     0xc4268
   c4248:	8b 4c 24 08          	mov    ecx,DWORD PTR [esp+0x8]
   c424c:	85 c9                	test   ecx,ecx
   c424e:	74 18                	je     0xc4268
   c4250:	8b 4c 24 10          	mov    ecx,DWORD PTR [esp+0x10]
   c4254:	51                   	push   ecx
   c4255:	8d 88 64 02 00 00    	lea    ecx,[eax+0x264]
   c425b:	e8 89 86 f7 ff       	call   0x3c8e9
   c4260:	85 c0                	test   eax,eax
   c4262:	0f 95 c0             	setne  al
   c4265:	c2 14 00             	ret    0x14
   c4268:	32 c0                	xor    al,al
   c426a:	c2 14 00             	ret    0x14
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?getWeaponInWeaponSlot@WeaponSet@@QBEPAVWeapon@@W4WeaponSlotType@@@Z,0x0003C8E9 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
