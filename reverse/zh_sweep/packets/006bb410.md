# Work packet: retail `0x006BB410`

- 54 bytes, unclaimed by any ledger row, an extent measured from
  reverse/ghidra_functions.csv
- address is a confirmed function start (reverse/ghidra_functions.csv)
- best Zero Hour candidate agrees on 90.5% of the bytes outside relocation sites: 38 of
  42 compared byte(s) match
- the other 12 byte(s) of its 54-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 92.6% counts every blanked byte as agreeing

## Reference source leads

- `??0DirectInputKeyboard@@QAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/Win32Device/GameClient/Win32DIKeyboard.cpp` (90.5% of 42 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (54 bytes from this address)

```
  6bb410:	56                   	push   esi
  6bb411:	8b f1                	mov    esi,ecx
  6bb413:	e8 30 dd 98 ff       	call   0x49148
  6bb418:	33 c0                	xor    eax,eax
  6bb41a:	6a 14                	push   0x14
  6bb41c:	c7 06 cc cd 11 01    	mov    DWORD PTR [esi],0x111cdcc
  6bb422:	89 86 1c 0e 00 00    	mov    DWORD PTR [esi+0xe1c],eax
  6bb428:	89 86 20 0e 00 00    	mov    DWORD PTR [esi+0xe20],eax
  6bb42e:	ff 15 04 90 35 01    	call   DWORD PTR ds:0x1359004
  6bb434:	a8 01                	test   al,0x1
  6bb436:	8b c6                	mov    eax,esi
  6bb438:	74 06                	je     0x6bb440
  6bb43a:	80 4e 09 02          	or     BYTE PTR [esi+0x9],0x2
  6bb43e:	5e                   	pop    esi
  6bb43f:	c3                   	ret
  6bb440:	80 66 09 fd          	and    BYTE PTR [esi+0x9],0xfd
  6bb444:	5e                   	pop    esi
  6bb445:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??0Keyboard@@QAE@XZ,0x00049148
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
