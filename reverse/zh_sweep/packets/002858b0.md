# Work packet: retail `0x002858B0`

- 36 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 91.7% of the bytes outside relocation sites

## Reference source leads

- `?recenterTurret@BattlePlanUpdate@@IAEXXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Update/BattlePlanUpdate.cpp` (91.7% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  2858b0:	8b 41 08             	mov    eax,DWORD PTR [ecx+0x8]
  2858b3:	56                   	push   esi
  2858b4:	8b b0 04 02 00 00    	mov    esi,DWORD PTR [eax+0x204]
  2858ba:	85 f6                	test   esi,esi
  2858bc:	74 14                	je     0x2858d2
  2858be:	8b ce                	mov    ecx,esi
  2858c0:	e8 de ed da ff       	call   0x346a3
  2858c5:	83 f8 ff             	cmp    eax,0xffffffff
  2858c8:	74 08                	je     0x2858d2
  2858ca:	50                   	push   eax
  2858cb:	8b ce                	mov    ecx,esi
  2858cd:	e8 d6 77 d8 ff       	call   0xd0a8
  2858d2:	5e                   	pop    esi
  2858d3:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?getWhichTurretForCurWeapon@AIUpdateInterface@@QBE?AW4WhichTurretType@@XZ,0x000346A3 (already in the ledger)
?recenterTurret@AIUpdateInterface@@QAEXW4WhichTurretType@@@Z,0x0000D0A8 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
