# Work packet: retail `0x00C02680`

- 51 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 90.2% of the bytes outside relocation sites

## Reference source leads

- `$L44544`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/System/CampaignManager.cpp` (90.2% aligned)
- `$L51681`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/System/SaveGame/GameStateMap.cpp` (90.2% aligned)
- `$L55230`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/GUI/GUICallbacks/Menus/ChallengeMenu.cpp` (90.2% aligned)
- `$L58117`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/GUI/GUICallbacks/Menus/SkirmishGameOptionsMenu.cpp` (90.2% aligned)

Retail folded 4 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  c02680:	8b 4d f0 e9 4c 4e 43 ff 8b 4d f0 83 c1 60 e9 95
  c02690:	b1 40 ff 68 3b 2d 42 00 6a 03 6a 0c 8b 45 f0 83
  c026a0:	c0 6c 50 e8 ce 46 df ff c3 8b 4d f0 81 c1 9c 00
  c026b0:	00 00 e9
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??1GameInfo@@QAE@XZ,0x000374D4
??1Snapshot@@QAE@XZ,0x0000D828 (already in the ledger)
??_M@YGXPAXIHP6EX0@Z@Z,0x009F6D76 (already in the ledger)
___CxxFrameHandler,0x-163FD94D
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
