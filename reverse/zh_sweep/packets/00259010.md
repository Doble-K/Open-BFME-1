# Work packet: retail `0x00259010`

- 46 bytes, unclaimed by any ledger row, an extent measured from
  reverse/ghidra_functions.csv
- address is a confirmed function start (reverse/ghidra_functions.csv)
- best Zero Hour candidate agrees on 97.8% of the bytes outside relocation sites
- that figure hides 4 blanked relocation slot(s): 1 of 46 compared byte(s) differ, and
  which global and which callee this body uses was never compared at all

## Reference source leads

4 of the candidates below align equally well, so the sweep cannot tell them apart:
masking the relocation sites hides exactly the bytes that differ between them — which
global the body loads, which callee it tail-jumps to. The first is not the answer.
0x002EFAF0 was landed under the wrong one of three such ties, and the call targets below
are what separated them.

- `??0W3DOverlordAircraftDraw@@QAE@PAVThing@@PBVModuleData@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/Drawable/Draw/W3DOverlordAircraftDraw.cpp` (97.8% aligned)
- `??0W3DOverlordTankDraw@@QAE@PAVThing@@PBVModuleData@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/Drawable/Draw/W3DOverlordTankDraw.cpp` (97.8% aligned)
- `??0W3DOverlordTruckDraw@@QAE@PAVThing@@PBVModuleData@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/Drawable/Draw/W3DOverlordTruckDraw.cpp` (97.8% aligned)
- `??0W3DScienceModelDraw@@QAE@PAVThing@@PBVModuleData@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/Drawable/Draw/W3DScienceModelDraw.cpp` (97.8% aligned)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (46 bytes from this address)

```
  259010:	8b 44 24 08          	mov    eax,DWORD PTR [esp+0x8]
  259014:	56                   	push   esi
  259015:	8b f1                	mov    esi,ecx
  259017:	8b 4c 24 08          	mov    ecx,DWORD PTR [esp+0x8]
  25901b:	50                   	push   eax
  25901c:	51                   	push   ecx
  25901d:	8b ce                	mov    ecx,esi
  25901f:	e8 2c 8e db ff       	call   0x11e50
  259024:	c7 06 a4 3c 0b 01    	mov    DWORD PTR [esi],0x10b3ca4
  25902a:	c7 46 0c e0 3b 0b 01 	mov    DWORD PTR [esi+0xc],0x10b3be0
  259031:	c7 46 10 68 3b 0b 01 	mov    DWORD PTR [esi+0x10],0x10b3b68
  259038:	8b c6                	mov    eax,esi
  25903a:	5e                   	pop    esi
  25903b:	c2 08 00             	ret    0x8
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??0W3DModelDraw@@QAE@PAVThing@@PBVModuleData@@@Z,0x00011E50
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
