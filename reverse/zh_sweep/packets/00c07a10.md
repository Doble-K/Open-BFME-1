# Work packet: retail `0x00C07A10`

- 57 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 96.5% of the bytes outside relocation sites

## Reference source leads

- `$L46964`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/RTS/PlayerList.cpp` (96.5% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  c07a10:	8b 4d ec             	mov    ecx,DWORD PTR [ebp-0x14]
  c07a13:	e9 28 a0 d9 ff       	jmp    0x9a1a40
  c07a18:	83 7d ec 00          	cmp    DWORD PTR [ebp-0x14],0x0
  c07a1c:	0f 84 0e 00 00 00    	je     0xc07a30
  c07a22:	8b 45 ec             	mov    eax,DWORD PTR [ebp-0x14]
  c07a25:	83 c0 08             	add    eax,0x8
  c07a28:	89 45 e8             	mov    DWORD PTR [ebp-0x18],eax
  c07a2b:	e9 07 00 00 00       	jmp    0xc07a37
  c07a30:	c7 45 e8 00 00 00 00 	mov    DWORD PTR [ebp-0x18],0x0
  c07a37:	8b 4d e8             	mov    ecx,DWORD PTR [ebp-0x18]
  c07a3a:	e9 41 a2 3f ff       	jmp    0x1c80
  c07a3f:	8b 4d ec             	mov    ecx,DWORD PTR [ebp-0x14]
  c07a42:	83 c1 0c             	add    ecx,0xc
  c07a45:	e9                   	.byte 0xe9
  c07a46:	dd 67 41             	frstor [edi+0x41]
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??1SubsystemInterface@@UAE@XZ,0x009A1A40 (already in the ledger)
??1Snapshot@@QAE@XZ,0x00001C80 (already in the ledger)
___CxxFrameHandler,0x42285832
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.


## Classified 2026-08-14 -- EH unwind funclet, not a function

Bulk sweep over all remaining packets flagged this one by shape, and the
disassembly in this packet confirms it: 3 `mov ecx,DWORD PTR [ebp-0xNN]` loads
feeding 3 `jmp` instructions to destructor addresses (0x1c80, 0x9a1a40, 0xc07a37), with no prologue, no
epilogue and no single coherent control flow. Some blocks carry a null-check
(`cmp DWORD PTR [ebp-0xNN],0x0` / `je`) before computing a sub-object offset --
that is the compiler's conditional-cleanup shape, not user code.

This is compiler-generated per-member cleanup on an exception path. No portable
C++ body exists here; reproducing the bytes would require inventing member
layout to force a match, which the rules forbid.

Two cheap tests that identify this class without disassembling:
- a chain of `mov ecx,[ebp-N]; jmp <dtor>` groups
- jump targets already pinned in reverse/symbols.csv under `Gen_uw_*` names,
  which are ICF-folded generic trivial-destructor stubs reused by hundreds of
  unrelated classes and therefore carry no class identity

Reclassify: not a portable candidate.
