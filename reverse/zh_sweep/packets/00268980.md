# Work packet: retail `0x00268980`

- 33 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 87.9% of the bytes outside relocation sites

## Reference source leads

- `?getRadiusCursorRadius@SpecialPowerTemplate@@QBEMXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/ScriptEngine/ScriptActions.cpp` (87.9% aligned)
- `?getViewObjectRange@SpecialPowerTemplate@@QBEMXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/SpecialPower/SpecialPowerModule.cpp` (87.9% aligned)

Retail folded 2 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  268980:	8b 41 04             	mov    eax,DWORD PTR [ecx+0x4]
  268983:	85 c0                	test   eax,eax
  268985:	74 13                	je     0x26899a
  268987:	8b 48 04             	mov    ecx,DWORD PTR [eax+0x4]
  26898a:	85 c9                	test   ecx,ecx
  26898c:	74 05                	je     0x268993
  26898e:	e8 ce 02 de ff       	call   0x48c61
  268993:	d9 80 0c 01 00 00    	fld    DWORD PTR [eax+0x10c]
  268999:	c3                   	ret
  26899a:	d9 81 0c 01 00 00    	fld    DWORD PTR [ecx+0x10c]
  2689a0:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?friend_getFinalOverride@Overridable@@QAEPAV1@XZ,0x00048C61 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
