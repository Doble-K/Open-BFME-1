# Work packet: retail `0x00956AA0`

- 27 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 92.6% of the bytes outside relocation sites

## Reference source leads

- `?Set_Position@AABoxRenderObjClass@@UAEXABVVector3@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/boxrobj.cpp` (92.6% aligned)
- `?Set_Position@OBBoxRenderObjClass@@UAEXABVVector3@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/boxrobj.cpp` (92.6% aligned)
- `?Set_Transform@AABoxRenderObjClass@@UAEXABVMatrix3D@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/boxrobj.cpp` (92.6% aligned)
- `?Set_Transform@OBBoxRenderObjClass@@UAEXABVMatrix3D@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/boxrobj.cpp` (92.6% aligned)

Retail folded 4 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  956aa0:	8b 44 24 04          	mov    eax,DWORD PTR [esp+0x4]
  956aa4:	56                   	push   esi
  956aa5:	50                   	push   eax
  956aa6:	8b f1                	mov    esi,ecx
  956aa8:	e8 f3 96 fc ff       	call   0x9201a0
  956aad:	8b 16                	mov    edx,DWORD PTR [esi]
  956aaf:	8b ce                	mov    ecx,esi
  956ab1:	ff 92 0c 02 00 00    	call   DWORD PTR [edx+0x20c]
  956ab7:	5e                   	pop    esi
  956ab8:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?Set_Position@RenderObjClass@@UAEXABVVector3@@@Z,0x009201A0 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
