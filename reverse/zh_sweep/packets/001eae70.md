# Work packet: retail `0x001EAE70`

- 42 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 97.6% of the bytes outside relocation sites

## Reference source leads

- `?isOutOfAmmo@WeaponSet@@QBE_NXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/WeaponSet.cpp` (97.6% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  1eae70:	56                   	push   esi
  1eae71:	57                   	push   edi
  1eae72:	33 ff                	xor    edi,edi
  1eae74:	8d 71 08             	lea    esi,[ecx+0x8]
  1eae77:	8b 0e                	mov    ecx,DWORD PTR [esi]
  1eae79:	85 c9                	test   ecx,ecx
  1eae7b:	74 0a                	je     0x1eae87
  1eae7d:	e8 0a e9 e1 ff       	call   0x978c
  1eae82:	83 f8 01             	cmp    eax,0x1
  1eae85:	75 0e                	jne    0x1eae95
  1eae87:	47                   	inc    edi
  1eae88:	83 c6 04             	add    esi,0x4
  1eae8b:	83 ff 04             	cmp    edi,0x4
  1eae8e:	7c e7                	jl     0x1eae77
  1eae90:	5f                   	pop    edi
  1eae91:	b0 01                	mov    al,0x1
  1eae93:	5e                   	pop    esi
  1eae94:	c3                   	ret
  1eae95:	5f                   	pop    edi
  1eae96:	32 c0                	xor    al,al
  1eae98:	5e                   	pop    esi
  1eae99:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?getStatus@Weapon@@QBE?AW4WeaponStatus@@XZ,0x0000978C
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
