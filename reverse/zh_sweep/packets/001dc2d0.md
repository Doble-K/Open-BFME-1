# Work packet: retail `0x001DC2D0`

- 40 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 95.0% of the bytes outside relocation sites

## Reference source leads

- `?isInSet@ObjectTypes@@QBE_NABVAsciiString@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/ObjectTypes.cpp` (95.0% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  1dc2d0:	8b 54 24 04          	mov    edx,DWORD PTR [esp+0x4]
  1dc2d4:	56                   	push   esi
  1dc2d5:	8b 71 0c             	mov    esi,DWORD PTR [ecx+0xc]
  1dc2d8:	8d 44 24 08          	lea    eax,[esp+0x8]
  1dc2dc:	50                   	push   eax
  1dc2dd:	8b 41 08             	mov    eax,DWORD PTR [ecx+0x8]
  1dc2e0:	52                   	push   edx
  1dc2e1:	56                   	push   esi
  1dc2e2:	50                   	push   eax
  1dc2e3:	e8 b2 ef e6 ff       	call   0x4b29a
  1dc2e8:	83 c4 10             	add    esp,0x10
  1dc2eb:	33 c9                	xor    ecx,ecx
  1dc2ed:	3b c6                	cmp    eax,esi
  1dc2ef:	0f 95 c1             	setne  cl
  1dc2f2:	8a c1                	mov    al,cl
  1dc2f4:	5e                   	pop    esi
  1dc2f5:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??$__find@PBVAsciiString@@V1@@_STL@@YAPBVAsciiString@@PBV1@0ABV1@ABUrandom_access_iterator_tag@0@@Z,0x0004B29A
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
