# Work packet: retail `0x00C27C7D`

**This address is not a function start.** The sweep placed a Zero Hour body here by byte
alignment alone, and this repo's own evidence refutes it: the address is 3 byte(s)
inside the function at 0x00C27C7A, which is 11 bytes long and is the body worth
converting here. Do not add a ledger row at this address; the bytes below say what is
here, but the address in this packet's name is not the start of it.

- 65 bytes, unclaimed by any ledger row, an UNVERIFIED size — the Zero Hour candidate's
  own length, which nothing here measures against retail
- address is NOT a function start (interior-of-function) — see above
- best Zero Hour candidate agrees on 75.7% of the bytes outside relocation sites: 28 of
  37 compared byte(s) match
- the other 28 byte(s) of its 65-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 86.2% counts every blanked byte as agreeing
- the quoted range still crosses a boundary (spans-5-function-start(s)), so its end is
  in dispute

## Reference source leads

- `$L96101`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/RTS/Team.cpp` (75.7% of 37 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (65 bytes from this address)

```
  c27c7d:	83 c1 5c             	add    ecx,0x5c
  c27c80:	e9 a3 5b 3e ff       	jmp    0xd828
  c27c85:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c27c88:	83 c1 60             	add    ecx,0x60
  c27c8b:	e9 98 5b 3e ff       	jmp    0xd828
  c27c90:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c27c93:	83 c1 64             	add    ecx,0x64
  c27c96:	e9 8d 5b 3e ff       	jmp    0xd828
  c27c9b:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c27c9e:	83 c1 68             	add    ecx,0x68
  c27ca1:	e9 82 5b 3e ff       	jmp    0xd828
  c27ca6:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c27ca9:	81 c1 84 00 00 00    	add    ecx,0x84
  c27caf:	e9 c3 d5 3d ff       	jmp    0x5277
  c27cb4:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c27cb7:	81 c1 94 00 00 00    	add    ecx,0x94
  c27cbd:	e9                   	.byte 0xe9
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??1MemoryPoolObject@@MAE@XZ,0x0000D828
??1Snapshot@@QAE@XZ,0x0000D828 (already in the ledger)
??1AsciiString@@QAE@XZ,0x0000D828 (already in the ledger)
??1TeamTemplateInfo@@QAE@XZ,0x0000D828 (already in the ledger)
??1AsciiString@@QAE@XZ,0x00005277 (already in the ledger)
___CxxFrameHandler,0x-163D8342
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
