# Work packet: retail `0x00118790`

- 32 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 93.8% of the bytes outside relocation sites

## Reference source leads

- `??0AssaultTransportAIUpdateModuleData@@QAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/Thing/ModuleFactory.cpp` (93.8% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  118790:	56                   	push   esi
  118791:	8b f1                	mov    esi,ecx
  118793:	e8 a3 02 f3 ff       	call   0x48a3b
  118798:	c7 06 c0 9c 08 01    	mov    DWORD PTR [esi],0x1089cc0
  11879e:	c7 46 64 00 00 00 00 	mov    DWORD PTR [esi+0x64],0x0
  1187a5:	c7 46 68 00 00 48 42 	mov    DWORD PTR [esi+0x68],0x42480000
  1187ac:	8b c6                	mov    eax,esi
  1187ae:	5e                   	pop    esi
  1187af:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??0AIUpdateModuleData@@QAE@XZ,0x00048A3B
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.


## Investigated 2026-08-14 -- name already claimed elsewhere, at a different size

`??0AssaultTransportAIUpdateModuleData@@QAE@XZ` is **already matched**, at 0x0027EAE0, **283 bytes**. This body is
**32**. One constructor cannot be both, and the size gap is far too large for
a boundary quibble.

This is the module-data constructor form of the false positive packets
007e87f0 and 00626060 document: "call the base constructor, store the vtable,
initialise N members" is the same shape for every module data class, the
vtable operand is a relocation and masked from the comparison, and the
similarity metric therefore cannot separate them. What little signal there is
lives in N and in the element offsets.

Before landing anything here, check whether the candidate name is already
claimed at another address -- that single check refutes it outright, and it
is cheaper than any byte work.
