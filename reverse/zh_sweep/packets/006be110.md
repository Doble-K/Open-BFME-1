# Work packet: retail `0x006BE110`

- 38 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 89.5% of the bytes outside relocation sites

## Reference source leads

- `?reset@W3DTerrainLogic@@UAEXXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameLogic/W3DTerrainLogic.cpp` (89.5% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  6be110:	56                   	push   esi
  6be111:	8b f1                	mov    esi,ecx
  6be113:	e8 ef d5 95 ff       	call   0x1b707
  6be118:	33 c0                	xor    eax,eax
  6be11a:	89 46 0c             	mov    DWORD PTR [esi+0xc],eax
  6be11d:	89 46 10             	mov    DWORD PTR [esi+0x10],eax
  6be120:	89 86 f8 18 00 00    	mov    DWORD PTR [esi+0x18f8],eax
  6be126:	c7 86 fc 18 00 00 00 	mov    DWORD PTR [esi+0x18fc],0x3f800000
  6be12d:	00 80 3f 
  6be130:	5e                   	pop    esi
  6be131:	e9 f1 8f 98 ff       	jmp    0x47127
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?reset@TerrainLogic@@UAEXXZ,0x0001B707
?freeListOfMapObjects@WorldHeightMap@@SAXXZ,0x00047127
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
