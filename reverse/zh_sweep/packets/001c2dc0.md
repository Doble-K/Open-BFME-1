# Work packet: retail `0x001C2DC0`

- 30 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 96.7% of the bytes outside relocation sites

## Reference source leads

- `?getName@Weapon@@QBE?AVAsciiString@@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/MessageStream/CommandXlat.cpp` (96.7% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  1c2dc0:	51                   	push   ecx
  1c2dc1:	8b 49 30             	mov    ecx,DWORD PTR [ecx+0x30]
  1c2dc4:	56                   	push   esi
  1c2dc5:	8b 74 24 0c          	mov    esi,DWORD PTR [esp+0xc]
  1c2dc9:	56                   	push   esi
  1c2dca:	c7 44 24 08 00 00 00 	mov    DWORD PTR [esp+0x8],0x0
  1c2dd1:	00 
  1c2dd2:	e8 37 a9 e6 ff       	call   0x2d70e
  1c2dd7:	8b c6                	mov    eax,esi
  1c2dd9:	5e                   	pop    esi
  1c2dda:	59                   	pop    ecx
  1c2ddb:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?getName@WeaponTemplate@@QBE?AVAsciiString@@XZ,0x0002D70E
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
