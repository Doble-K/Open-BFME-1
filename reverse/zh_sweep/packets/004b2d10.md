# Work packet: retail `0x004B2D10`

- 137 bytes, unclaimed by any ledger row, an extent measured from the int3 padding at
  0x004B2D99
- no inventory row confirms a function starts at this address; treat the start as
  unverified
- best Zero Hour candidate agrees on 93.5% of the bytes outside relocation sites: 87 of
  93 compared byte(s) match
- the other 44 byte(s) of its 137-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 95.6% counts every blanked byte as agreeing

## Reference source leads

- `?setPlayerName@EstablishConnectionsMenu@@QAEXHVUnicodeString@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/GUI/EstablishConnectionsMenu/EstablishConnectionsMenu.cpp` (93.5% of 93 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (137 bytes from this address)

```
  4b2d10:	64 a1 00 00 00 00 6a ff 68 48 8b 02 01 50 64 89
  4b2d20:	25 00 00 00 00 56 8b 44 24 14 8b 0c 85 f8 66 2b
  4b2d30:	01 51 8b 0d 00 d6 2e 01 c7 44 24 10 00 00 00 00
  4b2d40:	e8 92 80 b8 ff 8b 0d 40 1b 2f 01 8b 11 50 6a 00
  4b2d50:	ff 92 dc 00 00 00 8b f0 85 f6 74 1a 51 8d 44 24
  4b2d60:	1c 89 64 24 18 8b cc 50 e8 93 56 3d 00 56 e8 fc
  4b2d70:	93 b7 ff 83 c4 08 8d 4c 24 18 c7 44 24 0c ff ff
  4b2d80:	ff ff e8 49 54 3d 00 8b 4c 24 04 64 89 0d 00 00
  4b2d90:	00 00 5e 83 c4 0c c2 08 00
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?nameToKey@NameKeyGenerator@@QAE?AW4NameKeyType@@PBD@Z,0x0003ADD7 (already in the ledger)
??0UnicodeString@@QAE@ABV0@@Z,0x00888400 (already in the ledger)
?GadgetStaticTextSetText@@YAXPAVGameWindow@@VUnicodeString@@@Z,0x0002C16F (already in the ledger)
?releaseBuffer@UnicodeString@@IAEXXZ,0x008881D0 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
