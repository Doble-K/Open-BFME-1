# Work packet: retail `0x006BE0E0`

- 34 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 88.2% of the bytes outside relocation sites

## Reference source leads

- `?init@W3DTerrainLogic@@UAEXXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameLogic/W3DTerrainLogic.cpp` (88.2% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  6be0e0:	56                   	push   esi
  6be0e1:	8b f1                	mov    esi,ecx
  6be0e3:	e8 ac 98 96 ff       	call   0x27994
  6be0e8:	33 c0                	xor    eax,eax
  6be0ea:	89 46 0c             	mov    DWORD PTR [esi+0xc],eax
  6be0ed:	89 46 10             	mov    DWORD PTR [esi+0x10],eax
  6be0f0:	89 86 f8 18 00 00    	mov    DWORD PTR [esi+0x18f8],eax
  6be0f6:	c7 86 fc 18 00 00 00 	mov    DWORD PTR [esi+0x18fc],0x3f800000
  6be0fd:	00 80 3f 
  6be100:	5e                   	pop    esi
  6be101:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?init@TerrainLogic@@UAEXXZ,0x00027994
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
