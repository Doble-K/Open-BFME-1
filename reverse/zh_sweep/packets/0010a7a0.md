# Work packet: retail `0x0010A7A0`

- 26 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 88.5% of the bytes outside relocation sites

## Reference source leads

- `??0AABTreeLinkClass@@QAE@PAVAABTreeCullSystemClass@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WWMath/aabtreecull.cpp` (88.5% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  10a7a0:	8b c1                	mov    eax,ecx
  10a7a2:	8b 4c 24 04          	mov    ecx,DWORD PTR [esp+0x4]
  10a7a6:	89 48 04             	mov    DWORD PTR [eax+0x4],ecx
  10a7a9:	33 c9                	xor    ecx,ecx
  10a7ab:	c7 00 50 8d 08 01    	mov    DWORD PTR [eax],0x1088d50
  10a7b1:	89 48 08             	mov    DWORD PTR [eax+0x8],ecx
  10a7b4:	89 48 0c             	mov    DWORD PTR [eax+0xc],ecx
  10a7b7:	89 48 10             	mov    DWORD PTR [eax+0x10],ecx
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
