# Work packet: retail `0x00491550`

- 34 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 88.2% of the bytes outside relocation sites

## Reference source leads

- `?newMap@W3DTerrainLogic@@UAEX_N@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameLogic/W3DTerrainLogic.cpp` (88.2% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  491550:	56                   	push   esi
  491551:	57                   	push   edi
  491552:	8b 7c 24 0c          	mov    edi,DWORD PTR [esp+0xc]
  491556:	8b f1                	mov    esi,ecx
  491558:	8b 46 10             	mov    eax,DWORD PTR [esi+0x10]
  49155b:	57                   	push   edi
  49155c:	50                   	push   eax
  49155d:	e8 18 06 b9 ff       	call   0x21b7a
  491562:	83 c4 08             	add    esp,0x8
  491565:	57                   	push   edi
  491566:	8b ce                	mov    ecx,esi
  491568:	e8 77 2a b7 ff       	call   0x3fe4
  49156d:	5f                   	pop    edi
  49156e:	5e                   	pop    esi
  49156f:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?loadRoadsAndBridges@BaseHeightMapRenderObjClass@@QAEXPAVW3DTerrainLogic@@_N@Z,0x090D9964
?newMap@TerrainLogic@@UAEX_N@Z,0x00003FE4
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
