# Work packet: retail `0x0039EE00`

- 64 bytes, unclaimed by any ledger row, an extent measured from
  reverse/ghidra_functions.csv
- address is a confirmed function start (reverse/ghidra_functions.csv)
- best Zero Hour candidate agrees on 98.2% of the bytes outside relocation sites: 55 of
  56 compared byte(s) match
- the other 8 byte(s) of its 64-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 98.4% counts every blanked byte as agreeing

## Reference source leads

- `?push_back@?$vector@URunwayInfo@FlightDeckBehavior@@V?$allocator@URunwayInfo@FlightDeckBehavior@@@_STL@@@_STL@@QAEXABURunwayInfo@FlightDeckBehavior@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Behavior/FlightDeckBehavior.cpp` (98.2% of 56 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (64 bytes from this address)

```
  39ee00:	56 8b f1 8b 4e 08 8b 46 04 3b c1 8b 4c 24 08 74
  39ee10:	19 51 50 e8 31 92 c6 ff 8b 46 04 83 c4 08 05 88
  39ee20:	00 00 00 89 46 04 5e c2 04 00 6a 01 6a 01 8d 54
  39ee30:	24 10 52 51 50 8b ce e8 4d 6e c8 ff 5e c2 04 00
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??$_Construct@URunwayInfo@FlightDeckBehavior@@U12@@_STL@@YAXPAURunwayInfo@FlightDeckBehavior@@ABU12@@Z,0x00008049
?_M_insert_overflow@?$vector@URunwayInfo@FlightDeckBehavior@@V?$allocator@URunwayInfo@FlightDeckBehavior@@@_STL@@@_STL@@IAEXPAURunwayInfo@FlightDeckBehavior@@ABU34@ABU__false_type@2@I_N@Z,0x00025C89
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
