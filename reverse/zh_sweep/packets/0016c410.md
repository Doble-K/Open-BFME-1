# Work packet: retail `0x0016C410`

- 25 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 92.0% of the bytes outside relocation sites

## Reference source leads

- `?IsInGameChatActive@@YA_NXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/GUI/GUICallbacks/InGameChat.cpp` (92.0% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  16c410:	8b 41 58             	mov    eax,DWORD PTR [ecx+0x58]
  16c413:	8b 48 1c             	mov    ecx,DWORD PTR [eax+0x1c]
  16c416:	85 c9                	test   ecx,ecx
  16c418:	74 0c                	je     0x16c426
  16c41a:	8b 01                	mov    eax,DWORD PTR [ecx]
  16c41c:	ff 50 1c             	call   DWORD PTR [eax+0x1c]
  16c41f:	84 c0                	test   al,al
  16c421:	75 03                	jne    0x16c426
  16c423:	b0 01                	mov    al,0x1
  16c425:	c3                   	ret
  16c426:	32 c0                	xor    al,al
  16c428:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?winIsHidden@GameWindow@@QAE_NXZ,0x1C67C320 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
