# Work packet: retail `0x00673640`

- 57 bytes, unclaimed by any ledger row, an extent measured from
  reverse/ghidra_functions.csv
- address is a confirmed function start (reverse/ghidra_functions.csv)
- best Zero Hour candidate agrees on 85.7% of the bytes outside relocation sites: 42 of
  49 compared byte(s) match
- the other 4 byte(s) of its 53-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 86.8% counts every blanked byte as agreeing
- the candidate body is 53 bytes long, so it does not have retail's extent here; the
  bytes quoted below are retail's

## Reference source leads

- `??0NetGameCommandMsg@@QAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameNetwork/NetCommandMsg.cpp` (85.7% of 49 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (57 bytes from this address)

```
  673640:	8b c1                	mov    eax,ecx
  673642:	33 c9                	xor    ecx,ecx
  673644:	c7 40 08 ff ff ff ff 	mov    DWORD PTR [eax+0x8],0xffffffff
  67364b:	66 89 48 10          	mov    WORD PTR [eax+0x10],cx
  67364f:	89 48 0c             	mov    DWORD PTR [eax+0xc],ecx
  673652:	89 48 04             	mov    DWORD PTR [eax+0x4],ecx
  673655:	c7 40 18 01 00 00 00 	mov    DWORD PTR [eax+0x18],0x1
  67365c:	c7 00 74 a4 11 01    	mov    DWORD PTR [eax],0x111a474
  673662:	89 48 20             	mov    DWORD PTR [eax+0x20],ecx
  673665:	89 48 1c             	mov    DWORD PTR [eax+0x1c],ecx
  673668:	89 48 24             	mov    DWORD PTR [eax+0x24],ecx
  67366b:	c7 40 14 04 00 00 00 	mov    DWORD PTR [eax+0x14],0x4
  673672:	89 48 28             	mov    DWORD PTR [eax+0x28],ecx
  673675:	89 48 2c             	mov    DWORD PTR [eax+0x2c],ecx
  673678:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
