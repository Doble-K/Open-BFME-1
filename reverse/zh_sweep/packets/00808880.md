# Work packet: retail `0x00808880`

- 32 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 87.5% of the bytes outside relocation sites

## Reference source leads

- `??0Energy@@QAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/RTS/Energy.cpp` (87.5% aligned)
- `??0FireSpreadUpdateModuleData@@QAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Update/FireSpreadUpdate.cpp` (87.5% aligned)

Retail folded 2 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  808880:	56                   	push   esi
  808881:	8b f1                	mov    esi,ecx
  808883:	e8 28 fe fd ff       	call   0x7e86b0
  808888:	33 c0                	xor    eax,eax
  80888a:	89 46 08             	mov    DWORD PTR [esi+0x8],eax
  80888d:	89 46 0c             	mov    DWORD PTR [esi+0xc],eax
  808890:	89 46 04             	mov    DWORD PTR [esi+0x4],eax
  808893:	89 46 10             	mov    DWORD PTR [esi+0x10],eax
  808896:	c7 06 b0 96 12 01    	mov    DWORD PTR [esi],0x11296b0
  80889c:	8b c6                	mov    eax,esi
  80889e:	5e                   	pop    esi
  80889f:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??0Snapshot@@QAE@XZ,0x007E86B0 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
