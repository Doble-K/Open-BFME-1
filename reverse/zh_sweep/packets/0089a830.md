# Work packet: retail `0x0089A830`

- 73 bytes, unclaimed by any ledger row, an extent measured from
  reverse/ghidra_functions.csv
- address is a confirmed function start (reverse/ghidra_functions.csv)
- best Zero Hour candidate agrees on 97.8% of the bytes outside relocation sites: 44 of
  45 compared byte(s) match
- the other 28 byte(s) of its 73-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 98.6% counts every blanked byte as agreeing

## Reference source leads

- `??1EvaCheckInfo@@MAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/Eva.cpp` (97.8% of 45 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (73 bytes from this address)

```
  89a830:	6a ff                	push   0xffffffff
  89a832:	68 48 76 05 01       	push   0x1057648
  89a837:	64 a1 00 00 00 00    	mov    eax,fs:0x0
  89a83d:	50                   	push   eax
  89a83e:	64 89 25 00 00 00 00 	mov    DWORD PTR fs:0x0,esp
  89a845:	51                   	push   ecx
  89a846:	56                   	push   esi
  89a847:	8b f1                	mov    esi,ecx
  89a849:	89 74 24 04          	mov    DWORD PTR [esp+0x4],esi
  89a84d:	c7 06 c8 62 13 01    	mov    DWORD PTR [esi],0x11362c8
  89a853:	8d 4e 08             	lea    ecx,[esi+0x8]
  89a856:	c7 44 24 10 00 00 00 	mov    DWORD PTR [esp+0x10],0x0
  89a85d:	00 
  89a85e:	e8 0d 24 00 00       	call   0x89cc70
  89a863:	8b 4c 24 08          	mov    ecx,DWORD PTR [esp+0x8]
  89a867:	c7 06 68 5d 13 01    	mov    DWORD PTR [esi],0x1135d68
  89a86d:	5e                   	pop    esi
  89a86e:	64 89 0d 00 00 00 00 	mov    DWORD PTR fs:0x0,ecx
  89a875:	83 c4 10             	add    esp,0x10
  89a878:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??1?$vector@UEvaSideSounds@@V?$allocator@UEvaSideSounds@@@_STL@@@_STL@@QAE@XZ,0x0089CC70
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
