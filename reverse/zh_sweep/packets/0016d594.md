# Work packet: retail `0x0016D594`

- 28 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 85.7% of the bytes outside relocation sites

## Reference source leads

- `??0PrimitivePrototypeClass@@QAE@PAVRenderObjClass@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/proto.cpp` (85.7% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  16d594:	8b c1                	mov    eax,ecx
  16d596:	8b 4c 24 04          	mov    ecx,DWORD PTR [esp+0x4]
  16d59a:	c7 40 04 00 00 00 00 	mov    DWORD PTR [eax+0x4],0x0
  16d5a1:	c7 00 44 77 09 01    	mov    DWORD PTR [eax],0x1097744
  16d5a7:	89 48 08             	mov    DWORD PTR [eax+0x8],ecx
  16d5aa:	89 50 0c             	mov    DWORD PTR [eax+0xc],edx
  16d5ad:	c2 08 00             	ret    0x8
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.


## Investigated 2026-08-13/14 -- reference does not explain the bytes

The reference `PrimitivePrototypeClass(RenderObjClass*)` ctor does not account
for the retail body: retail writes 4 fields (vtable, NextHash=0, and two more
from ecx/edx-derived values) with ZERO calls, whereas the ZH source's ctor calls
`Proto->Add_Ref()` and sets only 3 fields.

An earlier attempt "landed" this by writing a naked byte-dump thunk. That was
REJECTED and reverted centrally: a naked dump reproduces the bytes by
construction and is not a conversion. The gen_asm placeholder row was restored.

Do not re-land it that way. A real conversion needs the extra member (and the
absent Add_Ref) explained -- most likely another BFME-vs-ZH layout divergence in
this class.
