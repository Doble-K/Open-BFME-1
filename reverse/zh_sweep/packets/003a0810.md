# Work packet: retail `0x003A0810`

- 53 bytes, unclaimed by any ledger row, an extent measured from the int3 padding at
  0x003A0845
- no inventory row confirms a function starts at this address; treat the start as
  unverified
- best Zero Hour candidate agrees on 84.0% of the bytes outside relocation sites: 21 of
  25 compared byte(s) match
- the other 4 byte(s) of its 29-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 86.2% counts every blanked byte as agreeing
- the candidate body is 29 bytes long, so it does not have retail's extent here; the
  bytes quoted below are retail's

## Reference source leads

- `??0RankInfo@@QAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/System/RankInfo.cpp` (84.0% of 25 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (53 bytes from this address)

```
  3a0810:	8b c1                	mov    eax,ecx
  3a0812:	33 c9                	xor    ecx,ecx
  3a0814:	89 48 04             	mov    DWORD PTR [eax+0x4],ecx
  3a0817:	88 48 08             	mov    BYTE PTR [eax+0x8],cl
  3a081a:	c7 00 f8 c4 0e 01    	mov    DWORD PTR [eax],0x10ec4f8
  3a0820:	89 48 0c             	mov    DWORD PTR [eax+0xc],ecx
  3a0823:	89 48 2c             	mov    DWORD PTR [eax+0x2c],ecx
  3a0826:	89 48 30             	mov    DWORD PTR [eax+0x30],ecx
  3a0829:	89 48 34             	mov    DWORD PTR [eax+0x34],ecx
  3a082c:	83 ca ff             	or     edx,0xffffffff
  3a082f:	89 48 10             	mov    DWORD PTR [eax+0x10],ecx
  3a0832:	89 50 14             	mov    DWORD PTR [eax+0x14],edx
  3a0835:	89 50 18             	mov    DWORD PTR [eax+0x18],edx
  3a0838:	89 50 1c             	mov    DWORD PTR [eax+0x1c],edx
  3a083b:	89 50 20             	mov    DWORD PTR [eax+0x20],edx
  3a083e:	89 50 24             	mov    DWORD PTR [eax+0x24],edx
  3a0841:	89 48 28             	mov    DWORD PTR [eax+0x28],ecx
  3a0844:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
