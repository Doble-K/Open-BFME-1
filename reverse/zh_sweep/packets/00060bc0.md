# Work packet: retail `0x00060BC0`

- 53 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 98.1% of the bytes outside relocation sites

## Reference source leads

- `?parseXRes@@YAHQAPADH@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/CommandLine.cpp` (98.1% aligned)
- `?parseYRes@@YAHQAPADH@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/CommandLine.cpp` (98.1% aligned)

Retail folded 2 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
   60bc0:	8b 0d c8 d5 2e 01    	mov    ecx,DWORD PTR ds:0x12ed5c8
   60bc6:	85 c9                	test   ecx,ecx
   60bc8:	b8 01 00 00 00       	mov    eax,0x1
   60bcd:	74 25                	je     0x60bf4
   60bcf:	39 44 24 08          	cmp    DWORD PTR [esp+0x8],eax
   60bd3:	7e 1f                	jle    0x60bf4
   60bd5:	8b 44 24 04          	mov    eax,DWORD PTR [esp+0x4]
   60bd9:	8b 48 04             	mov    ecx,DWORD PTR [eax+0x4]
   60bdc:	51                   	push   ecx
   60bdd:	ff 15 84 93 35 01    	call   DWORD PTR ds:0x1359384
   60be3:	8b 15 c8 d5 2e 01    	mov    edx,DWORD PTR ds:0x12ed5c8
   60be9:	89 42 2c             	mov    DWORD PTR [edx+0x2c],eax
   60bec:	83 c4 04             	add    esp,0x4
   60bef:	b8 02 00 00 00       	mov    eax,0x2
   60bf4:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
