# Work packet: retail `0x002BDB00`

- 36 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 97.2% of the bytes outside relocation sites

## Reference source leads

- `??$_Destroy@PAURunwayInfo@FlightDeckBehavior@@@_STL@@YAXPAURunwayInfo@FlightDeckBehavior@@0@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Behavior/FlightDeckBehavior.cpp` (97.2% aligned)
- `??$__destroy@PAURunwayInfo@FlightDeckBehavior@@U12@@_STL@@YAXPAURunwayInfo@FlightDeckBehavior@@00@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Behavior/FlightDeckBehavior.cpp` (97.2% aligned)
- `??$__destroy_aux@PAURunwayInfo@FlightDeckBehavior@@@_STL@@YAXPAURunwayInfo@FlightDeckBehavior@@0ABU__false_type@0@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Behavior/FlightDeckBehavior.cpp` (97.2% aligned)

Retail folded 3 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  2bdb00:	56                   	push   esi
  2bdb01:	8b 74 24 08          	mov    esi,DWORD PTR [esp+0x8]
  2bdb05:	57                   	push   edi
  2bdb06:	8b 7c 24 10          	mov    edi,DWORD PTR [esp+0x10]
  2bdb0a:	3b f7                	cmp    esi,edi
  2bdb0c:	74 13                	je     0x2bdb21
  2bdb0e:	8b ff                	mov    edi,edi
  2bdb10:	8b ce                	mov    ecx,esi
  2bdb12:	e8 2a 87 d7 ff       	call   0x36241
  2bdb17:	81 c6 b8 00 00 00    	add    esi,0xb8
  2bdb1d:	3b f7                	cmp    esi,edi
  2bdb1f:	75 ef                	jne    0x2bdb10
  2bdb21:	5f                   	pop    edi
  2bdb22:	5e                   	pop    esi
  2bdb23:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??1RunwayInfo@FlightDeckBehavior@@QAE@XZ,0x00036241
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
