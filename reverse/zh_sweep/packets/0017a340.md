# Work packet: retail `0x0017A340`

- 38 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 92.1% of the bytes outside relocation sites

## Reference source leads

- `?onExit@AIMoveAndEvacuateState@@UAEXW4StateExitType@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/AI/AIStates.cpp` (92.1% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  17a340:	56                   	push   esi
  17a341:	8b f1                	mov    esi,ecx
  17a343:	8b 46 1c             	mov    eax,DWORD PTR [esi+0x1c]
  17a346:	8d 4e 50             	lea    ecx,[esi+0x50]
  17a349:	c6 40 40 00          	mov    BYTE PTR [eax+0x40],0x0
  17a34d:	51                   	push   ecx
  17a34e:	8b 4e 1c             	mov    ecx,DWORD PTR [esi+0x1c]
  17a351:	e8 f7 8d e8 ff       	call   0x314d
  17a356:	8b 54 24 08          	mov    edx,DWORD PTR [esp+0x8]
  17a35a:	52                   	push   edx
  17a35b:	8b ce                	mov    ecx,esi
  17a35d:	e8 af ef ea ff       	call   0x29311
  17a362:	5e                   	pop    esi
  17a363:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?setGoalPosition@StateMachine@@QAEXPBUCoord3D@@@Z,0x0000314D
?onExit@AIInternalMoveToState@@UAEXW4StateExitType@@@Z,0x00029311 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
