# Work packet: retail `0x00490CC0`

- 36 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 94.4% of the bytes outside relocation sites

## Reference source leads

- `?processProgress@GameSpyLoadScreen@@UAEXHH@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/GUI/LoadScreen.cpp` (94.4% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  490cc0:	8b 44 24 04          	mov    eax,DWORD PTR [esp+0x4]
  490cc4:	8b 94 81 54 01 00 00 	mov    edx,DWORD PTR [ecx+eax*4+0x154]
  490ccb:	8b 4c 91 10          	mov    ecx,DWORD PTR [ecx+edx*4+0x10]
  490ccf:	85 c9                	test   ecx,ecx
  490cd1:	74 0e                	je     0x490ce1
  490cd3:	8b 44 24 08          	mov    eax,DWORD PTR [esp+0x8]
  490cd7:	50                   	push   eax
  490cd8:	51                   	push   ecx
  490cd9:	e8 9c 0e b9 ff       	call   0x21b7a
  490cde:	83 c4 08             	add    esp,0x8
  490ce1:	c2 08 00             	ret    0x8
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?GadgetProgressBarSetProgress@@YAXPAVGameWindow@@H@Z,0x00021B7A (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
