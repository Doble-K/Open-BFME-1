# Work packet: retail `0x0028C4B0`

- 56 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 98.2% of the bytes outside relocation sites

## Reference source leads

- `?calcSleepDelay@DeletionUpdate@@IAEIII@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Update/DeletionUpdate.cpp` (98.2% aligned)
- `?calcSleepDelay@LifetimeUpdate@@AAEIII@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Update/LifetimeUpdate.cpp` (98.2% aligned)

Retail folded 2 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  28c4b0:	8b 44 24 08          	mov    eax,DWORD PTR [esp+0x8]
  28c4b4:	56                   	push   esi
  28c4b5:	6a 37                	push   0x37
  28c4b7:	68 70 d5 0b 01       	push   0x10bd570
  28c4bc:	8b f1                	mov    esi,ecx
  28c4be:	8b 4c 24 10          	mov    ecx,DWORD PTR [esp+0x10]
  28c4c2:	50                   	push   eax
  28c4c3:	51                   	push   ecx
  28c4c4:	e8 e5 56 d7 ff       	call   0x1bae
  28c4c9:	83 c4 10             	add    esp,0x10
  28c4cc:	83 f8 01             	cmp    eax,0x1
  28c4cf:	73 05                	jae    0x28c4d6
  28c4d1:	b8 01 00 00 00       	mov    eax,0x1
  28c4d6:	8b 15 98 08 2f 01    	mov    edx,DWORD PTR ds:0x12f0898
  28c4dc:	8b 4a 3c             	mov    ecx,DWORD PTR [edx+0x3c]
  28c4df:	03 c8                	add    ecx,eax
  28c4e1:	89 4e 20             	mov    DWORD PTR [esi+0x20],ecx
  28c4e4:	5e                   	pop    esi
  28c4e5:	c2 08 00             	ret    0x8
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?GetGameLogicRandomValue@@YAHHHPADH@Z,0x00001BAE (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
