# Work packet: retail `0x007B7620`

- 49 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 87.8% of the bytes outside relocation sites

## Reference source leads

- `?ReAcquireResources@W3DShadowManager@@QAE_NXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/Shadow/W3DShadow.cpp` (87.8% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  7b7620:	8b 0d 18 6f 30 01 85 c9 53 b3 01 74 0b e8 6e cd
  7b7630:	88 ff 84 c0 75 02 32 db 8b 0d 78 71 30 01 85 c9
  7b7640:	74 0b e8 6f 83 85 ff 84 c0 75 02 32 db 8b 0d f0
  7b7650:	6d
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?ReAcquireResources@W3DVolumetricShadowManager@@QAE_NXZ,0x000443A0 (already in the ledger)
?ReAcquireResources@W3DProjectedShadowManager@@QAE_NXZ,0x0000F9B6
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
