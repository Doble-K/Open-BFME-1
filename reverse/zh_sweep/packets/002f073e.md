# Work packet: retail `0x002F073E`

**This address is not a function start.** The sweep placed a Zero Hour body here by byte
alignment alone, and this repo's own evidence refutes it: the retail byte at this
address is int3, which is what MSVC puts *between* function bodies, so the placement
straddles a boundary, and no known function encloses it. Do not add a ledger row at this
address; the bytes below say what is here, but the address in this packet's name is not
the start of it.

- 78 bytes, unclaimed by any ledger row, an UNVERIFIED size — the Zero Hour candidate's
  own length, which nothing here measures against retail
- address is NOT a function start (in-int3-padding) — see above
- best Zero Hour candidate agrees on 79.6% of the bytes outside relocation sites: 43 of
  54 compared byte(s) match
- the other 24 byte(s) of its 78-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 85.9% counts every blanked byte as agreeing
- the quoted range still crosses a boundary (spans-1-function-start(s)), so its end is
  in dispute

## Reference source leads

- `?doAddCommandBarButton@ScriptActions@@IAEXABVAsciiString@@0H@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/ScriptEngine/ScriptActions.cpp` (79.6% of 54 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (78 bytes from this address)

```
  2f073e:	cc cc 8b 44 24 08 8b 0d d8 f1 2e 01 56 50 e8 0f
  2f074e:	7e d3 ff 8b f0 85 f6 74 31 8b 4c 24 08 51 8b 0d
  2f075e:	f8 33 2f 01 e8 36 ae d4 ff 85 c0 74 1d 8b 4c 24
  2f076e:	10 49 78 16 83 f9 14 7d 11 50 51 8b 0d 98 08 2f
  2f077e:	01 83 c6 2c 56 e8 b5 11 d1 ff 5e c2 0c 00
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?findTemplateInternal@ThingFactory@@AAEPAVThingTemplate@@ABVAsciiString@@_N@Z,0x00028560
?findCommandButton@ControlBar@@QAEPBVCommandButton@@ABVAsciiString@@@Z,0x0003B59D (already in the ledger)
?setControlBarOverride@GameLogic@@QAEXABVAsciiString@@HPBVCommandButton@@@Z,0x0000193D
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
