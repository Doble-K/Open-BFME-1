# Work packet: retail `0x00060C60`

- 51 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 96.1% of the bytes outside relocation sites

## Reference source leads

- `?parseNetMinPlayers@@YAHQAPADH@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/CommandLine.cpp` (96.1% aligned)
- `?parsePlayStats@@YAHQAPADH@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/CommandLine.cpp` (96.1% aligned)
- `?parseSeed@@YAHQAPADH@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/CommandLine.cpp` (96.1% aligned)

Retail folded 3 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
   60c60:	a1 c8 d5 2e 01       	mov    eax,ds:0x12ed5c8
   60c65:	85 c0                	test   eax,eax
   60c67:	74 24                	je     0x60c8d
   60c69:	83 7c 24 08 01       	cmp    DWORD PTR [esp+0x8],0x1
   60c6e:	7e 1d                	jle    0x60c8d
   60c70:	8b 44 24 04          	mov    eax,DWORD PTR [esp+0x4]
   60c74:	8b 48 04             	mov    ecx,DWORD PTR [eax+0x4]
   60c77:	51                   	push   ecx
   60c78:	ff 15 84 93 35 01    	call   DWORD PTR ds:0x1359384
   60c7e:	8b 15 c8 d5 2e 01    	mov    edx,DWORD PTR ds:0x12ed5c8
   60c84:	83 c4 04             	add    esp,0x4
   60c87:	89 82 cc 0e 00 00    	mov    DWORD PTR [edx+0xecc],eax
   60c8d:	b8 02 00 00 00       	mov    eax,0x2
   60c92:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
