# Work packet: retail `0x000DF3C0`

- 50 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 90.0% of the bytes outside relocation sites

## Reference source leads

- `?setLocalPlayer@PlayerList@@QAEXPAVPlayer@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/RTS/PlayerList.cpp` (90.0% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
   df3c0:	56                   	push   esi
   df3c1:	8b 74 24 08          	mov    esi,DWORD PTR [esp+0x8]
   df3c5:	85 f6                	test   esi,esi
   df3c7:	57                   	push   edi
   df3c8:	8b f9                	mov    edi,ecx
   df3ca:	75 03                	jne    0xdf3cf
   df3cc:	8b 77 14             	mov    esi,DWORD PTR [edi+0x14]
   df3cf:	8b 4f 0c             	mov    ecx,DWORD PTR [edi+0xc]
   df3d2:	3b f1                	cmp    esi,ecx
   df3d4:	74 17                	je     0xdf3ed
   df3d6:	85 c9                	test   ecx,ecx
   df3d8:	74 07                	je     0xdf3e1
   df3da:	6a 00                	push   0x0
   df3dc:	e8 d3 98 f3 ff       	call   0x18cb4
   df3e1:	6a 01                	push   0x1
   df3e3:	8b ce                	mov    ecx,esi
   df3e5:	89 77 0c             	mov    DWORD PTR [edi+0xc],esi
   df3e8:	e8 c7 98 f3 ff       	call   0x18cb4
   df3ed:	8b                   	.byte 0x8b
   df3ee:	0d                   	.byte 0xd
   df3ef:	bc                   	.byte 0xbc
   df3f0:	d5 2e                	aad    0x2e
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?becomingLocalPlayer@Player@@QAEX_N@Z,0x00018CB4 (already in the ledger)
?becomingLocalPlayer@Player@@QAEX_N@Z,0x00018CB4 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.


## Investigated 2026-08-13 -- ZH body is right but covers only 45 of 97 bytes

The packet's disassembly stops decoding at 0xdf3ed and turns to garbage
(`.byte 0x8b` / `aad 0x2e`); the real body runs the full 97 bytes. The Zero
Hour source is correct as far as it goes and reproduces the **first 45 bytes
exactly**, compiled against the PlayerList spelling in
Code/GameEngine/Source/Common/RTS/PlayerListUpdate.cpp (m_local at +0x0c,
m_players at +0x14, MAX_PLAYER_COUNT 32):

    void PlayerList::setLocalPlayer(Player *player)
    {
        if (player == NULL)
            player = getNeutralPlayer();
        if (player != m_local)
        {
            if (m_local)
                m_local->becomingLocalPlayer(false);
            m_local = player;
            player->becomingLocalPlayer(true);
        }
    }

BFME then appends 52 bytes ZH has nothing for. Decoded:

```
8b 0d bc d5 2e 01   mov  ecx, ds:0x012ED5BC     ; global A
85 c9               test ecx,ecx
74 0e               je   +0x0e                  ; skip if null
8b 46 24            mov  eax,[esi+0x24]         ; player field at +0x24
68 31 f4 43 00      push 0x0043F431             ; -> RVA 0x3F431
50                  push eax
e8 7b 7f 81 00      call ...
8b 0d c0 d5 2e 01   mov  ecx, ds:0x012ED5C0     ; global B, adjacent to A
85 c9               test ecx,ecx
5f 5e               pop  edi / pop esi
74 0d               je   +0x0d                  ; skip if null
c7 44 24 04 8a d9 41 00   mov  DWORD PTR [esp+4],0x0041D98A  ; -> RVA 0x1D98A
e9 f2 19 7a 00      jmp  ...                    ; tail call, arg replaced
c2 04 00            ret  4
```

So: two null-checked singletons are notified, each handed a **function
pointer**, and the second call is a tail jump with the first stack argument
overwritten.

Both pointers resolve, and both land on unclaimed bodies:

    0x0043F431 -> RVA 0x0003F431 -> j_0003f431 -> 0x001072A0 (58 bytes, unclaimed)
    0x0041D98A -> RVA 0x0001D98A -> j_0001d98a -> 0x001072F0 (58 bytes, unclaimed)

Two 58-byte siblings 0x50 apart, in the same gen_asm bucket
(Code/gen_asm/d_000f93a0.asm) -- almost certainly a pair of callbacks of the
same shape.

Blockers for whoever finishes this: neither global is in reverse/symbols.csv
(0x012ED5BC and 0x012ED5C0, adjacent, so probably declared together), and
neither callback is identified. Naming either callback probably names the
subsystem, and vice versa. Nothing was landed -- a 45-of-97 reconstruction is
not a match, so no partial body was left in the tree.
