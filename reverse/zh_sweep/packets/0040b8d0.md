# Work packet: retail `0x0040B8D0`

- 47 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 89.4% of the bytes outside relocation sites

## Reference source leads

- `??0FunctionLexicon@@QAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/System/FunctionLexicon.cpp` (89.4% aligned)
- `??0QueueProductionExitUpdateModuleData@@QAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/Thing/ModuleFactory.cpp` (87.2% aligned)
- `??0DefaultProductionExitUpdateModuleData@@QAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/Thing/ModuleFactory.cpp` (85.4% aligned)

Retail folded 3 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  40b8d0:	56                   	push   esi
  40b8d1:	8b f1                	mov    esi,ecx
  40b8d3:	e8 58 61 59 00       	call   0x9a1a30
  40b8d8:	33 c0                	xor    eax,eax
  40b8da:	c7 06 e0 0a 0f 01    	mov    DWORD PTR [esi],0x10f0ae0
  40b8e0:	89 46 08             	mov    DWORD PTR [esi+0x8],eax
  40b8e3:	89 46 0c             	mov    DWORD PTR [esi+0xc],eax
  40b8e6:	89 46 10             	mov    DWORD PTR [esi+0x10],eax
  40b8e9:	89 46 14             	mov    DWORD PTR [esi+0x14],eax
  40b8ec:	89 46 18             	mov    DWORD PTR [esi+0x18],eax
  40b8ef:	88 46 1c             	mov    BYTE PTR [esi+0x1c],al
  40b8f2:	89 46 20             	mov    DWORD PTR [esi+0x20],eax
  40b8f5:	89 46 24             	mov    DWORD PTR [esi+0x24],eax
  40b8f8:	89 46 28             	mov    DWORD PTR [esi+0x28],eax
  40b8fb:	89 46 38             	mov    DWORD PTR [esi+0x38],eax
  40b8fe:	89                   	.byte 0x89
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??0SubsystemInterface@@QAE@XZ,0x009A1A30 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
