# Work packet: retail `0x00602ED0`

- 39 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 97.4% of the bytes outside relocation sites

## Reference source leads

- `??0ClientUpdateModule@@QAE@PAVThing@@PBVModuleData@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/Drawable/Update/AnimatedParticleSysBoneClientUpdate.cpp` (97.4% aligned)
- `??0DrawModule@@QAE@PAVThing@@PBVModuleData@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/Drawable/Draw/W3DDebrisDraw.cpp` (97.4% aligned)

Retail folded 2 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  602ed0:	8b 44 24 08          	mov    eax,DWORD PTR [esp+0x8]
  602ed4:	56                   	push   esi
  602ed5:	8b f1                	mov    esi,ecx
  602ed7:	8b 4c 24 08          	mov    ecx,DWORD PTR [esp+0x8]
  602edb:	50                   	push   eax
  602edc:	51                   	push   ecx
  602edd:	8b ce                	mov    ecx,esi
  602edf:	e8 90 f9 9f ff       	call   0x2874
  602ee4:	c7 06 10 52 11 01    	mov    DWORD PTR [esi],0x1115210
  602eea:	c7 46 0c 00 00 00 00 	mov    DWORD PTR [esi+0xc],0x0
  602ef1:	8b c6                	mov    eax,esi
  602ef3:	5e                   	pop    esi
  602ef4:	c2 08 00             	ret    0x8
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??0DrawableModule@@QAE@PAVThing@@PBVModuleData@@@Z,0x00002874
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
