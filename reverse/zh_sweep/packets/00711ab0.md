# Work packet: retail `0x00711AB0`

- 62 bytes, unclaimed by any ledger row, an extent measured from the int3 padding at
  0x00711AEE
- no inventory row confirms a function starts at this address; treat the start as
  unverified
- best Zero Hour candidate agrees on 96.8% of the bytes outside relocation sites
- that figure hides 1 blanked relocation slot(s): 2 of 62 compared byte(s) differ, and
  which global and which callee this body uses was never compared at all

## Reference source leads

- `?draw@RTS2DScene@@UAEXXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/W3DScene.cpp` (96.8% aligned)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (62 bytes from this address)

```
  711ab0:	8b 41 0c             	mov    eax,DWORD PTR [ecx+0xc]
  711ab3:	83 ec 0c             	sub    esp,0xc
  711ab6:	85 c0                	test   eax,eax
  711ab8:	74 30                	je     0x711aea
  711aba:	8d 14 24             	lea    edx,[esp]
  711abd:	52                   	push   edx
  711abe:	6a 00                	push   0x0
  711ac0:	6a 00                	push   0x0
  711ac2:	50                   	push   eax
  711ac3:	81 c1 f8 fe ff ff    	add    ecx,0xfffffef8
  711ac9:	51                   	push   ecx
  711aca:	c7 44 24 14 00 00 00 	mov    DWORD PTR [esp+0x14],0x0
  711ad1:	00 
  711ad2:	c7 44 24 18 00 00 00 	mov    DWORD PTR [esp+0x18],0x0
  711ad9:	00 
  711ada:	c7 44 24 1c 00 00 00 	mov    DWORD PTR [esp+0x1c],0x0
  711ae1:	00 
  711ae2:	e8 d9 c8 1e 00       	call   0x8fe3c0
  711ae7:	83 c4 14             	add    esp,0x14
  711aea:	83 c4 0c             	add    esp,0xc
  711aed:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?Render@WW3D@@SA?AW4WW3DErrorType@@PAVSceneClass@@PAVCameraClass@@_N2ABVVector3@@@Z,0x008FE3C0
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
