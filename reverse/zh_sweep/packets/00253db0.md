# Work packet: retail `0x00253DB0`

- 45 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 97.8% of the bytes outside relocation sites

## Reference source leads

- `?testKillerScience@CreateCrateDie@@AAE_NPBVCrateTemplate@@PAVObject@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Die/CreateCrateDie.cpp` (97.8% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  253db0:	8b 4c 24 08          	mov    ecx,DWORD PTR [esp+0x8]
  253db4:	85 c9                	test   ecx,ecx
  253db6:	75 05                	jne    0x253dbd
  253db8:	32 c0                	xor    al,al
  253dba:	c2 08 00             	ret    0x8
  253dbd:	e8 62 ca dc ff       	call   0x20824
  253dc2:	85 c0                	test   eax,eax
  253dc4:	74 f2                	je     0x253db8
  253dc6:	8b 4c 24 04          	mov    ecx,DWORD PTR [esp+0x4]
  253dca:	8b 51 30             	mov    edx,DWORD PTR [ecx+0x30]
  253dcd:	52                   	push   edx
  253dce:	8b c8                	mov    ecx,eax
  253dd0:	e8 6a 56 db ff       	call   0x943f
  253dd5:	84 c0                	test   al,al
  253dd7:	0f 95 c0             	setne  al
  253dda:	c2 08 00             	ret    0x8
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?getControllingPlayer@Object@@QBEPAVPlayer@@XZ,0x00020824 (already in the ledger)
?hasScience@Player@@QBE_NW4ScienceType@@@Z,0x0000943F (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
