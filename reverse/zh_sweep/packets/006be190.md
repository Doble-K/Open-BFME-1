# Work packet: retail `0x006BE190`

- 98 bytes, unclaimed by any ledger row, an extent measured from the int3 padding at
  0x006BE1F2
- no inventory row confirms a function starts at this address; treat the start as
  unverified
- best Zero Hour candidate agrees on 89.1% of the bytes outside relocation sites: 57 of
  64 compared byte(s) match
- the other 16 byte(s) of its 80-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 91.2% counts every blanked byte as agreeing
- the candidate body is 80 bytes long, so it does not have retail's extent here; the
  bytes quoted below are retail's

## Reference source leads

- `?getExtentIncludingBorder@W3DTerrainLogic@@UBEXPAURegion3D@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameLogic/W3DTerrainLogic.cpp` (89.1% of 64 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (98 bytes from this address)

```
  6be190:	8b 44 24 04          	mov    eax,DWORD PTR [esp+0x4]
  6be194:	c7 00 00 00 00 00    	mov    DWORD PTR [eax],0x0
  6be19a:	c7 40 04 00 00 00 00 	mov    DWORD PTR [eax+0x4],0x0
  6be1a1:	8b 15 e0 7f 2f 01    	mov    edx,DWORD PTR ds:0x12f7fe0
  6be1a7:	8b 92 f4 2f 00 00    	mov    edx,DWORD PTR [edx+0x2ff4]
  6be1ad:	db 42 10             	fild   DWORD PTR [edx+0x10]
  6be1b0:	d8 0d 74 5c 07 01    	fmul   DWORD PTR ds:0x1075c74
  6be1b6:	d9 c0                	fld    st(0)
  6be1b8:	d9 e0                	fchs
  6be1ba:	d9 10                	fst    DWORD PTR [eax]
  6be1bc:	d9 58 04             	fstp   DWORD PTR [eax+0x4]
  6be1bf:	db 41 10             	fild   DWORD PTR [ecx+0x10]
  6be1c2:	d8 0d 74 5c 07 01    	fmul   DWORD PTR ds:0x1075c74
  6be1c8:	d8 e1                	fsub   st,st(1)
  6be1ca:	d9 58 0c             	fstp   DWORD PTR [eax+0xc]
  6be1cd:	db 41 14             	fild   DWORD PTR [ecx+0x14]
  6be1d0:	d8 0d 74 5c 07 01    	fmul   DWORD PTR ds:0x1075c74
  6be1d6:	d8 e1                	fsub   st,st(1)
  6be1d8:	d9 58 10             	fstp   DWORD PTR [eax+0x10]
  6be1db:	8b 91 fc 18 00 00    	mov    edx,DWORD PTR [ecx+0x18fc]
  6be1e1:	89 50 08             	mov    DWORD PTR [eax+0x8],edx
  6be1e4:	8b 89 00 19 00 00    	mov    ecx,DWORD PTR [ecx+0x1900]
  6be1ea:	dd d8                	fstp   st(0)
  6be1ec:	89 48 14             	mov    DWORD PTR [eax+0x14],ecx
  6be1ef:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
