# Work packet: retail `0x004C12B0`

- 32 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 90.6% of the bytes outside relocation sites

## Reference source leads

- `??0PartitionFilterPossibleToAttack@@QAE@W4AbleToAttackType@@PBVObject@@W4CommandSourceType@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/PartitionManager.cpp` (90.6% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  4c12b0:	8b 54 24 08          	mov    edx,DWORD PTR [esp+0x8]
  4c12b4:	8b c1                	mov    eax,ecx
  4c12b6:	8b 4c 24 04          	mov    ecx,DWORD PTR [esp+0x4]
  4c12ba:	89 48 04             	mov    DWORD PTR [eax+0x4],ecx
  4c12bd:	8b 4c 24 0c          	mov    ecx,DWORD PTR [esp+0xc]
  4c12c1:	c7 00 64 d9 0f 01    	mov    DWORD PTR [eax],0x10fd964
  4c12c7:	89 50 08             	mov    DWORD PTR [eax+0x8],edx
  4c12ca:	89 48 0c             	mov    DWORD PTR [eax+0xc],ecx
  4c12cd:	c2 0c 00             	ret    0xc
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
