# Work packet: retail `0x0018CAF0`

- 55 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 87.3% of the bytes outside relocation sites

## Reference source leads

- `?resetIdleScan@TurretAIIdleState@@AAEXXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/AI/TurretAI.cpp` (87.3% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  18caf0:	a1 98 08 2f 01       	mov    eax,ds:0x12f0898
  18caf5:	56                   	push   esi
  18caf6:	8b f1                	mov    esi,ecx
  18caf8:	8b 4e 1c             	mov    ecx,DWORD PTR [esi+0x1c]
  18cafb:	8b 51 44             	mov    edx,DWORD PTR [ecx+0x44]
  18cafe:	57                   	push   edi
  18caff:	8b 78 3c             	mov    edi,DWORD PTR [eax+0x3c]
  18cb02:	8b 42 08             	mov    eax,DWORD PTR [edx+0x8]
  18cb05:	8b 48 4c             	mov    ecx,DWORD PTR [eax+0x4c]
  18cb08:	8b 40 48             	mov    eax,DWORD PTR [eax+0x48]
  18cb0b:	68 fe 04 00 00       	push   0x4fe
  18cb10:	68 84 bb 09 01       	push   0x109bb84
  18cb15:	51                   	push   ecx
  18cb16:	50                   	push   eax
  18cb17:	e8 92 50 e7 ff       	call   0x1bae
  18cb1c:	83 c4 10             	add    esp,0x10
  18cb1f:	03 c7                	add    eax,edi
  18cb21:	5f                   	pop    edi
  18cb22:	89 46 24             	mov    DWORD PTR [esi+0x24],eax
  18cb25:	5e                   	pop    esi
  18cb26:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?GetGameLogicRandomValue@@YAHHHPADH@Z,0x00001BAE (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.


## Investigated 2026-08-14 -- 53 of 55 bytes; scheduling residual

Three things this body needs, and all three were established:

- **retail's file and line**, restored with
  `#line 1278 "F:\bfme\Code\gameengine\Source\GameLogic\Ai\TurretAI.cpp"`.
  GameLogicRandomValue bakes both in as immediates -- `push 0x4fe` and the
  literal at 0x0109BB84 -- and with the directive placed so the *call* lands
  on 1278 both come out exact. (The directive sets the following line, so it
  goes one line above the call, not on it.)
- **State::m_machine at +0x1c**, this tree +0x20, as everywhere else.
- **the machine's turret pointer at +0x44**, this tree +0x38. Note that is
  the same +0xc widening AIDockMachine shows for m_approachPosition (packet
  0014e700), so it is the shared StateMachine base that grew, not either
  derived machine.

The module-data chain -- turret+0x08 then +0x48 and +0x4c for the min and max
idle scan intervals -- was already right.

What is left is instruction scheduling. Retail interleaves the getFrame load
into the middle of the turret chain:

```
    a1 98 08 2f 01   mov  eax,ds:0x12F0898     ; TheGameLogic
    56               push esi
    8b f1            mov  esi,ecx
    8b 4e 1c         mov  ecx,[esi+0x1c]       ; machine
    8b 51 44         mov  edx,[ecx+0x44]       ; turret
    57               push edi
    8b 78 3c         mov  edi,[eax+0x3c]       ; getFrame, deferred to here
```

Compiled from the ZH source order (`now` first) MSVC hoists the getFrame
field read to the top instead, which costs the two bytes. Reordering the two
locals was attempted but not completed. Nothing was landed.
