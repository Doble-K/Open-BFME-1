# Work packet: retail `0x00685420`

- 75 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 98.7% of the bytes outside relocation sites

## Reference source leads

- `?setName@User@@QAEXVUnicodeString@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameNetwork/User.cpp` (98.7% aligned)
- `?setCachedLocalPlayerStats@GameSpyInfo@@UAEXVPSPlayerStats@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameNetwork/GameSpy/PeerDefs.cpp` (96.0% aligned)

Retail folded 2 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  685420:	64 a1 00 00 00 00    	mov    eax,fs:0x0
  685426:	6a ff                	push   0xffffffff
  685428:	68 38 62 04 01       	push   0x1046238
  68542d:	50                   	push   eax
  68542e:	64 89 25 00 00 00 00 	mov    DWORD PTR fs:0x0,esp
  685435:	8d 44 24 10          	lea    eax,[esp+0x10]
  685439:	50                   	push   eax
  68543a:	83 c1 4c             	add    ecx,0x4c
  68543d:	c7 44 24 0c 00 00 00 	mov    DWORD PTR [esp+0xc],0x0
  685444:	00 
  685445:	e8 a6 3d 20 00       	call   0x8891f0
  68544a:	8d 4c 24 10          	lea    ecx,[esp+0x10]
  68544e:	c7 44 24 08 ff ff ff 	mov    DWORD PTR [esp+0x8],0xffffffff
  685455:	ff 
  685456:	e8 e5 24 20 00       	call   0x887940
  68545b:	8b 0c 24             	mov    ecx,DWORD PTR [esp]
  68545e:	64 89 0d 00 00 00 00 	mov    DWORD PTR fs:0x0,ecx
  685465:	83 c4 0c             	add    esp,0xc
  685468:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?set@UnicodeString@@QAEXABV1@@Z,0x008891F0 (already in the ledger)
?releaseBuffer@UnicodeString@@IAEXXZ,0x00887940 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
