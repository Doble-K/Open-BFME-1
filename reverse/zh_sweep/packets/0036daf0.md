# Work packet: retail `0x0036DAF0`

- 47 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 91.5% of the bytes outside relocation sites

## Reference source leads

- `??4PendingSurfaceStruct@Render2DSentenceClass@@QAEAAU01@ABU01@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/render2dsentence.cpp` (91.5% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  36daf0:	8b 44 24 04          	mov    eax,DWORD PTR [esp+0x4]
  36daf4:	53                   	push   ebx
  36daf5:	56                   	push   esi
  36daf6:	8b f1                	mov    esi,ecx
  36daf8:	8b 08                	mov    ecx,DWORD PTR [eax]
  36dafa:	57                   	push   edi
  36dafb:	8d 78 04             	lea    edi,[eax+0x4]
  36dafe:	89 0e                	mov    DWORD PTR [esi],ecx
  36db00:	8d 5e 04             	lea    ebx,[esi+0x4]
  36db03:	57                   	push   edi
  36db04:	8b cb                	mov    ecx,ebx
  36db06:	e8 55 a0 51 00       	call   0x887b60
  36db0b:	8b 57 04             	mov    edx,DWORD PTR [edi+0x4]
  36db0e:	89 53 04             	mov    DWORD PTR [ebx+0x4],edx
  36db11:	8b 47 08             	mov    eax,DWORD PTR [edi+0x8]
  36db14:	89 43 08             	mov    DWORD PTR [ebx+0x8],eax
  36db17:	5f                   	pop    edi
  36db18:	8b c6                	mov    eax,esi
  36db1a:	5e                   	pop    esi
  36db1b:	5b                   	pop    ebx
  36db1c:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??4?$VectorClass@PAVRender2DClass@@@@QAEAAV0@ABV0@@Z,0x00887B60 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.


## Investigated 2026-08-13 -- 39 of 47 bytes, and the callee pin is wrong

Compiled straight from render2dsentence.cpp the implicit
`PendingSurfaceStruct::operator=` reproduces 39 of the 47 bytes -- the whole
shape, in order: copy the dword at +0, take &dst.Renderers and &src.Renderers,
call through, then copy two more dwords. Two things do not reproduce.

**1. The callee pin in this packet is wrong.** It claims 0x00887B60 is
`??4?$VectorClass@PAVRender2DClass@@@@QAEAAV0@ABV0@@Z` and "already in the
ledger". It is not. 0x00887B60 is a **121-byte** body already claimed, and
ICF-folded across at least five copy *constructors*:

    ??0?$StringBase@D@@AAE@ABV0@@Z
    ??0?$list@PAVFXNugget@@V?$allocator@PAVFXNugget@@@_STL@@@_STL@@QAE@ABV01@@Z
    ??0BuddyInfo@@QAE@ABV0@@Z
    ??0GameSpyGroupRoom@@QAE@ABV0@@Z
    ??0MultiplayerColorDefinition@@QAE@ABV0@@Z

The three copies of `??4?$VectorClass@PAVRender2DClass@@@@...` that reverse/symbols.csv
does record sit at 0x00905C80, 0x0092F810 and 0x0093CEA0 -- none of them here.

**2. The two trailing dword copies are at +0x04 and +0x08 of the second
member, where this tree puts them at +0x10 and +0x14.** That is the gap
between `DynamicVectorClass`'s own members and its `VectorClass` base, so
retail's base measures 4 bytes here. It cannot: `?Clear@?$VectorClass@UFileInfoStruct@MixFileCreator@@@@UAEXXZ`
at 0x00934780 (converted, byte-exact) reads Vector at +0x04, VectorMax at
+0x08, IsValid at +0x0c and IsAllocated at +0x0d off a VectorClass with a
vtable at +0 -- 0x10 bytes.

Those two facts point the same way: the second member of this struct is
probably **not** a `DynamicVectorClass`, and the body is probably not
`PendingSurfaceStruct::operator=`. What the bytes actually support is a
two-member struct whose first member is a dword and whose second has a
4-byte base with two dwords after it, assigned through the folded body at
0x00887B60. Nothing was landed.
