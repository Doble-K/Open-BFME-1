# Work packet: retail `0x001DC360`

- 88 bytes, unclaimed by any ledger row, an extent measured from the int3 padding at
  0x001DC3B8
- no inventory row confirms a function starts at this address; treat the start as
  unverified
- best Zero Hour candidate agrees on 98.8% of the bytes outside relocation sites: 79 of
  80 compared byte(s) match
- the other 8 byte(s) of its 88-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 98.9% counts every blanked byte as agreeing

## Reference source leads

3 of the candidates below align equally well, so the sweep cannot tell them apart:
masking the relocation sites hides exactly the bytes that differ between them — which
global the body loads, which callee it tail-jumps to. The first is not the answer.
0x002EFAF0 was landed under the wrong one of three such ties, and the call targets below
are what separated them.

- `?resize@?$vector@HV?$allocator@H@_STL@@@_STL@@QAEXIABH@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/ObjectTypes.cpp` (98.8% of 80 compared bytes)
- `?resize@?$vector@PAVDrawable@@V?$allocator@PAVDrawable@@@_STL@@@_STL@@QAEXIABQAVDrawable@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/GameClient.cpp` (98.8% of 80 compared bytes)
- `?resize@?$vector@PAVObject@@V?$allocator@PAVObject@@@_STL@@@_STL@@QAEXIABQAVObject@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/System/GameLogic.cpp` (98.8% of 80 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (88 bytes from this address)

```
  1dc360:	8b 54 24 04 56 57 8b f9 8b 47 04 8b 37 8b c8 2b
  1dc370:	ce c1 f9 02 3b d1 73 2b 3b c0 8d 0c 96 75 0a 8b
  1dc380:	c1 89 47 04 5f 5e c2 08 00 8b f0 2b f0 56 50 51
  1dc390:	ff 15 5c 94 35 01 83 c4 0c 03 c6 89 47 04 5f 5e
  1dc3a0:	c2 08 00 8d 74 24 10 56 2b d1 52 50 8b cf e8 19
  1dc3b0:	d2 e5 ff 5f 5e c2 08 00
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?_M_fill_insert@?$vector@HV?$allocator@H@_STL@@@_STL@@QAEXPAHIABH@Z,0x000395CC (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
