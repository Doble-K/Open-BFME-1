# Work packet: retail `0x001DD460`

- 27 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 92.6% of the bytes outside relocation sites

## Reference source leads

- `?allow@PartitionFilterIrregularArea@@MAE_NPAVObject@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/PartitionManager.cpp` (92.6% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  1dd460:	8b 41 0c             	mov    eax,DWORD PTR [ecx+0xc]
  1dd463:	8b 49 08             	mov    ecx,DWORD PTR [ecx+0x8]
  1dd466:	8b 54 24 04          	mov    edx,DWORD PTR [esp+0x4]
  1dd46a:	50                   	push   eax
  1dd46b:	51                   	push   ecx
  1dd46c:	83 c2 38             	add    edx,0x38
  1dd46f:	52                   	push   edx
  1dd470:	e8 6c 41 e2 ff       	call   0x15e1
  1dd475:	83 c4 0c             	add    esp,0xc
  1dd478:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?PointInsideArea2D@@YA_NPBUCoord3D@@0H@Z,0x000015E1 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
