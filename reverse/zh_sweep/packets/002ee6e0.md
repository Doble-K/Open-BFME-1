# Work packet: retail `0x002EE6E0`

- 41 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 97.6% of the bytes outside relocation sites

## Reference source leads

- `?doInGamePopupMessage@ScriptActions@@IAEXABVAsciiString@@HHH_N@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/ScriptEngine/ScriptActions.cpp` (97.6% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  2ee6e0:	8b 54 24 14          	mov    edx,DWORD PTR [esp+0x14]
  2ee6e4:	8b 0d 8c 14 2f 01    	mov    ecx,DWORD PTR ds:0x12f148c
  2ee6ea:	8b 01                	mov    eax,DWORD PTR [ecx]
  2ee6ec:	6a 00                	push   0x0
  2ee6ee:	52                   	push   edx
  2ee6ef:	8b 54 24 18          	mov    edx,DWORD PTR [esp+0x18]
  2ee6f3:	52                   	push   edx
  2ee6f4:	8b 54 24 18          	mov    edx,DWORD PTR [esp+0x18]
  2ee6f8:	52                   	push   edx
  2ee6f9:	8b 54 24 18          	mov    edx,DWORD PTR [esp+0x18]
  2ee6fd:	52                   	push   edx
  2ee6fe:	8b 54 24 18          	mov    edx,DWORD PTR [esp+0x18]
  2ee702:	52                   	push   edx
  2ee703:	ff 50 28             	call   DWORD PTR [eax+0x28]
  2ee706:	c2 14 00             	ret    0x14
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
