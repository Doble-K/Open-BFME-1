# Work packet: retail `0x00465AB0`

- 95 bytes, unclaimed by any ledger row, an extent measured from
  reverse/ghidra_functions.csv
- address is a confirmed function start (reverse/ghidra_functions.csv)
- best Zero Hour candidate agrees on 98.5% of the bytes outside relocation sites: 66 of
  67 compared byte(s) match
- the other 28 byte(s) of its 95-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 98.9% counts every blanked byte as agreeing

## Reference source leads

- `?newLocomotor@LocomotorStore@@QBEPAVLocomotor@@PBVLocomotorTemplate@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Locomotor.cpp` (98.5% of 67 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (95 bytes from this address)

```
  465ab0:	6a ff                	push   0xffffffff
  465ab2:	68 fb 4e 02 01       	push   0x1024efb
  465ab7:	64 a1 00 00 00 00    	mov    eax,fs:0x0
  465abd:	50                   	push   eax
  465abe:	64 89 25 00 00 00 00 	mov    DWORD PTR fs:0x0,esp
  465ac5:	51                   	push   ecx
  465ac6:	6a 2c                	push   0x2c
  465ac8:	e8 63 c4 41 00       	call   0x881f30
  465acd:	83 c4 04             	add    esp,0x4
  465ad0:	89 04 24             	mov    DWORD PTR [esp],eax
  465ad3:	85 c0                	test   eax,eax
  465ad5:	c7 44 24 0c 00 00 00 	mov    DWORD PTR [esp+0xc],0x0
  465adc:	00 
  465add:	74 1d                	je     0x465afc
  465adf:	8b 4c 24 14          	mov    ecx,DWORD PTR [esp+0x14]
  465ae3:	51                   	push   ecx
  465ae4:	8b c8                	mov    ecx,eax
  465ae6:	e8 0c 93 bc ff       	call   0x2edf7
  465aeb:	8b 4c 24 04          	mov    ecx,DWORD PTR [esp+0x4]
  465aef:	64 89 0d 00 00 00 00 	mov    DWORD PTR fs:0x0,ecx
  465af6:	83 c4 10             	add    esp,0x10
  465af9:	c2 04 00             	ret    0x4
  465afc:	8b 4c 24 04          	mov    ecx,DWORD PTR [esp+0x4]
  465b00:	33 c0                	xor    eax,eax
  465b02:	64 89 0d 00 00 00 00 	mov    DWORD PTR fs:0x0,ecx
  465b09:	83 c4 10             	add    esp,0x10
  465b0c:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??2@YAPAXI@Z,0x00881F30 (already in the ledger)
??0Locomotor@@IAE@PBVLocomotorTemplate@@@Z,0x0002EDF7 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
