# Work packet: retail `0x00711920`

- 65 bytes, unclaimed by any ledger row, an extent measured from the int3 padding at
  0x00711961
- no inventory row confirms a function starts at this address; treat the start as
  unverified
- best Zero Hour candidate agrees on 93.4% of the bytes outside relocation sites: 57 of
  61 compared byte(s) match
- the other 4 byte(s) of its 65-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 93.8% counts every blanked byte as agreeing

## Reference source leads

- `?draw@RTS3DScene@@UAEXXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/W3DScene.cpp` (93.4% of 61 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (65 bytes from this address)

```
  711920:	8b 81 94 07 00 00    	mov    eax,DWORD PTR [ecx+0x794]
  711926:	83 ec 0c             	sub    esp,0xc
  711929:	85 c0                	test   eax,eax
  71192b:	74 30                	je     0x71195d
  71192d:	8d 14 24             	lea    edx,[esp]
  711930:	52                   	push   edx
  711931:	6a 00                	push   0x0
  711933:	6a 00                	push   0x0
  711935:	50                   	push   eax
  711936:	81 c1 f8 fe ff ff    	add    ecx,0xfffffef8
  71193c:	51                   	push   ecx
  71193d:	c7 44 24 14 00 00 00 	mov    DWORD PTR [esp+0x14],0x0
  711944:	00 
  711945:	c7 44 24 18 00 00 00 	mov    DWORD PTR [esp+0x18],0x0
  71194c:	00 
  71194d:	c7 44 24 1c 00 00 00 	mov    DWORD PTR [esp+0x1c],0x0
  711954:	00 
  711955:	e8 66 ca 1e 00       	call   0x8fe3c0
  71195a:	83 c4 14             	add    esp,0x14
  71195d:	83 c4 0c             	add    esp,0xc
  711960:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?Render@WW3D@@SA?AW4WW3DErrorType@@PAVSceneClass@@PAVCameraClass@@_N2ABVVector3@@@Z,0x008FE3C0 (already pinned in reverse/symbols.csv)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
