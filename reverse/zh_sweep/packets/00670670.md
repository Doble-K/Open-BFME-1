# Work packet: retail `0x00670670`

- 34 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 94.1% of the bytes outside relocation sites

## Reference source leads

- `?allCommandsReady@FrameDataManager@@QAE?AW4FrameDataReturnType@@I_N@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameNetwork/FrameDataManager.cpp` (94.1% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  670670:	8b 44 24 04          	mov    eax,DWORD PTR [esp+0x4]
  670674:	33 d2                	xor    edx,edx
  670676:	f7 35 88 a0 2b 01    	div    DWORD PTR ds:0x12ba088
  67067c:	8b 44 24 08          	mov    eax,DWORD PTR [esp+0x8]
  670680:	50                   	push   eax
  670681:	8b 41 04             	mov    eax,DWORD PTR [ecx+0x4]
  670684:	8d 14 92             	lea    edx,[edx+edx*4]
  670687:	8d 0c 90             	lea    ecx,[eax+edx*4]
  67068a:	e8 d4 fd 9b ff       	call   0x30463
  67068f:	c2 08 00             	ret    0x8
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?allCommandsReady@FrameData@@QAE?AW4FrameDataReturnType@@_N@Z,0x00030463
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
