# Work packet: retail `0x0007C440`

- 50 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 94.0% of the bytes outside relocation sites

## Reference source leads

- `?findDynamicLODLevel@GameLODManager@@QAE?AW4DynamicGameLODLevel@@M@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/GameLOD.cpp` (94.0% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
   7c440:	d9 44 24 04          	fld    DWORD PTR [esp+0x4]
   7c444:	56                   	push   esi
   7c445:	8b f1                	mov    esi,ecx
   7c447:	e8 ec a9 97 00       	call   0x9f6e38
   7c44c:	8b d0                	mov    edx,eax
   7c44e:	b8 04 00 00 00       	mov    eax,0x4
   7c453:	8d 8e 60 01 00 00    	lea    ecx,[esi+0x160]
   7c459:	8d a4 24 00 00 00 00 	lea    esp,[esp+0x0]
   7c460:	39 11                	cmp    DWORD PTR [ecx],edx
   7c462:	7c 0a                	jl     0x7c46e
   7c464:	48                   	dec    eax
   7c465:	83 e9 10             	sub    ecx,0x10
   7c468:	85 c0                	test   eax,eax
   7c46a:	7d f4                	jge    0x7c460
   7c46c:	33 c0                	xor    eax,eax
   7c46e:	5e                   	pop    esi
   7c46f:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
__ftol2,0x009F6E38 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
