# Work packet: retail `0x001DD060`

- 39 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 94.9% of the bytes outside relocation sites

## Reference source leads

- `?allow@PartitionFilterPlayer@@MAE_NPAVObject@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/PartitionManager.cpp` (94.9% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  1dd060:	56                   	push   esi
  1dd061:	8b f1                	mov    esi,ecx
  1dd063:	8b 4c 24 08          	mov    ecx,DWORD PTR [esp+0x8]
  1dd067:	e8 b8 37 e4 ff       	call   0x20824
  1dd06c:	8b 56 08             	mov    edx,DWORD PTR [esi+0x8]
  1dd06f:	33 c9                	xor    ecx,ecx
  1dd071:	3b d0                	cmp    edx,eax
  1dd073:	0f b6 56 0c          	movzx  edx,BYTE PTR [esi+0xc]
  1dd077:	0f 94 c1             	sete   cl
  1dd07a:	5e                   	pop    esi
  1dd07b:	2b ca                	sub    ecx,edx
  1dd07d:	f7 d9                	neg    ecx
  1dd07f:	1b c9                	sbb    ecx,ecx
  1dd081:	41                   	inc    ecx
  1dd082:	8a c1                	mov    al,cl
  1dd084:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?getControllingPlayer@Object@@QBEPAVPlayer@@XZ,0x00020824 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
