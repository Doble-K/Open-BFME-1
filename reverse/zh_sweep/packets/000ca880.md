# Work packet: retail `0x000CA880`

- 35 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 97.1% of the bytes outside relocation sites

## Reference source leads

- `?_M_do_count@?$_Base_bitset@$01@_STL@@QBEIXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Object.cpp` (97.1% aligned)
- `?count@?$BitFlags@$0CG@@@QBEHXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/WeaponSet.cpp` (97.1% aligned)
- `?count@?$BitFlags@$0CN@@@QBEHXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Object.cpp` (97.1% aligned)
- `?count@?$bitset@$0CG@@_STL@@QBEIXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/WeaponSet.cpp` (97.1% aligned)
- `?count@?$bitset@$0CN@@_STL@@QBEIXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Object.cpp` (97.1% aligned)

Retail folded 5 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
   ca880:	8d 51 18             	lea    edx,[ecx+0x18]
   ca883:	33 c0                	xor    eax,eax
   ca885:	3b ca                	cmp    ecx,edx
   ca887:	73 19                	jae    0xca8a2
   ca889:	56                   	push   esi
   ca88a:	8d 9b 00 00 00 00    	lea    ebx,[ebx+0x0]
   ca890:	0f b6 31             	movzx  esi,BYTE PTR [ecx]
   ca893:	0f b6 b6 28 71 2c 01 	movzx  esi,BYTE PTR [esi+0x12c7128]
   ca89a:	03 c6                	add    eax,esi
   ca89c:	41                   	inc    ecx
   ca89d:	3b ca                	cmp    ecx,edx
   ca89f:	72 ef                	jb     0xca890
   ca8a1:	5e                   	pop    esi
   ca8a2:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
