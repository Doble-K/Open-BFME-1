# Work packet: retail `0x0059EB90`

- 105 bytes, unclaimed by any ledger row, an extent measured from
  reverse/ghidra_functions.csv
- address is a confirmed function start (reverse/ghidra_functions.csv)
- best Zero Hour candidate agrees on 98.6% of the bytes outside relocation sites: 68 of
  69 compared byte(s) match
- the other 36 byte(s) of its 105-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 99.0% counts every blanked byte as agreeing

## Reference source leads

- `?parse@AttackNugget@@SAXPAVINI@@PAX1PBX@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/ObjectCreationList.cpp` (98.6% of 69 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (105 bytes from this address)

```
  59eb90:	6a ff 68 8b 83 03 01 64 a1 00 00 00 00 50 64 89
  59eba0:	25 00 00 00 00 51 56 6a 40 e8 82 33 2e 00 83 c4
  59ebb0:	04 89 44 24 04 33 f6 3b c6 89 74 24 10 74 09 8b
  59ebc0:	c8 e8 df 5b a6 ff 8b f0 8b 4c 24 18 68 a4 ca 10
  59ebd0:	01 56 c7 44 24 18 ff ff ff ff e8 c1 34 2b 00 8b
  59ebe0:	4c 24 1c 56 e8 ac e3 a6 ff 8b 4c 24 08 5e 64 89
  59ebf0:	0d 00 00 00 00 83 c4 10 c3
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??2@YAPAXI@Z,0x00881F30 (already in the ledger)
??0AttackNugget@@QAE@XZ,0x000047A5 (already in the ledger)
?initFromINI@INI@@QAEXPAXPBUFieldParse@@@Z,0x008520A0 (already in the ledger)
?addObjectCreationNugget@ObjectCreationList@@QAEXPAVObjectCreationNugget@@@Z,0x0000CF95 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
