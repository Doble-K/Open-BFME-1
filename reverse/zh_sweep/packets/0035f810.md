# Work packet: retail `0x0035F810`

- 89 bytes, unclaimed by any ledger row, an extent measured from
  reverse/ghidra_functions.csv
- address is a confirmed function start (reverse/ghidra_functions.csv)
- best Zero Hour candidate agrees on 96.7% of the bytes outside relocation sites: 59 of
  61 compared byte(s) match
- the other 28 byte(s) of its 89-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 97.8% counts every blanked byte as agreeing

## Reference source leads

2 of the candidates below align equally well, so the sweep cannot tell them apart:
masking the relocation sites hides exactly the bytes that differ between them — which
global the body loads, which callee it tail-jumps to. The first is not the answer.
0x002EFAF0 was landed under the wrong one of three such ties, and the call targets below
are what separated them.

- `?createInGameUI@W3DGameClient@@MAEPAVInGameUI@@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/W3DGameClient.cpp` (96.7% of 61 compared bytes)
- `?createKeyboard@W3DGameClient@@MAEPAVKeyboard@@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/W3DGameClient.cpp` (96.7% of 61 compared bytes)
- `?CreateGameTextInterface@@YAPAVGameTextInterface@@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/GameText.cpp` (95.1% of 61 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (89 bytes from this address)

```
  35f810:	6a ff                	push   0xffffffff
  35f812:	68 eb 9a 01 01       	push   0x1019aeb
  35f817:	64 a1 00 00 00 00    	mov    eax,fs:0x0
  35f81d:	50                   	push   eax
  35f81e:	64 89 25 00 00 00 00 	mov    DWORD PTR fs:0x0,esp
  35f825:	51                   	push   ecx
  35f826:	68 c4 00 00 00       	push   0xc4
  35f82b:	e8 00 27 52 00       	call   0x881f30
  35f830:	83 c4 04             	add    esp,0x4
  35f833:	89 04 24             	mov    DWORD PTR [esp],eax
  35f836:	85 c0                	test   eax,eax
  35f838:	c7 44 24 0c 00 00 00 	mov    DWORD PTR [esp+0xc],0x0
  35f83f:	00 
  35f840:	74 16                	je     0x35f858
  35f842:	8b c8                	mov    ecx,eax
  35f844:	e8 79 b5 ce ff       	call   0x4adc2
  35f849:	8b 4c 24 04          	mov    ecx,DWORD PTR [esp+0x4]
  35f84d:	64 89 0d 00 00 00 00 	mov    DWORD PTR fs:0x0,ecx
  35f854:	83 c4 10             	add    esp,0x10
  35f857:	c3                   	ret
  35f858:	8b 4c 24 04          	mov    ecx,DWORD PTR [esp+0x4]
  35f85c:	33 c0                	xor    eax,eax
  35f85e:	64 89 0d 00 00 00 00 	mov    DWORD PTR fs:0x0,ecx
  35f865:	83 c4 10             	add    esp,0x10
  35f868:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??2@YAPAXI@Z,0x00881F30 (already in the ledger)
??0W3DInGameUI@@QAE@XZ,0x0004ADC2 (unpinned: this is the address retail calls; reverse/symbols.csv pins it at 0x0003CDB2)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
