# Work packet: retail `0x000D7600`

- 43 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 93.0% of the bytes outside relocation sites

## Reference source leads

- `?setPlayerRelationship@Player@@QAEXPBV1@W4Relationship@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/RTS/Player.cpp` (93.0% aligned)
- `?setTeamRelationship@Player@@QAEXPBVTeam@@W4Relationship@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/RTS/Player.cpp` (90.7% aligned)

Retail folded 2 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
   d7600:	8b 44 24 04          	mov    eax,DWORD PTR [esp+0x4]
   d7604:	85 c0                	test   eax,eax
   d7606:	74 20                	je     0xd7628
   d7608:	8b 89 8c 02 00 00    	mov    ecx,DWORD PTR [ecx+0x28c]
   d760e:	8b 40 24             	mov    eax,DWORD PTR [eax+0x24]
   d7611:	8d 54 24 04          	lea    edx,[esp+0x4]
   d7615:	52                   	push   edx
   d7616:	83 c1 04             	add    ecx,0x4
   d7619:	89 44 24 08          	mov    DWORD PTR [esp+0x8],eax
   d761d:	e8 7b ab f3 ff       	call   0x1219d
   d7622:	8b 4c 24 08          	mov    ecx,DWORD PTR [esp+0x8]
   d7626:	89 08                	mov    DWORD PTR [eax],ecx
   d7628:	c2 08 00             	ret    0x8
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??A?$hash_map@HW4Relationship@@U?$hash@H@_STL@@U?$equal_to@H@3@V?$allocator@U?$pair@$$CBHW4Relationship@@@_STL@@@3@@_STL@@QAEAAW4Relationship@@ABH@Z,0x0001219D (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
