# Work packet: retail `0x000D4400`

- 37 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 94.6% of the bytes outside relocation sites

## Reference source leads

- `?findNaturalCommandCenter@Player@@QAEPAVObject@@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/RTS/Player.cpp` (94.6% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
   d4400:	83 ec 08             	sub    esp,0x8
   d4403:	8d 04 24             	lea    eax,[esp]
   d4406:	50                   	push   eax
   d4407:	68 60 43 4d 00       	push   0x4d4360
   d440c:	89 4c 24 08          	mov    DWORD PTR [esp+0x8],ecx
   d4410:	c7 44 24 0c 00 00 00 	mov    DWORD PTR [esp+0xc],0x0
   d4417:	00 
   d4418:	e8 ae ad f5 ff       	call   0x2f1cb
   d441d:	8b 44 24 04          	mov    eax,DWORD PTR [esp+0x4]
   d4421:	83 c4 08             	add    esp,0x8
   d4424:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?iterateObjects@Player@@QBEXP6AXPAVObject@@PAX@Z1@Z,0x0002F1CB (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
