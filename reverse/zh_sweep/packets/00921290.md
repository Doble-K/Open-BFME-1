# Work packet: retail `0x00921290`

- 40 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 97.5% of the bytes outside relocation sites

## Reference source leads

- `?Shutdown@VertexMaterialClass@@SAXXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/vertmaterial.cpp` (97.5% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  921290:	56                   	push   esi
  921291:	be 90 6d 34 01       	mov    esi,0x1346d90
  921296:	8b 0e                	mov    ecx,DWORD PTR [esi]
  921298:	85 c9                	test   ecx,ecx
  92129a:	74 0f                	je     0x9212ab
  92129c:	ff 49 04             	dec    DWORD PTR [ecx+0x4]
  92129f:	75 04                	jne    0x9212a5
  9212a1:	8b 01                	mov    eax,DWORD PTR [ecx]
  9212a3:	ff 10                	call   DWORD PTR [eax]
  9212a5:	c7 06 00 00 00 00    	mov    DWORD PTR [esi],0x0
  9212ab:	83 c6 04             	add    esi,0x4
  9212ae:	81 fe 98 6d 34 01    	cmp    esi,0x1346d98
  9212b4:	7c e0                	jl     0x921296
  9212b6:	5e                   	pop    esi
  9212b7:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
