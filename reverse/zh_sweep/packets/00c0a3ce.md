# Work packet: retail `0x00C0A3CE`

**This address is not a function start.** The sweep placed a Zero Hour body here by byte
alignment alone, and this repo's own evidence refutes it: the address is 3 byte(s)
inside the function at 0x00C0A3CB, which is 11 bytes long and is the body worth
converting here. Do not add a ledger row at this address; the bytes below say what is
here, but the address in this packet's name is not the start of it.

- 155 bytes, unclaimed by any ledger row, an UNVERIFIED size — the Zero Hour candidate's
  own length, which nothing here measures against retail
- address is NOT a function start (interior-of-function) — see above
- best Zero Hour candidate agrees on 89.7% of the bytes outside relocation sites
- that figure hides 13 blanked relocation slot(s): 16 of 155 compared byte(s) differ,
  and which global and which callee this body uses was never compared at all
- the quoted range still crosses a boundary (spans-9-function-start(s)), so its end is
  in dispute

## Reference source leads

2 of the candidates below align equally well, so the sweep cannot tell them apart:
masking the relocation sites hides exactly the bytes that differ between them — which
global the body loads, which callee it tail-jumps to. The first is not the answer.
0x002EFAF0 was landed under the wrong one of three such ties, and the call targets below
are what separated them.

- `$L82516`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Update/BattlePlanUpdate.cpp` (89.7% aligned)
- `$L85110`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Update/BattlePlanUpdate.cpp` (89.7% aligned)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (155 bytes from this address)

```
  c0a3ce:	83 c1 34             	add    ecx,0x34
  c0a3d1:	e9 a9 20 43 ff       	jmp    0x3c47f
  c0a3d6:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c0a3d9:	83 c1 40             	add    ecx,0x40
  c0a3dc:	e9 4e f4 41 ff       	jmp    0x2982f
  c0a3e1:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c0a3e4:	81 c1 80 00 00 00    	add    ecx,0x80
  c0a3ea:	e9 39 34 40 ff       	jmp    0xd828
  c0a3ef:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c0a3f2:	81 c1 b4 00 00 00    	add    ecx,0xb4
  c0a3f8:	e9 38 cb 41 ff       	jmp    0x26f35
  c0a3fd:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c0a400:	81 c1 28 01 00 00    	add    ecx,0x128
  c0a406:	e9 2a cb 41 ff       	jmp    0x26f35
  c0a40b:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c0a40e:	81 c1 98 01 00 00    	add    ecx,0x198
  c0a414:	e9 1c cb 41 ff       	jmp    0x26f35
  c0a419:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c0a41c:	81 c1 08 02 00 00    	add    ecx,0x208
  c0a422:	e9 0e cb 41 ff       	jmp    0x26f35
  c0a427:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c0a42a:	81 c1 78 02 00 00    	add    ecx,0x278
  c0a430:	e9 00 cb 41 ff       	jmp    0x26f35
  c0a435:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c0a438:	81 c1 e8 02 00 00    	add    ecx,0x2e8
  c0a43e:	e9 f2 ca 41 ff       	jmp    0x26f35
  c0a443:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c0a446:	81 c1 58 03 00 00    	add    ecx,0x358
  c0a44c:	e9 e4 ca 41 ff       	jmp    0x26f35
  c0a451:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c0a454:	81 c1 c8 03 00 00    	add    ecx,0x3c8
  c0a45a:	e9 d6 ca 41 ff       	jmp    0x26f35
  c0a45f:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c0a462:	81 c1 38 04 00 00    	add    ecx,0x438
  c0a468:	e9                   	.byte 0xe9
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??1SpecialPowerUpdateModule@@MAE@XZ,0x0003C47F
??1AudioEventRTS@@UAE@XZ,0x0002982F (already in the ledger)
??1AudioEventRTS@@UAE@XZ,0x0000D828 (already in the ledger)
??1AudioEventRTS@@UAE@XZ,0x00026F35 (already in the ledger)
??1AudioEventRTS@@UAE@XZ,0x00026F35 (already in the ledger)
??1AudioEventRTS@@UAE@XZ,0x00026F35 (already in the ledger)
??1AudioEventRTS@@UAE@XZ,0x00026F35 (already in the ledger)
??1AudioEventRTS@@UAE@XZ,0x00026F35 (already in the ledger)
??1AudioEventRTS@@UAE@XZ,0x00026F35 (already in the ledger)
??1AudioEventRTS@@UAE@XZ,0x00026F35 (already in the ledger)
??1AudioEventRTS@@UAE@XZ,0x00026F35 (already in the ledger)
___CxxFrameHandler,0x-163F5B93
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
