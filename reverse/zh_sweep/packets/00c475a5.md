# Work packet: retail `0x00C475A5`

**This address is not a function start.** The sweep placed a Zero Hour body here by byte
alignment alone, and this repo's own evidence refutes it: the address is 6 byte(s)
inside the function at 0x00C4759F, which is 14 bytes long and is the body worth
converting here. Do not add a ledger row at this address; the bytes below say what is
here, but the address in this packet's name is not the start of it.

- 68 bytes, unclaimed by any ledger row, an UNVERIFIED size — the Zero Hour candidate's
  own length, which nothing here measures against retail
- address is NOT a function start (interior-of-function) — see above
- best Zero Hour candidate agrees on 83.3% of the bytes outside relocation sites: 30 of
  36 compared byte(s) match
- the other 24 byte(s) of its 68-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 90.0% counts every blanked byte as agreeing
- the quoted range still crosses a boundary (spans-5-function-start(s)), so its end is
  in dispute

## Reference source leads

- `$L88502`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Update/AIUpdate/JetAIUpdate.cpp` (83.3% of 36 compared bytes)
- `$L90243`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Update/AIUpdate/JetAIUpdate.cpp` (77.5% of 40 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (68 bytes from this address)

```
  c475a5:	01 00                	add    DWORD PTR [eax],eax
  c475a7:	00 e9                	add    cl,ch
  c475a9:	88 f9                	mov    cl,bh
  c475ab:	3d ff 8b 4d f0       	cmp    eax,0xf04d8bff
  c475b0:	81 c1 30 02 00 00    	add    ecx,0x230
  c475b6:	e9 7a f9 3d ff       	jmp    0x26f35
  c475bb:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c475be:	81 c1 a0 02 00 00    	add    ecx,0x2a0
  c475c4:	e9 6c f9 3d ff       	jmp    0x26f35
  c475c9:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c475cc:	81 c1 10 03 00 00    	add    ecx,0x310
  c475d2:	e9 5e f9 3d ff       	jmp    0x26f35
  c475d7:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c475da:	81 c1 80 03 00 00    	add    ecx,0x380
  c475e0:	e9 50 f9 3d ff       	jmp    0x26f35
  c475e5:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c475e8:	81                   	.byte 0x81
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??1AIUpdateInterface@@MAE@XZ,0x00026F35
??1AICommandParmsStorage@@QAE@XZ,0x00026F35
??1AudioEventRTS@@UAE@XZ,0x00026F35 (already in the ledger)
??1?$list@W4ObjectID@@V?$allocator@W4ObjectID@@@_STL@@@_STL@@QAE@XZ,0x00026F35
___CxxFrameHandler,0x-163B8A1C
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
