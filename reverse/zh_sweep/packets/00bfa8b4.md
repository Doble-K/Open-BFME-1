# Work packet: retail `0x00BFA8B4`

**This address is not a function start.** The sweep placed a Zero Hour body here by byte
alignment alone, and this repo's own evidence refutes it: the address is 3 byte(s)
inside the function at 0x00BFA8B1, which is 11 bytes long and is the body worth
converting here. Do not add a ledger row at this address; the bytes below say what is
here, but the address in this packet's name is not the start of it.

- 62 bytes, unclaimed by any ledger row, an UNVERIFIED size — the Zero Hour candidate's
  own length, which nothing here measures against retail
- address is NOT a function start (interior-of-function) — see above
- best Zero Hour candidate agrees on 76.5% of the bytes outside relocation sites: 26 of
  34 compared byte(s) match
- the other 28 byte(s) of its 62-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 87.1% counts every blanked byte as agreeing
- the quoted range still crosses a boundary (spans-4-function-start(s)), so its end is
  in dispute

## Reference source leads

- `$L117198`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/Drawable/Draw/W3DModelDraw.cpp` (76.5% of 34 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (62 bytes from this address)

```
  bfa8b4:	83 c1 34             	add    ecx,0x34
  bfa8b7:	e9 6c 2f 41 ff       	jmp    0xd828
  bfa8bc:	68 28 d8 40 00       	push   0x40d828
  bfa8c1:	6a 0a                	push   0xa
  bfa8c3:	6a 04                	push   0x4
  bfa8c5:	8b 45 f0             	mov    eax,DWORD PTR [ebp-0x10]
  bfa8c8:	83 c0 38             	add    eax,0x38
  bfa8cb:	50                   	push   eax
  bfa8cc:	e8 a5 c4 df ff       	call   0x9f6d76
  bfa8d1:	c3                   	ret
  bfa8d2:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  bfa8d5:	83 c1 60             	add    ecx,0x60
  bfa8d8:	e9 fb cd 42 ff       	jmp    0x276d8
  bfa8dd:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  bfa8e0:	83 c1 6c             	add    ecx,0x6c
  bfa8e3:	e9 50 5e 43 ff       	jmp    0x30738
  bfa8e8:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  bfa8eb:	81 c1 80 00 00 00    	add    ecx,0x80
  bfa8f1:	e9                   	.byte 0xe9
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??1DrawModule@@MAE@XZ,0x0000D828
??_M@YGXPAXIHP6EX0@Z@Z,0x009F6D76 (already in the ledger)
??1?$vector@UParticleSysTrackerType@W3DModelDraw@@V?$allocator@UParticleSysTrackerType@W3DModelDraw@@@_STL@@@_STL@@QAE@XZ,0x000276D8
??1?$vector@UHideShowSubObjInfo@ModelConditionInfo@@V?$allocator@UHideShowSubObjInfo@ModelConditionInfo@@@_STL@@@_STL@@QAE@XZ,0x00030738
___CxxFrameHandler,0x-1640570E
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
