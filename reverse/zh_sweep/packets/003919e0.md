# Work packet: retail `0x003919E0`

- 29 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 96.6% of the bytes outside relocation sites

## Reference source leads

- `?setBuildableStatusOverride@GameLogic@@QAEXPBVThingTemplate@@W4BuildableStatus@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/System/GameLogic.cpp` (96.6% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  3919e0:	8b 44 24 04          	mov    eax,DWORD PTR [esp+0x4]
  3919e4:	85 c0                	test   eax,eax
  3919e6:	74 12                	je     0x3919fa
  3919e8:	83 c0 20             	add    eax,0x20
  3919eb:	50                   	push   eax
  3919ec:	83 c1 0c             	add    ecx,0xc
  3919ef:	e8 a2 20 ca ff       	call   0x33a96
  3919f4:	8b 4c 24 08          	mov    ecx,DWORD PTR [esp+0x8]
  3919f8:	89 08                	mov    DWORD PTR [eax],ecx
  3919fa:	c2 08 00             	ret    0x8
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??A?$hash_map@VAsciiString@@W4BuildableStatus@@U?$hash@VAsciiString@@@rts@@U?$equal_to@VAsciiString@@@4@V?$allocator@U?$pair@$$CBVAsciiString@@W4BuildableStatus@@@_STL@@@_STL@@@_STL@@QAEAAW4BuildableStatus@@ABVAsciiString@@@Z,0x00033A96
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
