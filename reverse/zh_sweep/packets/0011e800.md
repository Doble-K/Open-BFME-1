# Work packet: retail `0x0011E800`

- 96 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 99.0% of the bytes outside relocation sites

## Reference source leads

- `?friend_newModuleInstance@DumbProjectileBehavior@@SAPAVModule@@PAVThing@@PBVModuleData@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/Thing/ModuleFactory.cpp` (99.0% aligned)
- `?friend_newModuleInstance@MinefieldBehavior@@SAPAVModule@@PAVThing@@PBVModuleData@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/Thing/ModuleFactory.cpp` (99.0% aligned)

Retail folded 2 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  11e800:	6a ff 68 ab 05 00 01 64 a1 00 00 00 00 50 64 89
  11e810:	25 00 00 00 00 51 6a 14 e8 13 37 76 00 83 c4 04
  11e820:	89 04 24 85 c0 c7 44 24 0c 00 00 00 00 74 20 8b
  11e830:	4c 24 18 8b 54 24 14 51 52 8b c8 e8 d9 72 ee ff
  11e840:	8b 4c 24 04 64 89 0d 00 00 00 00 83 c4 10 c3 8b
  11e850:	4c 24 04 33 c0 64 89 0d 00 00 00 00 83 c4 10 c3
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??2@YAPAXI@Z,0x00881F30 (already in the ledger)
??0DumbProjectileBehavior@@QAE@PAVThing@@PBVModuleData@@@Z,0x00005B19
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
