# Work packet: retail `0x004C8570`

- 44 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 97.7% of the bytes outside relocation sites

## Reference source leads

- `?EstablishConnectionsControlSystem@@YA?AW4WindowMsgHandledType@@PAVGameWindow@@III@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/GUI/GUICallbacks/Menus/EstablishConnectionsWindow.cpp` (97.7% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  4c8570:	81 7c 24 08 08 40 00 	cmp    DWORD PTR [esp+0x8],0x4008
  4c8577:	00 
  4c8578:	75 1c                	jne    0x4c8596
  4c857a:	8b 4c 24 0c          	mov    ecx,DWORD PTR [esp+0xc]
  4c857e:	e8 21 03 b5 ff       	call   0x188a4
  4c8583:	3b 05 c0 39 2f 01    	cmp    eax,DWORD PTR ds:0x12f39c0
  4c8589:	75 0b                	jne    0x4c8596
  4c858b:	8b 0d 3c 36 2f 01    	mov    ecx,DWORD PTR ds:0x12f363c
  4c8591:	8b 01                	mov    eax,DWORD PTR [ecx]
  4c8593:	ff 50 0c             	call   DWORD PTR [eax+0xc]
  4c8596:	b8 01 00 00 00       	mov    eax,0x1
  4c859b:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?winGetWindowId@GameWindow@@QAEHXZ,0x000188A4 (already in the ledger)
?abortGame@EstablishConnectionsMenu@@QAEXXZ,0x0C9D8497
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
