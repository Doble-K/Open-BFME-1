# Work packet: retail `0x00C4D6A0`

- 74 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 93.2% of the bytes outside relocation sites

## Reference source leads

- `$L26394`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/streak.cpp` (93.2% aligned)
- `$L26632`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/streak.cpp` (93.2% aligned)
- `$L26646`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/streak.cpp` (93.2% aligned)

Retail folded 3 identical bodies onto this address, so the candidates
below are the same code under different names. Any of them reproduces the
bytes; which name belongs here needs separate evidence.

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  c4d6a0:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c4d6a3:	e9 d8 45 3b ff       	jmp    0x1c80
  c4d6a8:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c4d6ab:	81 c1 b8 00 00 00    	add    ecx,0xb8
  c4d6b1:	e9 9c 2f 3e ff       	jmp    0x30652
  c4d6b6:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c4d6b9:	81 c1 bc 00 00 00    	add    ecx,0xbc
  c4d6bf:	e9 8e 2f 3e ff       	jmp    0x30652
  c4d6c4:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c4d6c7:	81 c1 c0 00 00 00    	add    ecx,0xc0
  c4d6cd:	e9 1e f7 cf ff       	jmp    0x94cdf0
  c4d6d2:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c4d6d5:	81 c1 e8 00 00 00    	add    ecx,0xe8
  c4d6db:	e9 10 f7 cf ff       	jmp    0x94cdf0
  c4d6e0:	68 95 a4 43 00       	push   0x43a495
  c4d6e5:	68 e0 2e 00 00       	push   0x2ee0
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??1RenderObjClass@@UAE@XZ,0x00001C80 (already in the ledger)
??1?$SimpleDynVecClass@VVector3@@@@UAE@XZ,0x00030652 (already in the ledger)
??1?$SimpleDynVecClass@VVector4@@@@UAE@XZ,0x00030652
??1?$SimpleDynVecClass@M@@UAE@XZ,0x0094CDF0
??1SegLineRendererClass@@QAE@XZ,0x0094CDF0
___CxxFrameHandler,0x00C505CA
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.


## Investigated 2026-08-14 -- not a function: EH unwind funclet chain

This 74-byte span is compiler-generated EH cleanup glue, not a portable C++
body. It is four `mov ecx,[ebp-0x10]` (some with `add ecx,N`) / `jmp` blocks
dispatching to destructors that are ALREADY matched at their real addresses:
RenderObjClass, three SimpleDynVecClass instantiations (Vector3, Vector4,
float), and SegLineRendererClass.

Note the trailing `push 0x43a495` / `push 0x2ee0` is the NEXT unrelated
function's frame-handler setup, not part of this span -- it would otherwise
look like body.

The streak.cpp `$L26394` / `$L26632` / `$L26646` labels this packet cites are
the destructor JUMP TARGETS (already claimed elsewhere), not source for this
thunk. No legitimate non-__asm C++ reproduces a bare mov/jmp dispatch table.

Reclassify: this packet should not be offered as a portable candidate.
