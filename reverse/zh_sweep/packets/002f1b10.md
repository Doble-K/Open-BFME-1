# Work packet: retail `0x002F1B10`

- 87 bytes, unclaimed by any ledger row, an extent measured from
  reverse/ghidra_functions.csv
- address is a confirmed function start (reverse/ghidra_functions.csv)
- best Zero Hour candidate agrees on 94.5% of the bytes outside relocation sites: 52 of
  55 compared byte(s) match
- the other 32 byte(s) of its 87-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 96.6% counts every blanked byte as agreeing

## Reference source leads

- `??0UnitCrateCollideModuleData@@QAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/Thing/ModuleFactory.cpp` (94.5% of 55 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (87 bytes from this address)

```
  2f1b10:	6a ff                	push   0xffffffff
  2f1b12:	68 83 5e 01 01       	push   0x1015e83
  2f1b17:	64 a1 00 00 00 00    	mov    eax,fs:0x0
  2f1b1d:	50                   	push   eax
  2f1b1e:	64 89 25 00 00 00 00 	mov    DWORD PTR fs:0x0,esp
  2f1b25:	51                   	push   ecx
  2f1b26:	56                   	push   esi
  2f1b27:	8b f1                	mov    esi,ecx
  2f1b29:	89 74 24 04          	mov    DWORD PTR [esp+0x4],esi
  2f1b2d:	e8 fe fe 6a 00       	call   0x9a1a30
  2f1b32:	33 c0                	xor    eax,eax
  2f1b34:	8d 4e 0c             	lea    ecx,[esi+0xc]
  2f1b37:	c7 06 14 00 0d 01    	mov    DWORD PTR [esi],0x10d0014
  2f1b3d:	89 44 24 10          	mov    DWORD PTR [esp+0x10],eax
  2f1b41:	89 01                	mov    DWORD PTR [ecx],eax
  2f1b43:	68 50 6e 33 01       	push   0x1336e50
  2f1b48:	c6 44 24 14 01       	mov    BYTE PTR [esp+0x14],0x1
  2f1b4d:	88 46 08             	mov    BYTE PTR [esi+0x8],al
  2f1b50:	e8 3b 61 59 00       	call   0x887c90
  2f1b55:	8b 4c 24 08          	mov    ecx,DWORD PTR [esp+0x8]
  2f1b59:	8b c6                	mov    eax,esi
  2f1b5b:	5e                   	pop    esi
  2f1b5c:	64 89 0d 00 00 00 00 	mov    DWORD PTR fs:0x0,ecx
  2f1b63:	83 c4 10             	add    esp,0x10
  2f1b66:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??0CrateCollideModuleData@@QAE@XZ,0x009A1A30
?set@AsciiString@@QAEXPBD@Z,0x00887C90
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
