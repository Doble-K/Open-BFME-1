# Work packet: retail `0x00C5F7D0`

- 43 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 93.0% of the bytes outside relocation sites

## Reference source leads

- `$L25764`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/hlod.cpp` (93.0% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  c5f7d0:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c5f7d3:	e9 08 31 d2 ff       	jmp    0x9828e0
  c5f7d8:	8b 4d f0             	mov    ecx,DWORD PTR [ebp-0x10]
  c5f7db:	81 c1 3c 01 00 00    	add    ecx,0x13c
  c5f7e1:	e9 6a bf d1 ff       	jmp    0x97b750
  c5f7e6:	8b 45 04             	mov    eax,DWORD PTR [ebp+0x4]
  c5f7e9:	50                   	push   eax
  c5f7ea:	e8 01 27 c2 ff       	call   0x881ef0
  c5f7ef:	59                   	pop    ecx
  c5f7f0:	c3                   	ret
  c5f7f1:	8b 45 04             	mov    eax,DWORD PTR [ebp+0x4]
  c5f7f4:	50                   	push   eax
  c5f7f5:	e8 b6 26 c2 ff       	call   0x881eb0
  c5f7fa:	59                   	pop    ecx
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??1Animatable3DObjClass@@UAE@XZ,0x009828E0 (already in the ledger)
??1ModelArrayClass@HLodClass@@UAE@XZ,0x0097B750 (already in the ledger)
??_V@YAXPAX@Z,0x00881EF0 (already in the ledger)
___CxxFrameHandler,0x5AC5BA21
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
