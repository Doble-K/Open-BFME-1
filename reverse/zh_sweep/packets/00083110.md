# Work packet: retail `0x00083110`

- 46 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 95.7% of the bytes outside relocation sites

## Reference source leads

- `?reset@GlobalData@@UAEXXZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/GlobalData.cpp` (95.7% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
   83110:	8b 0d c8 d5 2e 01    	mov    ecx,DWORD PTR ds:0x12ed5c8
   83116:	3b 0d cc d5 2e 01    	cmp    ecx,DWORD PTR ds:0x12ed5cc
   8311c:	74 1f                	je     0x8313d
   8311e:	56                   	push   esi
   8311f:	90                   	nop
   83120:	8b 01                	mov    eax,DWORD PTR [ecx]
   83122:	8b b1 8c 12 00 00    	mov    esi,DWORD PTR [ecx+0x128c]
   83128:	6a 01                	push   0x1
   8312a:	ff 10                	call   DWORD PTR [eax]
   8312c:	3b 35 cc d5 2e 01    	cmp    esi,DWORD PTR ds:0x12ed5cc
   83132:	8b ce                	mov    ecx,esi
   83134:	89 0d c8 d5 2e 01    	mov    DWORD PTR ds:0x12ed5c8,ecx
   8313a:	75 e4                	jne    0x83120
   8313c:	5e                   	pop    esi
   8313d:	c3                   	ret
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
(no relative calls in this body)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
