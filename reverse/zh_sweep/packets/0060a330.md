# Work packet: retail `0x0060A330`

- 86 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 97.7% of the bytes outside relocation sites

## Reference source leads

- `??1MetaMapRec@@MAE@XZ`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/MessageStream/MetaEvent.cpp` (97.7% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
  60a330:	6a ff 68 03 db 03 01 64 a1 00 00 00 00 50 64 89
  60a340:	25 00 00 00 00 51 56 8b f1 89 74 24 04 c7 06 30
  60a350:	5c 11 01 8d 4e 18 c7 44 24 10 01 00 00 00 e8 dd
  60a360:	d5 27 00 8d 4e 0c c6 44 24 10 00 e8 d0 d5 27 00
  60a370:	8b 4c 24 08 c7 06 44 37 07 01 5e 64 89 0d 00 00
  60a380:	00 00 83 c4 10 c3
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?releaseBuffer@UnicodeString@@IAEXXZ,0x00887940 (already in the ledger)
?releaseBuffer@UnicodeString@@IAEXXZ,0x00887940 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
