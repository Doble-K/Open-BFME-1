# Work packet: retail `0x006824C0`

- 93 bytes, unclaimed by any ledger row, an extent measured from
  reverse/ghidra_functions.csv
- address is a confirmed function start (reverse/ghidra_functions.csv)
- best Zero Hour candidate agrees on 90.8% of the bytes outside relocation sites: 59 of
  65 compared byte(s) match
- the other 28 byte(s) of its 93-byte body are relocation slots, blanked before
  comparing, so which global and which callee this body uses never entered that figure —
  the sweep's own 93.5% counts every blanked byte as agreeing

## Reference source leads

- `?sendDisconnectChat@Network@@UAEXVUnicodeString@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameNetwork/Network.cpp` (90.8% of 65 compared bytes)

## What this is

The vendored Zero Hour tree contains a body that nearly reproduces these retail
bytes. Near is not identical, so the names above are leads and not identity: the
two versions differ by something real — a changed constant, an extra member, a
different inlining decision — or they are different functions that share a shape.
Port the reference body into a `Code/` source, then close the remaining gap
against the disassembly below.

## Retail disassembly (93 bytes from this address)

```
  6824c0:	6a ff 68 58 60 04 01 64 a1 00 00 00 00 50 64 89
  6824d0:	25 00 00 00 00 51 56 8b f1 51 8d 44 24 1c 89 64
  6824e0:	24 08 8b cc 50 c7 44 24 18 00 00 00 00 e8 0e 5f
  6824f0:	20 00 8b 4e 08 e8 5a f6 97 ff 8d 4c 24 18 c7 44
  682500:	24 10 ff ff ff ff e8 c5 5c 20 00 8b 4c 24 08 64
  682510:	89 0d 00 00 00 00 5e 83 c4 10 c2 04 00
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
??0UnicodeString@@QAE@ABV0@@Z,0x00888400 (already in the ledger)
?sendDisconnectChat@ConnectionManager@@QAEXVUnicodeString@@@Z,0x00001B54
?releaseBuffer@UnicodeString@@IAEXXZ,0x008881D0 (already in the ledger)
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above — which is exactly why the
name and the extent above still need checking by hand. The gate copies
relocation bytes from retail rather than proving them, so a row at the wrong
address, the wrong size, or under a tied candidate's name goes green anyway.
