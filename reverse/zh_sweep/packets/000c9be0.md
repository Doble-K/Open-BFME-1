# Work packet: retail `0x000C9BE0`

- 42 bytes, unclaimed by any ledger row
- best Zero Hour candidate agrees on 95.2% of the bytes outside relocation sites

## Reference source leads

- `?removeObjectFromHotkeySquad@Player@@QAEXPAVObject@@@Z`
  in `reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/RTS/Player.cpp` (95.2% aligned)

## What this is

The vendored Zero Hour tree contains this function. Compiled with this repo's
toolchain it lands *near* these retail bytes but not on them, so the two
versions differ by something real: a changed constant, an extra member, a
different inlining decision. Port the reference body into a `Code/` source,
then close the remaining gap against the disassembly below.

## Retail disassembly (the exact target)

```
   c9be0:	53                   	push   ebx
   c9be1:	8b 5c 24 08          	mov    ebx,DWORD PTR [esp+0x8]
   c9be5:	56                   	push   esi
   c9be6:	57                   	push   edi
   c9be7:	8d b1 54 06 00 00    	lea    esi,[ecx+0x654]
   c9bed:	bf 0a 00 00 00       	mov    edi,0xa
   c9bf2:	8b 0e                	mov    ecx,DWORD PTR [esi]
   c9bf4:	85 c9                	test   ecx,ecx
   c9bf6:	74 06                	je     0xc9bfe
   c9bf8:	53                   	push   ebx
   c9bf9:	e8 f0 f6 f5 ff       	call   0x292ee
   c9bfe:	83 c6 04             	add    esi,0x4
   c9c01:	4f                   	dec    edi
   c9c02:	75 ee                	jne    0xc9bf2
   c9c04:	5f                   	pop    edi
   c9c05:	5e                   	pop    esi
   c9c06:	5b                   	pop    ebx
   c9c07:	c2 04 00             	ret    0x4
```

## Callee pins (paste unresolved ones into reverse/symbols.csv)

```
?removeObject@Squad@@QAEXPAVObject@@@Z,0x000292EE
```

## Landing it

Add a row to `reverse/functions.csv` naming your source and this address, then
run `./build.sh '<your symbol>'`. It passes only when every byte outside a
relocation site is identical to the address above.
