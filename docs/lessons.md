# Lessons

Operational facts agents paid for once already. Append ONE line per lesson
(what + the working alternative). Keep entries that stay true; delete ones a
tool fix has made obsolete.

* `land_zh.py` takes reference BASENAMES (`Dict`), not paths; 1-2 per call — batching many files blows the subprocess timeout and hides which succeeded.
* `sweep_generalsmd.py --files` also wants basenames without `.cpp`.
* The sweep report's `located` counts are an upper bound from sweep time: sub-`--min-size` functions and rows other agents landed since make `land_zh` return "nothing landed" — that's stale data, not a bug.
* `functions.csv` is CRLF; python `csv.writer` defaults reformat the whole file. Use `tools/dedup_csv.py` / binary-safe edits, never hand-rolled rewrites.
* Mangled names contain commas — `awk -F,`/`cut -d,` mis-parse both ledgers; use python `csv` or the tools.
* `harvest.py` recompiles the whole file per run (3-20s); never loop it over all of `src/` — 40 min of that produced 0 matches overnight.
* Piping a gate command through `tail`/`grep` masks its exit code; agents have shipped broken rows because "the output looked green".
* Compile errors from `land_zh` were once swallowed; run `tools/sweep_generalsmd.py --files <name>` to see a file's real blocker.
* A near-match (register choice, branch layout, x87 order) is a FAILURE — revert it; do not commit "close".
* Drift-report rows lack resolved callees; hand-appending them to functions.csv fails the gate later. Only `locate.py --emit` / `land_zh.py` / `land_ambiguous.py` write safe rows.
* String literals must be byte-exact incl. encoding: a CP1252 `µ` re-encoded to UTF-8 U+FFFD passed compile and failed the string-ref gate. Use `\xNN` escapes for non-ASCII bytes.
* After compaction/summarization, trust `git status` + `tools/progress.py`, not your summary's claims about what was reverted or committed.
* Inline `static`/tiny helpers (e.g. `flipCoord3D`) often don't exist standalone in the binary — a "unique" locate hit inside another function's body is a misplacement; check `ghidra_functions.csv` boundaries before claiming.
* This host runs a second fleet (`~/Projects/fleet/wt/*`) sharing wine and /tmp (tmpfs with quota): never kill cl.exe/build.py by name — scope by path; if /tmp hits quota, set TMPDIR to a repo-local dir.
* `locate.py --emit` historically appended LF-tail rows into the CRLF ledger; `tools/add_match.py` / `tools/dedup_csv.py` write it correctly — prefer them over hand-appends.
* landed ZH cpps (now under `Code/`) compile against the REFERENCE ZH headers (the `// cl: /Ireference/...` preamble), not `src/w3d`. MSVC's `#include "..."` same-dir rule means a header pulls siblings from ITS OWN dir first, so to shadow a reference header you must also shim every header on the include chain that names it (e.g. camera drift needed BOTH `camera.h` and `rendobj.h` in `reference/shims/sweep/` — shimming `rendobj.h` alone was invisible because reference `camera.h` pulled reference `rendobj.h`).
* Once the camera shim lands, the per-function lanes are DRY: a full landed-set `locate` sweep yields ~0 (the one hit, `InitDbghelp`, fails string-verify — pooled literal), `next_work`'s "literal quick wins" are class-layout/width drifts in disguise (rendobj +0x34, BitFlags 0xff->0x7ff, flag bits), and the structural queue is 100% systemic (File vtable −1 slot, Object +0x20, AsciiString/StringBase ctor, STLport `_List_node` 0x0c->0x2c). Further % needs a header/class-layout reconstruction + full `./build.sh`, NOT per-function edits. Also: structural-queue RVAs are often misplaced (e.g. `getClassMemoryPool` "true start" was actually the already-matched `getModuleNameKey`) — verify an RVA is unclaimed before analyzing.
* Many "size mismatch (110B vs 104B) / misplaced" structural candidates are ICF-folded twins: `getClassMemoryPool@X` and `getModuleNameKey@X` compile to identical 104B so the linker folds them to ONE address; we already match that address as `getModuleNameKey`. The 6 trailing `0xcc` are inter-function padding ghidra counted, not code — and the twin is NOT extra landable bytes (same RVA, one-name-one-address). Don't chase these.
* BFME was built with RTTI off (`/GR-`): only 33 type descriptors exist and they're all exception/STL/locale/ATL types — NO game class (no `File`/`Object`/`LocalFile`). So you cannot read a class vtable from the binary via RTTI; class layout must be triangulated from `call [reg+slot]` sites. Also note matched INI parsers dispatch via function-pointer tables (`call esi`/`edi`), not C++ vtables, so they don't expose the `File` vtable either.
* Confirmed by disassembling `parseDefaultColor` (0x485970): BFME `File::scanString` is vtable slot **9** (`call [eax+0x24]`, `eax=[esi]`=File vtbl) while our ZH build puts it at slot 10 (`0x28`) — so BFME's `File` really does have one fewer virtual before `scanString` (the +1 shift is real, not a misread). To pin the dropped slot you still need BFME's slots for the methods adjacent to `scanString` (`scanReal` our-slot-9, `print` our-slot-11); get them from functions that call those via vtable (not the fn-ptr INI parsers).
* ZH `File` vtable is 16 slots, NOT the 14 you'd count from the visible `virtual` list in `Common/file.h`: `MEMORY_POOL_GLUE_ABC(File)` (GameMemory.h:681) injects two virtuals declared before `open` — slot0 `~File()` (GameMemory.h:683) and slot1 `getObjectMemoryPool()` (GameMemory.h:712). So the real order is 0:`~File`,1:`getObjectMemoryPool`,2:`open`,3:`close`,4:`read`,5:`write`,6:`seek`,7:`nextLine`,8:`scanInt`,9:`scanReal`,10:`scanString`,11:`print`,12:`size`,13:`position`,14:`readEntireAndClose`,15:`convertToRAMFile`. That is why `scanString` is slot 10 (0x28), not 8. BFME has 15 slots (one fewer somewhere in 0..9). WARNING: deleting any virtual in 0..9 shifts `scanString` AND every later slot, so it will BREAK already-matched funcs that use slots >= the deleted one — the net byte delta is uncertain and only the full `./build.sh` can measure it; do not blind-shim this.
* Ghidra 12.1.2 is NOT a shortcut to class vtables here: with RTTI off (`/GR-`) its analyzer only confidently recovers vtables for the string-keyed `FXParticleSystem::*` cluster + the `Module`/`Snapshot`/`Xfer` bases (class/method names are derived from embedded `MODULE_KEY`/`MODULE_NAME` literals returned by `getClass()`, not from a symbol table) — `tools/ghidra/export_vtables.java` dumps them (`reverse/vtables.tsv`, ~204 vtables). `File`/`Object`/`LocalFile`/`RAMFile` vtables are NOT labeled, and free functions stay `FUN_` (no names to pin), so the File/Object/STLport/AsciiString linchpins must still be triangulated from `call [reg+slot]` sites.
* A contiguous `.rdata` scan for "runs of code pointers" does NOT find class vtables here: of the ~1,492 runs of 14-18 code pointers, almost all share the same 10-15-slot suffix (`0xC79D,0x28B87,0x23416,0x15D9D,0x273BD,0x2CB42,0x21A7B,0x351B1,0x7405,0x16B3A`) — these are the INI parser function-pointer DISPATCH tables (`call esi/edi`), not C++ vtables, so the scan conflates them and can't isolate `File`/`Object`. The only reliable way to read a specific class vtable is constructor-anchored: find a `LocalFile`/`RAMFile` ctor that does `mov [this], imm32` (imm32 = the vtable in `.rdata`), then dump its slots. (Also: a batched `struct.unpack_from('<IIII',d,b+8)` for PE section headers reads `(VirtualSize, VirtualAddress, SizeOfRawData, PtrToRawData)` — assign in that order, not `(vrva,vsize,...)`, or every downstream RVA/VMA is silently wrong.)
* BFME's import IAT (FirstThunk) is BOUND (slots hold real `0x7C…`/`0x76…` addresses, not name RVAs), so to map an IAT slot to a name read the NAME from the parallel ILT (OriginalFirstThunk) and the SLOT VMA from the IAT (`IB + FirstThunk + i*4`); reading the name from the IAT entry itself yields nothing. Also BFME memory-maps files: file open is funneled through one free helper `FUN_00e3c63b` (calls `CreateFileA/W`,`GetFileSize`,`CreateFileMappingA`,`MapViewOfFile`) — so `LocalFile::open` is a vtable slot that CALLS that helper rather than inlining Win32, meaning the `File` vtable is anchored by finding callers of `FUN_00e3c63b` (one level up), not by CreateFileA sites. Gotcha: `re.finditer(re.escape(pat), …)` on a bytes regex that contains a `[…]` char-class silently matches 0 (re.escape turns `[` into a literal) — never re.escape an already-built bytes regex.
* Anchoring the `File` vtable via `FUN_00e3c63b` does NOT work either: its 15 callers are all free loader functions, none a vtable slot — because BFME reads from `.BIG` archives, so `LocalFile::read/seek/size` operate on an in-memory archive index and do NOT call `ReadFile/SetFilePointer/GetFileSize` (those Win32 calls live only in the open/mmap helper and higher loaders). So the `File` vtable's slots point to custom `FUN_` (archive-layer) code, not OS imports — the only remaining way to read it is constructor-anchored (`mov [this], imm32` in a `LocalFile`/`RAMFile` ctor) or by first naming `scanString`'s body (slot 9) and searching `.rdata` for it.
* Anchoring `File` via `File::print/write` is ALSO dead: MSVCR71 `fprintf`(0x13593c0)/`fwrite`(0x13593f4) have vtable-slot callers, but the only `fprintf+fwrite` slot is `FUN_00d93a40`, a len-1 `.rdata` singleton (a free helper/callback, not a class vtable) — same funnel pattern as `FUN_00e3c63b`; the other 5 slots call only `sprintf` (AsciiString formatting). So no import reaches a `File` slot. Bonus anchor: BFME `AsciiString`/`StringBase<char>` vtable is at **0x125f114** (5 slots: 0x887b60,0x888f90,0x888b50,**0x888bc0**,0x888b80=sprintf format) — confirms 0x888bc0 is a virtual in the AsciiString base and gives a fixed point for the AsciiString/StringBase drift. `build/find_file_print.py` reproduces this scan.
* Full `./build.sh` gates ARE viable on this contended host when backgrounded with `disable_timeout=true`: a clean tree verified green (3556/3556) in ~17.5 min at load ~2.7 (exceeds the 15-min default tool timeout, so always disable_timeout). Proven fact via a red gate: BFME `RenderObjClass::Bits` is at offset **0x10** (`Set_Additive`/`Set_Alpha` in part_buf.cpp read `[ecx+0x10]`); a pre-`Bits` +0x34 pad breaks exactly those two, so the CameraClass/LightClass +0x34 field drift is NOT in front of `Bits` — it must be reached via a different base subobject, not by padding `RenderObjClass` own-members.
* `getClassMemoryPool` codegen is driven by the GameMemory.h pool-glue macros (reference/.../Common/GameMemory.h:572-717): `GCMP_CREATE`/`MEMORY_POOL_GLUE_WITH_USERLOOKUP_CREATE` emit `TheMemoryPoolFactory->createMemoryPool("ClassName", sizeof, -1, -1)` (NAME-based) while the id-based path emits `createW3DMemPool(-1,-1,size)`. Proven name-based in retail: `DefectorSpecialPower` pushes the string "DefectorSpecialPower"(0x108fa74) and `SlowDeathBehavior` pushes "SlowDeathBehavior"(0x1090bf4). So unmatched `getClassMemoryPool@*` rows are NOT 110->104 padding — they split into (a) ICF twins of already-matched `getModuleNameKey@*`/`getClassMemoryPool@*Upgrade` (9/15, dead — add_match rejects overlap) and (b) UNCLAIMED classes (DefectorSpecialPower 0x259FD0, InactiveBody 0x213D60, OCLSpecialPower 0x262AF0, AnimatedParticleSysBoneClientUpdate 0x602F60, BeaconClientUpdate 0x603130, SpecialAbility 0x2A6520) whose retail uses the NAME-based macro while our build uses id-based. Win path: switch those classes to `MEMORY_POOL_GLUE_WITH_USERLOOKUP_CREATE(Class,"Class")` (per-class, low blast — but it also changes operator new for the class), per-file build.py verify, then add_match at full size.
* CORRECTION to the createMemoryPool lesson: our getClassMemoryPool codegen is ALREADY name-based and correct (objdump -r build/match/src_zh_modulefactory.obj shows the string DIR32 reloc + createMemoryPool(const char*,int,int,int) DISP32). The actual getClassMemoryPool@* diff is the function-local STATIC-INIT SEH GUARD: ours is ~6-7B longer (JNE +0x5b vs retail +0x55; extra `mov [esp+0x18],0`) — a static-local-init SEH config diff (build.py uses `/EHsc-`), not pool args. TOOLING GOTCHA: `explain_mismatch` compiled-bytes dumps are post-link with relocs resolved and can MISLEAD (a string push shows as `push 0xc24`-like garbage) — always `objdump -r build/match/<obj>.obj` to see the real relocs before concluding a call/arg is wrong. The identical-bytes vein is EXHAUSTED (fresh drift_classify regenerates only 234 no-obj rows — do NOT overwrite the 7702-row drift_report.csv with it; restored).
- getClassMemoryPool root cause = GCMP_FIND (findMemoryPool 1-arg, 104B), NOT createMemoryPool 4-arg; the modulefactory.cpp "restore ZH macro" block overrode the shim default. Discriminate pool vs namekey static-inits by `mov ecx,[GLOBAL]`: 0xEED600=TheMemoryPoolFactory (pool). build.py byte-compare MASKS all DIR32 relocs (globals/statics/strings) - skeleton+string+call-thunk only; nameToKey/findMemoryPool/createMemoryPool share thunk 0x3ADD7.
BFME gadget-msg enum: retail GCM_ADD_ENTRY=0x4022 (ZH 0x4020) - BFME added 2 msgs before combo block; proven via GadgetComboBoxAddEntry immediate. Shim tested safe (22 files green) but every gadget wrapper is blocked by GameWindowManager vtable slot (winSendSystemMsg retail 0xd4 vs ours 0x120 = 19-slot class-hierarchy diff), so it lands nothing until the vtable is reconstructed.
* Tree rename (src/ -> Code/<official path from exe __FILE__ strings>, 359 files): relative `#include "../math/x.h"` style includes silently break on move (cl.exe has NO default /I; quoted includes are same-dir + explicit /I only) — grep for `#include "../` after any move and convert to plain names + /I on the `// cl:` line. src/w3d scatter deferred: moved zh cpps would rebind to the 169 modified ported w3d headers via the same-dir rule.
* Case-colliding dirs (Ai vs AI, Gui vs GUI) compile fine on Linux until cl.exe under wine resolves the WRONG one — quoted same-dir includes then fail with C1083 on files that demonstrably exist. Reference tree casing wins (GameLogic/AI, GameClient/GUI). After ANY tree shuffle: `find Code -print | tr A-Z a-z | sort | uniq -d` must be empty.
* Big pushes (rename-class deltas) die with "Connection to github.com closed by remote host" because GitHub idle-drops SSH while the ~15-min pre-push hook runs — push with `GIT_SSH_COMMAND="ssh -o ServerAliveInterval=30 -o ServerAliveCountMax=20"`.
* w3d scatter phase 2: bulk-swapping 86 colliding ported headers to REFERENCE content failed both ways (zh files need the SHIM-grown variants; reference camera.h even byte-regressed camera.cpp 18/28). Resolution: 324 non-conflicting files moved to Code/Libraries; 85 unreconciled headers stay in src/w3d with /Isrc/w3d idirs on the ported cpps. Reconcile one header at a time: move, rebuild zh+ported includers, both must byte-match.
* MeshGeometry BFME layout: Flags immediately after MeshName (SORT at this+0x18 for Define_FVF); MeshGeometry +0x1c pad before end so CurMatDesc is at MeshModel+0x9c; MeshMatDesc drops empty W3DMPO base so UV[] starts at +0x0c. Note reference/WW3D2/meshgeometry.h is often included first (dx8renderer.h lives only in reference) — patch both Code and reference copies or same-dir /I loses the shim.
* RenderObjClass BFME (sweep shim): drop PersistClass (only two vtbls at +0/+8); Bits@0x10 + 4B mid-pad + Transform@0x18; sizeof 0xC8 (FrustumValid@0x100); two pre-Render stubs so Validate_Transform is +0x50, plus _bfme_ro_after_validate so Get_Bounding_Box stays +0x104 (updateBounds). Pin Matrix3x3::Set@0x8D9000 for Update_Frustum.
* MeshMatDesc Get_Shader_Array: ZH ShareBufferClass is W3DMPO+Array+Count (sizeof 0x14); BFME uses Code/WWLib sharebuf (RawBuffer+Array+Count+Alignment, sizeof 0x18). Put BFME sharebuf.h in reference/shims/sweep so zh-landed TUs pick it up; pin ShareBuffer<ShaderClass> 3-arg ctor @0x92B940.
* sharebuf.h field order is ALREADY correct (RawBuffer@this+0x8, Array@this+0xc) — do NOT swap it. Proof: `Get_Element`/`Peek_Element`/`Clear`@{Mat,Tex}BufferClass and the 3 ShareBufferClass<T> ctors already matched in meshmatdesc.cpp (17 functions, several genuine ICF aliases with byte-identical target bytes across T) all require Array@+0xc; full-gating `./build.sh Code/.../WW3D2/meshmatdesc.cpp` after swapping the fields breaks all 17. A prior pin claiming Peek_Material lives at 0x928F50 needing Array@+0x8 is a false lead: that address makes ONE candidate byte-match but is a structural near-match coincidence (matching.md's "near-matches are still failures" trap), not real Peek_Material — don't re-attempt it without independently re-deriving the address. Separately, the 7 present-unmatched MeshMatDescClass accessors (Peek/Get/Set_Material, Peek/Get/Set_Texture, Set_Single_Texture) had FALSE `matched` rows at RVA 0x2F8E2 (all 7 sharing byte-identical `E9 89 BA 6A 00`, which actually jumps into the middle of `Get_Color_Array`'s body) plus symbols.csv pin-harvest rows sharing the identical bogus 6-candidate set across all 7 names — both are decode_calls.py garbage-decode artifacts (see the AsciiString-divergence lesson above); rows were deleted (present-unmatched markers restored) rather than repointed since no independently-verified true address was found for any of the 7.
* MatBufferClass/TexBufferClass: BFME allocates via global operator new (not W3DMPO pool); drop W3DMPO_GLUE so Get_Material_Array/Get_Texture_Array match (global new @0x881F30).
* CRC_Memory is dual-linked (0x9E7F90 and 0x9E19C0); pin both in symbols.csv so locate accepts Install_UV/Update_CRC callees from either copy.
* SEH array-ctors (e.g. DefaultAlphaModuleInfo default): C++/naked under /EHsc- cannot emit the retail SEH prologue; MSVC still prepends member-init fragments before a naked ctor body. Land exact bytes via MASM (.asm + ml.exe; build.py routes .asm to ml).
* MASM exact-byte dumps: size = last non-0xCC byte ending in ret (C3/C2); strip trailing int3. When removing C++ body, force-emit any sibling COMDATs it was the only caller of (AABoxClass::Init, GameSpyStagingRoom setExe/IniCRC).
* MASM dump sizes: clamp true size to next claimed ledger RVA (drift report can oversize into a neighbor; setGameOptions 1997→383). Prefer last C2/C3 before 0xCC.
* MASM db immediates that start with A-F need a leading 0 (`0C1h` not `C1h`) or ml.exe treats them as symbols (A2006).
* build.py has a sound /showIncludes dep-cache (per-obj .deps.json sidecar): gates skip byte-identical TUs; a header edit recompiles exactly its includers. The old stale-obj-fakes-green trap is gone — no more touch-each-includer.
* w3d scatter COMPLETE: all 85 src/w3d headers reconciled to retail and moved to Code/Libraries/Source/WWVegas/...; /Isrc/w3d stripped from all 84 idir lines; src/ deleted. Final headers (rendobj/light/mesh/boxrobj/agg_def/predlod) needed retail vtable archaeology (capstone on lotrbfme.exe; ledger addrs are FILE OFFSETS == RVA, VA=+0x400000).
* Export table carries aliased manglings at the same RVA (IAE/MAE dtor variants) and pre-existing rows may use lowercase hex: normalize rva with int(x,16) before dedup, keep earliest row; functions.csv is CRLF — always append with \r\n and ensure a trailing newline or the last row fuses with your first.
* Retail W3DMPO is an EMPTY base (HLod/OBBox ctors store 2 vtables) but the pool GLUE statics+operator new stay (retail $E static-init counts include them); cl 7.1 pre-fix compiled some member fns EBO-view anyway, so switching to empty W3DMPO only breaks rows that matched with vptr-view — restore their offsets with one explicit _bfme_* pad dword per class (vertmaterial before MaterialOld, meshgeometry before MeshName).
* BFME DX8MeshRendererClass owns texture_category_delete_list@+0x1c and fvf_category_container_delete_list@+0x34 (MultiList sizeof 0x18 each) as members after texture_category_container_list_skin@+0x18 — ZH file statics break Clear_Pending_Delete_Lists; enqueue via TheDX8MeshRenderer.*
* Vendored cl is VS2003 RTM (13.10.3077): it rejects dependent nested-type return types without `typename` (prim_anim.h Get_Key/Set_Key pattern) — SP1-era cl accepts it, so assetmgr/prim_anim/ringobj/sphereobj stay sweep-blocked until an SP1 toolchain is vendored; do NOT patch the reference header.
* Sweep scratch copies must sit BESIDE the reference source (as .sweep_<name>.cpp), not in a central scratch dir: quoted sibling includes ("_pch.h" in profile/debug clusters) otherwise resolve to shim _pch.h and cascade C2653; candidates() excludes .sweep_ files.
* zh_annotate.py REGENERATES the landed cpp from the reference (it re-copies ZH content + inserts present-unmatched markers) — any source-level fix (e.g. NetPacket return NULL->NetPacketList() for stlport) must be applied AFTER annotate, then re-verified.
* anchor_by_string.py candidates on big GameEngine files (ModuleFactory, ThingTemplate) fail byte-verify WHOLESALE (20/20 incl. unique=1 ?validate@ThingTemplate): the string-xref->function-start convention only holds for the small utility files it was validated on (ini/ffactory/rawfile/crc/md5/mapper). Do not --emit against GameEngine trees; also the tool was CRLF-corrupting functions.csv (now fixed).
* Vendored cl RTM rejects default arguments on function typedefs (C2383) — ZH Display.h DebugDisplayCallback(fp=NULL) compiled under SP1; land W3DDisplay-style 2-arg callback calls with an explicit NULL third arg (identical codegen).
* Shim shadows CANNOT win header-to-header quoted includes inside the reference tree (INI.H -> "INDEX.H" resolves same-dir in reference/.../WWLib before any /I). For RTM-cl header bugs there (missing typename etc.), patch the reference header directly — precedent 43580ed1 edited reference meshgeometry.h/meshmatdesc.h. Copy the same fix into the Code/ copy if one exists (shared truth).
* Retail string literals differ from ZH: CopyProtection LAUNCHER_GUID is 46EB79F1-... in BFME vs 150C6462-... in ZH — String-ref verify catches these; patch the landed file (zh_annotate regen wipes it, re-apply after).
* Adding unused decls to reference/shims/sweep/windows.h can DETERMINISTICALLY perturb cl's regalloc in unrelated TUs (5 winuser cursor/capture decls flipped a two-load order in wwmemlog's VectorClass<ActiveCategoryStackClass>::Resize — 3/3 reproducible, not fleet jitter). If a full gate fails on one function after a shim round, build that file standalone with the shim reverted to bisect; isolate consumer-specific decls into a narrower shim (the cursor block now lives in dinput.h, seen only by DI device TUs).
* Drift-vote sanity check for pool-glue rows: the pushed pool-name string must exist in lotrbfme.exe (.rdata). 0x1F8AB0 was voted to 3 ZH glue names but pushes "DynamicPortalBehaviour" (BFME-only); "Anim2DTemplate"/"AnimateWindow" strings are absent from retail entirely — those copies cannot exist with ZH names.
Retail import calls are per-TU: most TUs use ff 15 [IAT] (dllimport shims) but some use e8->ILT-thunk (plain extern); switch one TU by aliasing the symbol after the headers (`extern "C" ... bfme_x(...); #define x bfme_x`) and pinning _bfme_x@N to the thunk — never de-dllimport a shim globally (35 matched rows rely on it).
If a header-inline accessor is a real out-of-line call in retail (small body matched + e8 at call sites), remove `inline` in the header and add a `__declspec(noinline)` body in the TU the ledger attributes the body to; position in the file does NOT stop MSVC from inlining.
Drift RVAs misattribute constantly: to locate the true body, walk a MATCHED caller — its retail call rel32 lands on the callee thunk/body (e.g. Register_Missing → ??0MissingAnimClass chain); /D_MBCS in the // cl: line switches _tcslen to MSVCR71!_mbslen (import call, no intrinsic) — retail WW TUs use it.
stlport dllimport wall: `// cl: /MD` files calling stlport-lib fns (__new_alloc::allocate, _Rb_global helpers) emit `call [__imp_x]`; retail folded to direct `e8`. Fix per-file: add `/D_STLP_USE_STATIC_LIB` to the cl line (15/15 rows unaffected in ObjectCreationList.cpp), then pin the callee symbol to its real body.
VC7.1 parses `/EHsc-` as `/EHs /EHc-` (the dash binds to the LAST letter only) — our base flags compile every TU with /EHs ON; to actually disable EH append `/EHs-` to the file's `// cl:` line. Under /EHs a ctor/dtor gets an SEH cleanup frame iff the unwind must run a user-provided dtor (implicit and trivial base dtors give NO frame); retail BFME dropped the MemoryPoolObject base from Net* wrapper classes, so their ctors have no frame while same-TU functions with new-guards keep theirs.
* A killed batch run leaves stripped present-unmatched markers plus uncommitted ledger rows behind; the pre-commit hook names every matched-but-still-marked symbol — delete those comment lines and recommit the delta instead of re-running the batch.
* symbols.csv pins to a callee BODY now auto-expand to that body's ILT thunks in load_symbol_map (tools/build.py); per-TU COMDAT copies of the same callee (e.g. Snapshot ctor at 0x9a3a30/0x9a1a30 vs matched 0x6b180) still need one additive pin per copy address.
* ICF alias groups: retail folds byte-identical COMDATs to one address (all de-pooled `??3Class@@...MagicEnum` deletes share 2 RVAs; 300+ source symbols at 12B each). Ledger now allows many names per (target_rva,target_size) — check_csv/build.py patch_exe/dedup_csv all treat identical-range groups as aliases; land them by verifying each name against the SAME claimed RVA (exact-overlap only; partial overlaps still rejected).
* REL32-blocked rows are pin-harvestable mechanically: for a failed candidate, decode retail's displacement at the unresolved reloc offset from each of its OWN candidate hits (build/_probe/pinharvest.py) — votes cluster on the true per-TU thunk/body addresses (e9 byte = thunk, else prologue). Additive pins are self-proving: the verifier only accepts a pin whose displacement equals retail's at that site, so wrong pins are inert. 1,300+ derived pins unlocked ~390 rows in one session; min_votes=1 catches the long tail.
* ZH GameEngine sources including PreRTS.h/STLTypedefs.h need a `// stlport` opt-in line near the top (build.py source_needs_stlport scans first 2KB): STLTypedefs.h uses `std::hash`, which only vendor/stlport provides — without the opt-in the vendored VC7.1 <xhash> fails with C2039 'hash' is not a member of 'std'. land_zh's verbatim copy does NOT add it; when a land_zh/sweep compile dies in STLTypedefs.h/xhash, add `// stlport` as line 2 (precedent: BuddyThread.cpp) instead of shimming.
- Shim constants must be verbatim SDK (ReactOS psdk mirrors work): empirical D3DRS values were silently costing matches (NORMALIZENORMALS=29, MATERIALSOURCE+18); wrong values never false-match but always block.
- Bulk mask-harvest can cross-match byte-identical template/getter bodies to wrong rvas (masked DIR32s hide wrong-global refs); the DIR32 consistency full-gate catches it. Fix: retarget rows via body-rescan constrained to anchored bases (pinned/majority-validated globals, IAT-parse for __imp__*, body-scan valid sets for ctors/dtors) else DELETE; per-TU C statics sharing a decorated name are legit multi-base — whitelist. See build/_probe/fix_dir32_v2.py.
- Sweep-env C2143/C2065 "blockers" on files whose Code/ tree already has a ported header (e.g. prim_anim.h typename fix) are artifacts: quoted includes resolve beside the reference file in the sweep, but to Code/ in the harvest path. Trust prep_batch+ctest over report.csv for those; the harvest subsumes the sweep.
- Proving a factory/findMemoryPool-style function dead: its name-string argument is the anchor — if the string is absent from the retail image (or present but with zero .text references), the body was dead-code-eliminated; drift candidates pointing at another class's pool fn (identifiable by its pushed name) are misplaced.
- wine resolves paths case-insensitively: same-dir sources differing only by case (INI.cpp vs ini.cpp) overwrite each other's obj under build/match — obj_path now encodes uppercase as ^x so both coexist.
- build/_probe/landmulti/*.obj are keyed {i}.obj per run but funcs.update spans ALL objs in the dir: stale high-index objs from bigger earlier runs silently overwrite fresh compiles — land_multi now wipes the dir first.
* BFME InGameUI/Radar/Audio vtable slots differ from ZH: force the retail slot with a pure-abstract layout class of N dummy `virtual`s then the real method (InGameUI setSuperweaponDisplayEnabledByScript is +0x90 = 36 dummies; Radar refreshTerrain +0x10; Audio removeDisabledEvents +0x9c with an extra `0` arg). VC7.1 rejects `__thiscall` function-pointer structs (C4234).
* All AUTOMATED bulk levers are exhausted at 11,003 matched: `next_work.py` reports sweep-winners 0, drift-quick-wins 0, ambiguous 0, ghidra-anchored-absent 0; and 0 unmatched EXPORTED code functions remain (the 503 unmatched exports are all `??_7` vtables + data symbols, not matchable code). Remaining work is 100% manual structural RE (2,464 rows). Ghidra IS available (`build/toolchains/bfme_ghidra/bfme.gpr`, fully analyzed; `ghdec <rva>` wrapper). `tools/ghidra/check_placement.java <rva...>` classifies drift RVAs ENTRY/INSIDE/NONE — empirically ~47% of drift candidates are ENTRY (real function start) but for SMALL functions (createTerrainLogic 89B, getObjectMemoryPool 24B, newModuleDataFromINI) the drift RVA is systematically INSIDE/NONE (misplaced), and their callees (e.g. `??0TerrainLogic@@QAE@XZ`) are neither exported nor matched so calls can't be resolved without first tracing the callee body in Ghidra. Per docs/structural.md this is ~30-60 min/function; reaching 20% (+4,698) is swarm-scale, not one-session.
* Structural queue RVAs are often wrong: doSetRallyPoint was listed at 0xC6DF40 (no Ghidra fn); true body is 880B @ 0x396D40 via RallyPointNoPath/Set string xrefs. Always check_placement/ghdec or string_xrefs before burning time on structural candidates.
* String-anchored present-unmatched leaves: unique `string_xrefs.tsv` ENTRY + long source literal → `build.read_target_bytes` MASM dump (not broken `mkdumpexport.py` PE parser) + `add_match`; reject claims where retail strings are a different UI/class than the marker (dtor/ctor vs window init). Drift `structural` RVAs are often INSIDE mega-functions — filter with ghidra ENTRY first.
* STLport indirect-call mismatch: a source whose STL calls compile to `ff 15 [__imp_...]` (indirect) where retail is a direct `e8` needs `/D_STLP_USE_STATIC_LIB` prepended to its `// cl:` recipe — flips STLport off dllimport. Unlocked ConnectionManager `insert_unique<G>`; the fix is repeatable across STL-template sources.
* Drift `structural` queue is ~94% mislocated: `candidate_rva` frequently points at the `0xcc` int3 pad *before* the real function (or inside another fn). Skip leading `0xcc` bytes to snap to the real ghidra start, then VALIDATE the symbol↔RVA pairing by emitted size (`tools/validate_recovery.py`) — same-name/wrong-size mispairings (e.g. a 238B `_M_insert` voted onto an 18B stub) are common and unmatchable by construction.
* A mangled name already matched as a 5-byte `jmp` thunk (land_thunks / MASM dump) BLOCKS its real body: `add_match` refuses a second address for one name. Farming thunks for count strands the real function — a name-dedup check before surfacing a candidate avoids the wasted slot.
* WW3D2 / GameEngineDevice sweep sources that pull the WWDebug chain hit `wwdebug.h`'s broken 4-levels-up `debug.h` include; pointing the recipe at `Code/.../WWDebug` then trips `tagPOINT`/`tagRECT` redefinition (project `windows.h` vs the d3d8 shim, both unguarded) + `DEBUG_ASSERTCRASH`-not-found. Systemic across the WW3D2 queue; needs shim dedup, not a per-file fix.
* Toolchain version is CORRECT — do NOT chase an SP1 cl.exe. The retail lotrbfme.exe Rich header shows it was built with MSVC 13.10.3077 (RTM) — the exact version we vendor (verified: dominant Rich entries prodID 0x0060 count 1266 / 0x005f count 184, all build 3077). So "codegen-order" dead-ends (e.g. the temp-slot `mov [esp+N],esp` saved before `mov ecx,esp`) are NOT a compiler-version cap; they're flag/source-shape differences and stay solvable.
* next_work.py --tier structural now snaps candidate_rva to the nearest ghidra function start (skip int3 pad, or ≤48B backward-snap) and prints the corrected rva in its explain_mismatch command, with a "drift-corrected …, verify the prologue" note. Deep mega-function-internal candidates (no nearby start) are left uncorrected — treat the absence of a snap-note as low confidence.
* Confirmed via `??0DozerPrimaryStateMachine@@QAE@PAVObject@@@Z` (0x2B7870, 551B, prologue byte-exact): retail builds the `State(machine,"Name")` debug-name AsciiString temp via a direct thiscall(this=stack temp, char*) to 0x888BC0 — the same address already matched as `StringBase<char>::StringBase(const char*)` + `BFMERetailAsciiString`/`BFMEPlayerTemplateAsciiString` (icf3, functions.csv rows for 0x888BC0). Our `AsciiString(const char*)` (verbatim ZH, AsciiString.h) goes through `ensureUniqueBufferOfSize` instead, never reaching that shape — this one root cause is why `decode_calls.py` garbage-decodes EVERY downstream call site in such functions (byte offsets stop lining up right after the divergent ctor call), and almost certainly blocks the whole family of tiny `State`-derived ctors in the structural queue (`ActAsDozerState`, `AIBusyState`, `AIDeadState`, `AIWaitState`, etc. — anything doing `State(machine,"ClassName")`). Not yet fixed: unknown whether BFME's `AsciiString(const char*)` genuinely diverged from ZH's, and the header is included everywhere (high blast radius) — needs a dedicated investigation + full `./build.sh` gate, not a per-function patch.
* Missing-SEH-frame is broader than the one Net*-wrapper precedent above: `playerTemplateComboBoxTooltip` (LobbyUtils.cpp, local `UnicodeString`) and `WouldMapTransfer` (MapUtil.cpp, no locals with dtors even needed by the source) BOTH byte-confirm retail has ZERO unwind prologue (no `push -1`/handler/`mov fs:0`) at their true (hand-verified) starts, while our plain-`/EHsc` compile always emits one. 2/2 unrelated files outside Net* — grep confirms 0 files in Code/ currently use the documented `/EHs-` lever, so this is fully untested. Worth a dedicated investigation (maybe most of retail was built effectively frame-free and the ALREADY-matched framed functions are the minority), but don't flip flags blind: file-wide blast radius, only the full `./build.sh` gate can prove it safe.
* `next_work.py --tier structural`'s `candidate_rva` regularly lands with NO ghidra-recognized function boundary anywhere in a ±64B window (not padding, not a start — genuinely inside a neighboring function's body/epilogue, e.g. `pop edi/esi/ebx; add esp,N; ret` then straight into the next function with no gap at all). `snap_rva`'s int3/bisect heuristics can't help here since there's nothing in `starts` to snap to; the only way to find the true boundary is hand-reading the disassembly for a `ret`/epilogue immediately before the drift target. Treat a structural-queue row with no snap-note as "candidate_rva may need manual archaeology," not just "low confidence."
* Found 3 mislabeled `matched` rows while landing the DozerAIUpdate.cpp State-machine ctor cluster (`?onExit@DozerActionState@@...`, `?update@DozerPrimaryIdleState@@...`, `?update@DozerActionPickActionPosState@@...` — all "Exact NB Open-BFME4" or "string-anchor...Open-BFME4+Grok" notes): the RVA/bytes were right but the NAME was wrong — all three were actually a *different* class's multi-arg constructor (proof: pushes a distinct class-name string, has an SEH prologue + base-ctor call + own-vtable store + `operator new`, none of which a 0-arg `update()`/1-arg `onExit(StateExitType)` override can produce). Byte-verification can't catch this — a MASM dump only proves the bytes match *some* address, never that the attached name is the right one; cross-check by reading the pushed string literal directly (`push VA` immediates are VAs, subtract 0x400000 for the RVA into `build.read_target_bytes`, unlike REL32 call targets which objdump already reports as RVA when given `--adjust-vma=<rva>`) and comparing against the reference source's actual complexity. Fixed via `git mv` + rewrite the .asm symbol + binary-safe functions.csv line-swap (never hand-edit the CRLF file with a text editor) + `add_match.py` re-verify. A spot-check of two more bare-"Open-BFME4" onExit/update rows outside this file (`onExit@AIAttackState`, in AIStates.cpp) found a plausible real match, so this looks localized to whatever batch landed the DozerAIUpdate.cpp cluster, not universal — but the batch's OTHER rows in that file are unaudited and worth a pass.
* RefreshGameInfoWindow structural candidate 0x4C8ACE is 114B of int3 pad; true body SEH prologue @ 0x4C8B40 size 1026 (ret @ 0x4C8F41). Still blocked: ZH inline AsciiString(const char*) fat path vs retail thin 0x888BC0 at static findImageByName sites + further body shape.
* BFME PathfindCell packs type+zone in one dword at +0xc (type&7, zone>>6&0x3f; impassable checks use 4/5); Pathfinder::m_zoneManager is at this+0xc9c (67 lea sites), not ZH +0x54. ZH 9-arg Pathfinder::checkForPossible (ret 0x24) is absent - retail is 8-arg @0x3D6210/181 (thunk 0x18BE7); queue 0x3F4B0D is Ghidra NONE.
* PathfindLayer::classifyCells true body 0x3FC790/1405 (queue 0x73F191 is mid-fn noise); PathfindCell retail sizeof 0x10 with packed dword@+0xc (type:3@0, layer:6@6, connect:6@12) vs ZH 8B nibble@+7; PathfindLayer m_bridge@+0x38 m_destroyed@+0x34 (ZH bridge@+0x30); classifyLayerMapCell inlined — leaf C++ cannot match until cell/layer layout lands.
* Pathfinder::checkForLanding ZH 6-arg (ret 0x18, ~160B) ABSENT standalone: queue 0x3F4B09 is mid-body of ret-0x1c fn ending 0x3F4C9B; 0 pathfind ret-0x18 leaves call getCell/adjustCoordToCell; landing spirals use checkForPossible@0x3D6210 (thunk 0x18BE7) instead. Same family dead as checkForPossible/validMovementPosition at 0x3F4B0x misplacements.
* Locomotor drops the `public Snapshot` base in retail (ctor@0x1B60E0 stores exactly ONE vptr, m_template@+4 not header's +8) — proven, fixed via reference/shims/locomotor/GameLogic/Locomotor.h. But that shim can't just apply to Locomotor.cpp: its implicit `??1Locomotor@@MAE@XZ`/`??_GLocomotor@@MAEPAXI@Z` rows are icf-owner claims (folded with `??1Upgrade@@`, which genuinely has the ZH dual-vtable shape) that silently regress if the class layout changes underneath them — split shim-dependent methods into a sibling TU (LocomotorAccessors.cpp, same trick as ScriptEngineFields.cpp/GameWindowFields.cpp) so the main file keeps compiling the unshimmed header. Also: LocomotorTemplate has its OWN separate field drift beyond m_surfaces (m_rollDamping compiles to +0x7c, retail reads +0x98) — the Locomotor vtable fix alone does not unblock the OVERRIDE Real-getter family (getRollDamping etc.), only LocomotorSet::findLocomotor. AIUpdateInterface::m_curLocomotor is retail+0x1cc vs header+0x1b8 (confirmed via isUsingAirborneLocomotor@Object 0x1C1770), but its 1237B ctor writes ~40+ fields with no isolated low-risk accessor to pin the insertion point(s) against — unshimmed, unlanded.
* BFME Object layout (proven from retail accessor bodies, NOT ZH): m_id@0x74 m_producerID@0x78 m_builderID@0x7c m_drawable@0x80 m_name@0x84 m_next@0x88 m_prev@0x8c m_status@0x90(2 dwords) m_geometryInfo@0xa8 m_disabledMask@0x1a4 m_disabledTillFrame[11]@0x1a8 m_behaviors@0x1f0 m_contain@0x1fc m_ai@0x204 m_objectUpgradesCompleted@0x224(6 dwords) m_team@0x23c m_weaponSet@0x264 m_scriptStatus(Byte)@0x343 m_partitionData@0x3b0; Thing::m_template@0x04. DISABLED_COUNT is 11 (ZH 13; SCRIPT_DISABLED=9 SCRIPT_UNDERPOWERED=10), UpgradeMaskType is 6 words (ZH 4), BehaviorModule reaches BehaviorModuleInterface at +0x0c (ZH +0x10), and BehaviorModuleInterface slots are getUpgrade 9/+0x24, getDockUpdateInterface 21/+0x54, getSpecialPowerUpdateInterface 24/+0x60 (ZH 26), getProductionUpdateInterface 27/+0x6c; SpecialPowerUpdateInterface doesSpecialPowerHaveOverridableDestinationActive is slot 4/+0x10 (ZH 7). The SHARED reference/shims/sweep/GameLogic/Object.h is still ZH-shaped and ~100 rows depend on it - do NOT move its fields; put BFME-layout leaves in Code/GameEngine/Source/GameLogic/Object/ObjectFields.cpp, which gets a private /Ireference/shims/bfmeobject (opaque-padded Object skeleton) exactly like the gamewindow precedent, and declare the module interfaces as TU-local pure-virtual slot skeletons instead of pulling the ZH BehaviorModule.h.
* To read the offsets a shim ACTUALLY produces (instead of counting bytes by hand), compile a throwaway TU with `unsigned probe[] = { offsetof(C,f), ..., 0xdeadbeef };` after `#define private public`, then find the 0xdeadbeef marker in the .obj and unpack the preceding dwords - VC7.1 does not print the value in a template-instantiation error, so the data-array trick is the only cheap way.
* A `notes` field literally reading "thunk accessor of ported class (icf alias)" is not proof of a real fold — it's a bulk-harvester's boilerplate label with no per-pair verification. Caught one: `?setOrRestoreTeam@Object@@...` and `?setScriptStatus@Object@@...` both claimed the identical 5B thunk 0x2BF2B→0x1D01D0, but their ZH bodies (a ~100-line Team-list/AI/Radar update vs. a bitmask+setDisabled dispatch) cannot plausibly compile to byte-identical code — confirm via ZH source shape (or independent caller-derived RVAs), not the label text, before trusting an icf-alias claim; the wrong one here was deleted (row + orphaned .asm), the present-unmatched marker was already intact in Object.cpp so no source restore was needed. Object's BehaviorModule query family (getProductionUpdateInterface/getDockUpdateInterface/getSpawnBehaviorInterface/getProjectileUpdateInterface/getCountermeasuresBehaviorInterface×2) all compile to the same 42B shape (m_behaviors@+0x1f0 loop, `lea ecx,[module+0xc]` this-adjust into a SECOND BehaviorModuleInterface vtable, `call [vtbl+SLOT]`) so byte-matching alone can't disambiguate which name owns which SLOT — only a caller whose own matched body calls the accessor AND then dispatches a distinctively-shaped method (unique arg count/type) off the returned interface proves it; landed getSpawnBehaviorInterface@+0x74 this way (retail ActionManager::getCanAttackObject calls slot+0x74 then the result's vtbl+0x8 with a Coord3D*, which only SpawnBehaviorInterface::getClosestSlave matches) — the vtable's ZH declaration order does NOT predict the retail slot number (getProjectileUpdateInterface is early in ZH's list but nowhere near the low slots in retail), so don't extrapolate slot assignments from ZH source order alone.
* Object.h has NEVER been shimmed (git log shows only the original vendor commit touched it) yet `?setVisionRange@Object@@QAEXM@Z` is already matched at 0x98D2C0 using its pristine ZH offset 0x124 — proof the header's natural layout is correct at least through m_visionRange. Since ZH declares m_visionRange/m_shroudClearingRange/m_shroudRange/m_disabledMask as 4 consecutive Reals+a BitFlags with nothing between them, the header's natural offsets put disabledMask at 0x130, but disabledMask is independently retail-proven at 0x1a4 (clearDisabled MASM notes) — so the real unresolved BFME insertion is the narrow 0x130–0x1a4 (116B) window AFTER m_shroudRange, not the wider m_geometryInfo(0xc8)-to-m_disabledMask span a task brief once assumed. 3 unnamed candidate bodies live in that window (0x1BF3A0 plain Real setter@0x194, 0x1BF3B0 compare-guarded Real setter@0x198 with a conditional callback through a helper pointer@+0x3b0, 0x1BF3F0 plain Real getter@0x19c) but match no ZH Object.h accessor — likely BFME-only new fields, unnamed/unlanded.
* BFME `TabControlData`/`ListboxData` (GameClient/Gadget.h) keep ZH's natural layout verbatim (subPanes[8]@+0x14, count@+0x10, activeTab@+0x40 — proven byte-exact via `GadgetTabControlShowSubPane` @0x4BDD10/105B); a `min(a,b)` call there only matches retail if written as a raw pointer ternary `Int *p = (a<b) ? &a : &b; x = *p;` (the reference-returning idiom) — both the plain macro `min` (stdlib.h, value ternary) AND the STLport/xutility template `(min)(a,b)` (also reference-returning, included via `// stlport` + `#include <algorithm>`) get folded by /O2 into the SAME simple value-select codegen, neither reproduces retail's address-of-both-then-select-pointer shape; only a hand-written local `Int*` variable resists that fold. Separately, `DisplayString` (GameClient/DisplayString.h) is NOT safely ZH-shaped: its retail vtable (read from `??0DisplayString@@QAE@XZ`'s stored vptr, VA 0x01083E50 -> RVA 0xC83E50) has only 4 entries total (dtor, 2 unknown, then `getTextLength`@slot3/+0xc) versus ZH's ~20 declared virtuals — `WinInstanceData::getTextLength()`'s inlined `m_text->getTextLength()` call lands at vtbl+0x10 in our build vs retail's +0xc, and a TU-local reinterpret_cast probe-class workaround (bypassing the header) changes upstream register allocation for the whole caller function, so it isn't a safe drop-in fix; needs the shared header corrected with real evidence for what BFME kept virtual, not attempted.
* BFME Drawable field drift is scattered, not one block: m_id compiles ZH+0x8c but retail reads +0x100 (getID 0x4116B0, mangleCustomAudioName 0x417330), m_locoInfo ZH+0xb8 -> +0x138 (getWheelInfo 0x77F6B0, DrawableLocoInfo's OWN layout unchanged so m_wheelInfo stays +0x3c into it), m_modules ZH+0xc0 -> +0x150 (xferDrawableModules note, NUM_DRAWABLE_MODULE_TYPES retail=3 vs ZH=2) — three checkpoints prove +0x74/+0x80/+0x90 cumulative insertions, i.e. at least 3 distinct scattered insertion points, not a single padded block; see reference/shims/drawable/GameClient/Drawable.h. Drawable::setPosition's thunk (0x1742C) chains through TWO more ILT hops to literally Thing::setPosition's body (0x132CE0) — a genuine retail identical-code-fold, but reproducing it needs the full Thing field set (m_transform/m_cachedPos/etc, not just m_template) inside the Drawable shim; not attempted this session (marginal value, high risk). clearAndSetModelConditionState's thunk target (0x526660) has an SEH frame and calls 5+ unresolved helpers — dump-only per the ladder. draw@Drawable's thunk target (0x786BD0) disassembles as a hashtable/list lookup (div, linked-list walk, `ret 4`), not draw-shaped logic at all — likely a bogus decode_calls.py attribution (see the MeshMatDescClass false-lead lesson); left unconverted rather than fabricate a match.
* DisplayString's vtable does NOT differ from ZH's declared virtual order (retail: dtor=0,setText=1,getText=2,getTextLength=3,notifyTextChanged=4,reset=5,setFont=6,getFont=7,... — same as ZH source order); the real bug is that ZH's `MEMORY_POOL_GLUE_WITH_USERLOOKUP_CREATE` macro (Common/GameMemory.h) injects a SECOND base virtual (`getObjectMemoryPool`, overriding MemoryPoolObject's pure virtual) ahead of the class's own virtuals, and retail's build doesn't have it — every DisplayString slot is off by exactly one vs the naive ZH-header compile. Proven three ways: getListboxEntryBasedOnCoord's own retail bytes (`call [edx+0xC]` for getTextLength), the existing computeExtents masm note (getText @ vtbl+8), and GadgetListBoxSetFont's retail bytes (`call [eax+0x18]` = slot 6, matching its pre-existing hand-cast `(*(void***)dString)[6]` workaround). Fix: TU-scoped shim (reference/shims/displaystring/GameClient/DisplayString.h) that drops the MemoryPoolObject base/macro entirely (safe when the TU never `new`s or `deleteInstance()`s a DisplayString, only consumes existing instances by pointer) — do NOT touch the shared Common/GameMemory.h, its blast radius is every MEMORY_POOL_GLUE class in the tree. NB: don't derive the vtable RVA from `??0DisplayString` ctor's stored vptr alone (0xC83E50) and disassemble its 4 slots directly — those 4 raw dwords are themselves VAs pointing into the incremental-link jump-thunk table (`E9 rel32` stubs, need a SECOND -0x400000-and-follow hop to reach real code), and even after that hop it's DisplayString's own tiny transient base-construction vtable, not the derived (W3DDisplayString) vtable actually used for dynamic dispatch on any real (fully-constructed) instance.
* ListboxData (GameClient/Gadget.h) has multiple field-offset/order drifts vs ZH beyond the already-documented listData@+0x18/endPos@+0x2c/insertPos@+0x2e/multiSelect@+0xb/selectPos@+0x34/selections@+0x38: columnWidth@+0x14 (ZH-naive +0x10), and displayPos/displayHeight are additionally reordered — retail's `list->displayPos + list->displayHeight` sum reads +0x3c then +0x44, but a lone `+ list->displayPos` elsewhere reads +0x44 (not +0x3c), i.e. whichever field is "displayPos" by ZH name sits at +0x44, not +0x3c. Follow the file's own established convention (moveRowsDown) — raw `*(Short*)((char*)list+OFFSET)` casts in the .cpp, not editing the shared reference/shims/sweep/GameClient/Gadget.h struct (high blast radius, many already-matched functions in this same file depend on its current shape via similar raw-offset workarounds).
* WaterRenderObjClass drift extends well past the proven -0xc pixel-shader-trio shift (addVelocity/ReAcquireResources): the offsetof-probe trick (compile a TU-local `#define private public` probe.cpp against the real `// cl:` recipe, read the `unsigned int probe[]={MARKER,offsetof(...),...,MARKER2}` array straight out of the raw .obj bytes — no functions.csv row needed, build.py's verify_functions just needs to be bypassed by calling compiler_command/compiler_environment directly) proves m_waterType stays at the header-natural +0x11c (zero shift, confirmed against setTimeOfDay's `cmp dword ptr[ecx+0x11c],2`), but m_tod is at +0x400, not the shim's +0x2d8. generateVertexBuffer's `Setting*setting=&m_settings[m_tod]` (edi=esi+0x2e0+m_tod*0x30, sizeof(Setting)=0x30 confirmed unchanged) proves m_settings sits at +0x2e0, i.e. BEFORE m_tod in retail (ZH declares m_tod then m_settings[TIME_OF_DAY_COUNT=5]) — 0x400-0x2e0=0x120=6*0x30, hinting a 6-element array, unconfirmed. Separately, generateVertexBuffer/generateIndexBuffer's CreateVertexBuffer/CreateIndexBuffer calls read the D3D device from a fixed global (`mov ecx,[0x1340534]`) matching DX8Wrapper::_Get_D3D_Device8()'s inlined `return D3DDevice;`, NOT this->m_pDev — a genuine BFME source-level edit vs both Generals and GeneralsMD ZH trees, not just a layout issue. m_vertexBufferD3D/m_indexBufferD3D/m_vertexBufferD3DOffset proven at +0x124/+0x128/+0x12c (net -4 from the current shim) while m_numVertices/m_numIndices land back on header-natural +0x13c/+0x140 (net zero) — an unresolved extra field is inserted somewhere in that 16-byte span. The 4 blocked WaterRenderObjClass symbols (setTimeOfDay, generateVertexBuffer, generateIndexBuffer, updateRenderTargetTextures) had ~260KB of per-attempt evidence in the since-deleted reverse/re_attempts.log; recover it with `git show 044166a6:reverse/re_attempts.log`.
* DisplayString's landed shim vtable (slots 0-9: dtor..setWordWrapCentered) is proven correct through setWordWrapCentered only - getSize is NOT at its naive declared-order slot 12 (0x30): retail computeTotalHeight (0x4B7ED0) AND addEntry (0x4B86B0) both independently call getSize via `call [reg+0x3c]` (slot 15), a 3-slot gap past setWordWrapCentered that the current shim's ordering doesn't explain (draw/getSize/getWidth/etc. past slot 9 remain unproven) - don't assume 'vtable order identical to ZH' extends past slot 9; for any DisplayString virtual beyond setWordWrapCentered, decode the actual retail call-site offset first and use a scoped hand-cast (GadgetListBoxSetFont's `(*(Fn*)&vtbl[N])(...)` idiom) rather than a plain `->method()` call through the shim header. Also: retail computeTotalHeight calls a DISTINCT 2-arg adjustDisplay entry point at 0x4B7B20 (2 pushes + `add esp,8`), separate from the general 3-arg adjustDisplay@0x4B7CA0 addEntry calls (3 pushes + `add esp,0xc`) - a genuine BFME-only overload; landing a matching C++ overload needs `__declspec(noinline) void __cdecl` or MSVC will fold the single-caller static into a custom register ABI (this file's own compiler picked ecx+al for an unqualified static 2-arg wrapper) instead of the plain 2-push cdecl shape retail's caller expects.
* NameKeyGenerator (Common/NameKeyGenerator.h/.cpp) keeps ZH natural layout exactly (SOCKET_COUNT=45007, m_sockets@this+8 right after the inherited SubsystemInterface vtable+m_name, m_nextID@this+8+45007*4=0x2bf44 — proven via init/reset) EXCEPT AsciiStringData always carries the extra debug pointer (8B header, chars@+8) that the ZH header only adds under `#if _DEBUG||_INTERNAL`; do NOT fix this by `#define _INTERNAL` for the whole TU (Win32LocalFileSystem.cpp does this safely only because it never touches a SubsystemInterface-derived layout) — `_INTERNAL` cascades into GameCommon.h `#define DUMP_PERF_STATS`, which adds 6 real Real/Bool members to SubsystemInterface and shifts every derived-class field after them; patch just the one struct in a TU-scoped `Common/AsciiString.h` shim instead (reference/shims/namekeygenerator/). freeSockets/keyToName/nameToKey(const char*) additionally touch an unidentified auxiliary key-indexed structure past m_nextID (a 3-pointer vector-shaped member at this+0x2bf48) that ZH does not have — left unconverted (present-unmatched); only the layout-independent nameToKey(const AsciiString&) forwarder was landed.
* Remaining Drawable@@ masm_dumps beyond getID/getWheelInfo are all deeper than field-offset work: mangleCustomAudioName (0x417330/205B) is ZH-identical source (AsciiString customizedName.format(" CUSTOM %d ",(Int)getID())...) using the already-proven getID()@+0x100, but compiles to structurally different code from retail beyond the shared SEH prologue (retail pushes m_id twice then a literal format-string pointer 0x10f1520 + byte-flag=1 into a helper at 0x888bc0, ours doesn't match that arg count/register shape) — logged blocked, not a near-miss worth forcing. flashAsSelected (0x76e4f0/556B) and setCaptionText (0x418880/381B) are also SEH-framed (local-object dtor unwind) and unattempted (out of budget); xferDrawableModules (0x417e20/530B) and calcPhysicsXform (already logged layout-blocked) likewise. getClassMemoryPool@Drawable/@DrawableIconInfo/@DrawableLocoInfo rows all cite the literal SAME target_rva 0x27F130 as getClassMemoryPool@AIUpdateInterface, whose disassembly references AIUpdateInterface-specific guard byte 0x12efe34/static-pool-ptr 0x12efe30/name-string 0x1090584 — true byte-identity across unrelated classes would require BFME to bucket them into one shared named MemoryPool (same ARGPOOLNAME + sizeof), unverified; don't trust the "ICF alias" note without confirming the shared-pool theory first.
* NameKeyGenerator's BFME-only aux structure at this+0x2bf48 (freeSockets/keyToName/nameToKey blocker noted above) is now fully identified: `std::hash_map<NameKeyType,Bucket*,rts::hash<NameKeyType>,rts::equal_to<NameKeyType>>` (STLport `hashtable<>`) — sizeof 0x14, empty hasher/equal/get_key folded into the leading 4B, `vector<void*>` bucket array @+4/+8/+0xc, `_M_num_elements` @+0x10; ctor's `push 0x64; call helper` is literally `hash_map()`'s default `_M_ht(100,...)`; `_Hashtable_node<pair<const NameKeyType,Bucket*>>` (next@0,key@4,Bucket*@8=0xC) matches exactly. freeSockets' tail-jump target 0x8F880 IS `hashtable<>::clear()` byte-for-byte (stl/_hashtable.c) and LANDED clean (declare it as raw `UnsignedInt m_keyToBucketStorage[5]` + `reinterpret_cast`, not a real member — a real `hash_map` member would force an implicit `~hash_map()` call into the already-matched 79B `~NameKeyGenerator`, which retail's dtor doesn't have; same "placement-construct, never destruct" trick as WWDebug/wwmemlog.cpp's `char buf[sizeof(CRITICAL_SECTION)]`). Also: retail's `Bucket` is NOT a `MemoryPoolObject` — freeSockets deletes each node with a single virtual call (`push 1; call [vtbl]`, i.e. plain `delete b;`), not `MemoryPoolObject::deleteInstance()`'s 3-call getObjectMemoryPool()+dtor+freeBlock() sequence; drop the `MEMORY_POOL_GLUE_WITH_USERLOOKUP_CREATE` base/macro in the TU-scoped shim (keep only the already-matched placement `operator new/delete(size_t,BucketMagicEnum)` pair + a lone `virtual ~Bucket()`) — same pattern as the DisplayString shim dropping the macro's extra `getObjectMemoryPool` vtable slot. keyToName/nameToKey(char*) themselves still don't byte-verify though: retail's `AsciiString` copy ctor is OUT-OF-LINE at 0x887B60 with what looks like a lazily-initialized magic-static guard (checks/sets flags at 0x1336e28/0x1336e2c, calls through 0x1358e4c/0x1358d18) before the plain refcount++, and `AsciiString::set(const char*)` is out-of-line at 0x887D20 too — our shim's copy ctor/operator= are plain ZH-style inlines, so both functions' compiled bytes diverge at the first AsciiString touch. That's real AsciiString-ABI reconstruction, not a NameKeyGenerator-shim fix.
* Player's radar-cluster field block is proven: m_playerIndex is UNSHIFTED at [this+0x24] (getPlayerMask@Player, matched), but m_radarCount/m_disableProofRadarCount/m_radarDisabled land at [this+0x58]/[this+0x5c]/[this+0x60] vs this header's natural 0x44/0x48/0x4c — a +0x14 (5-dword) insertion somewhere in the m_side..m_upgradeList span (exact sub-field unresolved; TU-scoped opaque pad in reference/shims/player/Common/Player.h is sufficient since nothing reads those fields by name). hasRadar's own standalone body (0xC9A80, pure field read, no calls) lands clean off this shim. disableRadar/enableRadar/addRadar/removeRadar all additionally call `TheAudio->getMiscAudio()` which retail dispatches VIRTUALLY (`call [vtbl+0x124]`) while the current unshimmed `Common/GameAudio.h` still declares `AudioManager::getMiscAudio()` non-virtual (plain direct call) — an AudioManager-side vtable drift, unrelated to Player, that blocks the whole radar-sound-emitting cluster (and blocks m_sciences too: `okToPlayRadarEdgeSound`'s own body proves m_isPlayerDead sits at [this+0x680], not natural+0x14=+0x45c, i.e. a SECOND ~0x224B unresolved insertion exists somewhere between m_radarCount and m_isPlayerDead, so m_sciences's true offset is still unknown). Also found in passing: the existing `??1AudioEventRTS@@UAE@XZ` ledger row (thunk 0x2671F) resolves to a body (0xCFA40) that writes NO vtable pointer, while AudioEventRTS demonstrably has one virtual (`virtual ~AudioEventRTS()`, vtable value 0x1081d40 confirmed via the copy-ctor body @0xB2FB0) — that row may be misattributed to an unrelated same-shape dtor; unconfirmed, not touched.
* The above AsciiString-ABI gap for NameKeyGenerator IS closeable without touching the shared string ABI: retail's "0x887B60 guarded copy ctor" / "0x887D20 out-of-line set()" are NOT AsciiString's own methods — they're WWLib's already-matched `StringBase<char>` members (`Code/Libraries/Source/WWVegas/WWLib/string_base.cpp`; `str()` has zero matched rows anywhere in the whole binary, i.e. it's ALWAYS fully inlined). Rebuild the TU-scoped `Common/AsciiString.h` shim on top of `#include ".../WWLib/string_base.h"` with a single `char* m_text` member (matches `StringBase<char>::m_data`) and INLINE (in-header, not out-of-line like the real `Code/Libraries/Source/WWVegas/WWLib/ascii_string.h`/`.cpp`) copy ctor / `operator=(const char*)` that `reinterpret_cast<StringBase<char>*>(this)` and explicitly call the private-but-friended StringBase ctor/`set(str,len)` — the compiler then inlines the thin wrapper at the call site and emits a direct call to the already-matched StringBase address, matching retail exactly. `str()` must NOT delegate the same way (no matched row exists to call) — write its ternary (`m_text?m_text+8:""`) directly inline instead. This landed `nameToKey(const char*)` byte-exact (0x8FFC0/299B, plus REL32 pins for the aux hash_map's `resize()`@0x8FB70/`insert_unique_noresize()`@0x8FAB0 — decode_calls.py's suggested ILT-address pin is wrong here, pin the body address instead, same convention as the already-landed `hashtable<>::clear()`@0x8F880 pin). `keyToName` (0x8FD30/119B) still does NOT land with this fixed ABI: it's now purely a `hash_map<>::find()`-inlining-shape near-miss — retail's compiled `_M_find()` loop has ONE shared post-loop test reused for both the found/not-found exits, but every idiomatic `it != m.end()` / `it == m.end()` phrasing here compiles a second, redundant test the optimizer won't fold away. The full per-attempt note was in the since-deleted reverse/re_attempts.log (`git show 044166a6:reverse/re_attempts.log`).
* ProductionPrerequisite::PrereqUnitRec (const ThingTemplate*+Int+AsciiString) vector<T> thunk-dumps: only the STL internals that a TUs own push_back()/clear() genuinely trigger (here: _Construct, _Destroy, erase, the __copy called from erase) link out of that TU and byte-match; _M_allocate_and_copy/__uninitialized_copy/__copy<const-source>/_M_insert_overflow/_M_clear are simply absent from the object ("symbol not found in object") or diverge structurally (SEH prologues, size>=0x80 free-list vs plain-delete allocator branch) even though the analogous vector<ProductionPrerequisite> instantiations of the SAME helpers sit unconverted in ThingTemplate.cpp too — treat each STL-internal thunk as its own attempt, do not assume a working push_back/clear proves the whole cluster is reachable. Also: a struct dtor thunk (PrereqUnitRec::~PrereqUnitRec) can have TWO different non-ICF-folded retail addresses for the SAME mangled name depending on caller (0x195FB for the _Construct/_Destroy growth path, 0x2F99B for erase) -- symbols.csv supports multiple address rows per name (build.py tries each candidate), so pin both rather than picking one.
* To get an exact compiler-verified `offsetof`/`sizeof` for a class in this MSVC7.1/wine toolchain without guessing from disasm: temporarily make the fields `public`, then in the TU add `template<int N> struct Dummy{}; Dummy<(int)offsetof(T,field)> probe; void use(){ probe.NOSUCHMEMBER(); }` for each field and `./build.sh <file>` — the C2039 error text includes `with [ N=<value> ]`, one per field, all in a single compile. Far faster and more reliable than hand-walking a ctor/dtor disasm (which is extremely easy to mis-tally across a 200+-line function). Always revert the probe + the `public:` edit before committing.
* PlayerTemplate's "0x34-byte tail pad after m_strMedallionSelected" (wave-13 claim, driven only by the sizeof-stride literal 0x124) is DISPROVEN as a tail-only model: using the offsetof-probe trick above plus direct retail ctor/dtor disasm (thunks at 0xE2E50/0xE27A0/0xE32D0), retail's Handicap ctor (proven by its distinctive 4×float=1.0 default-init body) sits at retail offset 0xC, but the ZH-order reconstructed header places `m_handicap` at 0x10 — i.e. retail has only ONE AsciiString slot between `m_displayName` and `m_handicap`, not `m_side`+`m_baseSide`. That -4 shift holds exactly through Money/RGBColor/m_startingBuilding/m_startingUnits (confirmed byte-for-byte), but breaks down harder in the production-maps region: retail offset 0x6C holds a container whose ctor does a locale/env-var lookup (call chain 0x433465->0x4D8450->0x415659) before a vector-shaped alloc — not another plain `ProductionChangeMap` — strongly suggesting a NEW BFME field with a string-keyed, locale-aware comparator inserted mid-struct, and retail offset 0xA8 is destroyed as an AsciiString where the reconstruction has an Int (`m_specialPowerShortcutButtonCount`). Full evidence and the exact retail field-type list for offsets 0xA4-0x124 (clean: alternating AsciiString/vector<AsciiString>/pointer-release, matches ZH order there) was in the since-deleted reverse/re_attempts.log under the three PlayerTemplate special-member symbols (`git show 044166a6:reverse/re_attempts.log`). Next step before any shim attempt: decode callees 0x4062a3 and 0x42a059 (called from the ctor at 0x4D8450) to identify the real container type at retail 0x6C.
* STLport template-internal thunk-dumps (`??$_Construct/_Destroy/__copy/fill@..@_STL@@`, `?get_allocator/pop_front/erase@?$vector|deque|list|set@..@_STL@@`, `??0?$vector|_STLP_alloc_proxy@..`, `??1?$_Rb_tree|set@..`) convert to clean C++ by REPOINTING the row — NO source is written. The body is a COMDAT the class's home TU (any landed .cpp already instantiating that container, i.e. one already carrying matched `..@T@..@_STL@@` sibling rows) emits; set target_rva=the E9 jump target, target_size=len(COMDAT bytes rstrip 0xcc). Byte-identical template COMDATs ICF-fold, so ANY emitting TU is a valid source and add_match needs `--icf-owner <existing row at same rva/size>` when the dest is already claimed. Mechanized recipe: index every cached build/match/*.obj by symbol (build.read_object_symbols), for each thunk mask DIR32+REL32 reloc slots to retail bytes and require masked==retail (skeleton test); if so, harvest EVERY rel32 target from retail (`dest+off+4+<i disp>`) and pin any not already among the callee's `load_symbol_map` candidates (build_call_thunks misses per-site incremental-link thunks e.g. `??1Nugget@ModuleInfo` body 0x772354 but retail calls it via thunk 0x2F487 — pin the exact site) — pins are additive/self-proving. This landed 15/164 of the `@_STL@@` set (WeaponBarrelInfo _Construct, GameResultsResponse _Construct, Bit_iter __copy, fill<Object**>, 3× _Destroy, list<AsciiString> copy-ctor, _Rb_tree<AsciiString> dtor, 2× get_allocator, vector<ICoord2D> ctor, STLP_alloc_proxy ctor, set<StagingRoom> dtor). The other 149 are UNconvertible by repoint: retail's `_Construct/_M_insert_overflow/_M_allocate_and_copy/__uninitialized_copy/__copy` for non-POD element types carry an SEH unwind frame (`push -1;push <handler>;mov eax,fs:0;...`, ~70B) that our /EHs-effective compile emits frame-free (~19B) — a real codegen divergence (lessons 48/70), plus size>=0x80 free-list allocator-branch splits; skeleton test fails, so they stay MASM dumps. Scanner/harvester: build/_probe not needed — see scratch stl_scan2/harvest2/land2 pattern.
* Multiple drift-queue candidates snapped to the SAME RVA (e.g. CreateGameTextInterface/createKeyboard@W3DGameClient/createTerrainLogic@GameLogic all at 0xB026E0) = one bogus ghidra-snap, not ICF — the site was a 7-byte trivial vtable-dtor in a small-class clone cluster. Recover the true body bottom-up: take the class's already-matched ctor, scan for callers with BOTH E8 and E9 patterns (incremental-link thunks are E9 rel32 — GameTextManager's ctor had ZERO E8 callers and one E9 thunk @0x46C4; E8-only scans falsely conclude "never constructed"), then scan callers of the thunk. Layout reconciliation ground truth = the factory's pushed sizeof immediate + the matched ctor MASM's member-store map: BFME GameTextManager is ZH minus m_tbuffer (0xA000) AND minus m_maxLabelLen (one Int) -> sizeof 0x7840, mapped store-by-store against ??0GameTextManager@@QAE@XZ @0x438350. Windows host: add_match.py's auto-verify dies with WinError 193 launching build.sh — use --no-verify and run ./build.sh <source> manually before committing.
DCE'd STL instantiation family proof: masked-scan the ZH obj bodies (build.read_object_symbol_bytes gives bytes+exact reloc offsets) vs retail .text; zero unmasked >=8B anchors or >40% best-window diff across ALL instantiation-specific bodies = family absent; ignore tiny ICF-generic helpers, batch-log every queue sibling dead in one commit (map<AsciiString,AudioEventRTS>, 29 rows, 2026-07-26).
* Twin-rebase union-merge (strip markers + exact-line dedup) is for APPEND-ONLY files (lessons.md) ONLY. Run on code it deletes legitimate duplicate lines (#endif, `}`): the ed9ac753 replay silently broke Player.h/PlayerFields.cpp until restored from healthy twin c6536a51. Resolve code conflicts by hand, then build the TU before continuing.
* `reverse/symbols.csv` addresses are RVAs (`0x00078210`), NOT the VMAs you read off a disassembly (`0x00478210`) — a VMA pin silently resolves to nothing and the call just stays unresolved. Pin the BODY, not the incremental-link thunk: `build_call_thunks` finds the `E9` thunk for you. Also, a BFME-only class with no ZH counterpart is reachable from one string: `initSubsystem`'s "TheXxx" literal gives the `new <size>` + ctor + global in `GameEngine::init` (0x79060), the ctor gives the vtable, and diffing that vtable against the base's shows exactly which slots the class overrides (see SubsystemLegend.cpp).
* Retail built STLport with `_STLP_NO_EXCEPTIONS` while the compiler still ran `/EHsc`: `_STLP_TRY`/`_STLP_UNWIND` are no-ops, so `list<T>::_M_create_node` is a 33-byte frame-free body (allocate; `_Construct`; return) instead of our ~90-byte `push -1`/funcinfo version — and being EH-free it also *inlines*, which is what makes `insert`/`push_back`/callers match. Add `/D_STLP_NO_EXCEPTIONS` to the file's `// cl:` line; it does NOT disturb the compiler's own member-unwind frames (entry ctors keep theirs). This is the "SEH unwind frame ~70B vs our frame-free ~19B" divergence noted in the `@_STL@@` lesson above, in the opposite direction, and it should unblock the other 33-byte `_M_create_node` MASM dumps (e.g. `list<BuddyMessage>` @0xEB530).
* When a ctor is byte-exact except for WHERE the EH-state store lands, the missing thing is usually a nested subobject boundary, not a statement reorder: STLport keeps a vector's end-of-storage pointer inside an `_STLP_alloc_proxy` member with its own inline ctor. Modelling a 12-byte vector stand-in as `{void*, void*, Proxy}` (Proxy holding the third pointer) instead of three plain pointers moved the `mov [esp+N],eax` two instructions later and matched SubsystemLegendEntry's 166-byte ctor after three earlier sessions had failed on that one instruction.
* MASM byte-dump rows can silently swallow whole neighbourhoods: `?parseSideInfo@AI@@...` claimed 516 bytes at 0x009A15C0 covering EIGHTEEN int3-separated functions (a 219-byte INI parser, its 21-byte callback, and sixteen tiny folded stubs — two of which are vtable slots for 131 and 146 different classes). Its name came from a 35%-shape-ratio guess in `reverse/zh_sweep/drift_report.csv`, which had itself recorded the true size as 219. Before trusting any row, check `int3` padding inside its range and count image-wide dword references to interior addresses: padding inside a "function" or an interior address appearing in 100+ vtables both prove the claim is a blob, not a body.
* An INI FieldParse table is the cheapest field-name oracle in the binary. `INI::initFromINI`'s second argument is a DIR32 to a `{const char *token, parseProc, userData, Int offset}` array; walking it names every member the block writes and pins their offsets. The table 0x011415C0 turned SubsystemLegendEntry's `m_strings1/2/3/m_value/m_text` into `m_initFile/m_initPath/m_extension/m_loader/m_initFileDebug`, and the block-table row that points at the callback (0x00ED7768) gives the INI keyword ("LoadSubsystem").
* SOLVED (was: "register-allocation coin-flip that strands a function at 99%"). `SubsystemLegend::findEntry` is now 264/264 C++ and the MASM dump is deleted. The eax-vs-ecx loop latch was never the disease: it was a symptom of `compare` being an out-of-line CALL where retail inlines it. Scan first, theorise later — `?compare@?$StringBase@D@@QBEHABV1@@Z` (0x0005FEB0, 92B) has ZERO direct call sites anywhere in .text, i.e. retail inlines the comparison at every use and keeps the COMDAT only because the symbol is exported. Giving `AsciiString::compare` a real body in ascii_string.h (the `memcmp` intrinsic form: read `str`'s length/data first, then `this`'s, `n = min`, `memcmp`, then the length difference) puts retail's `repe cmpsb` in line and the function falls out exactly. Do it at the AsciiString level and leave `StringBase<char>::compare`'s naked COMDAT alone: inlining StringBase's own copy also matches findEntry but leaves its 92-byte COMDAT 9 bytes off (retail keeps `this->m_data` in eax where we reuse ecx, and its tail is `sub edx,ebx / mov eax,edx` where MSVC canonicalises ours to `mov eax,edx / sub eax,ebx` regardless of source shape), which would trade two matched rows for one.
* SOLVED, and the fix is a class of bug worth naming: the "retail records the unwind esp BEFORE materialising `this` for a by-value class argument (`push ecx / mov [ebp-N],esp / mov ecx,esp`) where we emit the reverse" divergence was NOT a compiler difference — it was a header-fidelity gap. Retail's `AsciiString` copy ctor, `const char*` ctor and `operator=(const AsciiString&)` are INLINE in the header, each delegating to its `StringBase<char>` member; ours were out-of-line in ascii_string.cpp. Making the definition visible in the TU flips MSVC's EH scheduling to retail's order. The tell was sitting in symbols.csv the whole time: a pin that points one class's mangled name at a DIFFERENT class's body (`??0AsciiString@@QAE@ABV0@@Z -> ??0?$StringBase@D@@AAE@ABV0@@Z`, same for `@PBD@Z` and `??4AsciiString@@ -> ?set@?$StringBase@D@@`) means retail's call sites resolve straight to the delegate, which only happens when the delegation is inline — the pin was papering over the inlining instead of expressing it. Each inline still needs its out-of-line COMDAT forced in ascii_string.cpp under `#pragma auto_inline(off)`, the same trick the default ctor already used. Cost at the full gate: zero (13620/13620 unchanged). Payoff: INI::loadSubsystemFiles 214/214, SubsystemInterface::loadIniFilesFromLegend 286/286, SubsystemInterfaceList::initSubsystem 271/271, initSubsystem<SubsystemLegend> 154/154. Diagnostic recipe: bisect the TU by top-level function until a probe function flips ordering — the trigger is a DEFINITION being present, not a flag, and not nothrow-provability (`throw()` and `__declspec(nothrow)` do nothing). The companion half of that lesson — "retail keeps the EH-state stores inside a loop where we hoist them to the preheader" — is a try-SCOPE signal, not a quirk: move the `try` inside the loop body and MSVC stops hoisting (that alone took INI::loadSubsystemFiles from 33 bytes off to exact). Before blaming register allocation for a near-miss, check whether the callee should have been inline.
* GameEngine::init (0x00079060, 6689B, still a MASM dump) is the engine boot sequence and the highest-value unported function in Common/, and I have now measured what it costs instead of guessing. The 60 subsystem registrations ARE mechanically extractable — walk init's push/call stream, split at each "TheXxx" literal feeding StringBase<char>::StringBase(const char*), read back the preceding operator-new size and ctor (or the factory call) — and the result is checked in as reverse/gameengine_init_subsystems.tsv: name, retail sizeof, ctor RVA, new-vs-factory, and each subsystem's own initSubsystem<T> instantiation (~192B apiece, consecutive from 0x00072DD0, so 60 more matchable functions once the classes exist). Generating 60 stub classes from that table and writing init against them COMPILES and emits 4026 bytes, but only ~10% match, and three things are why: retail performs several un-named setup allocations before the first registration, our operator new resolves to a different body, and roughly 1700 bytes of init is not registrations at all. A skeleton generated from it (747 lines of stub classes plus 113 unverifiable pins) reached only ~10% and was deleted; there is no exception list to park such a thing behind any more. Port the subsystem classes first; the table tells you their sizes and ctor addresses, and each one you finish makes init strictly closer.
* The two escape hatches that let non-1:1 source into this repo are now closed, and it is worth knowing why they existed so nobody rebuilds them. (1) `reverse/unclaimed_sources_whitelist.txt` let a .cpp own ZERO byte-verified rows if you wrote a reason — six files were living there, including a GameEngine::init reconstruction with ~26 verified bytes out of 6689. The list and its `verify_source_claims` support are deleted: every .cpp under Code/ must own at least one byte-verified row, no exceptions, or it does not belong in the tree. (2) `tools/next_work.py` skipped any candidate whose `re_attempts.log` entry was `dead` or had 3+ attempts, which had silently erased 697 still-unmatched functions from the work queue (667 via one `dead` entry). That log and `tools/log_attempt.py` are now deleted outright and next_work.py no longer reads any attempt history, so nothing can drop a candidate from the queue again; the 3162 historical entries are recoverable with `git show 044166a6:reverse/re_attempts.log` if a specific blocked symbol ever needs its evidence. If you cannot byte-match something, the answer is to delete the source and leave the evidence in this file — not to find somewhere to keep it.
* BFME compiles INI in TWO translation units and conflating them is unfixable from one file. The split is visible in the binary: the field parsers at 0x00851050..0x008542B0 INLINE INI::getNextToken (each body carries that function's own "Expected additional data after '%s'" string), while the template/store parsers -- parseScience, parseThingTemplate, parseFXList and ten more at 0x000B8E80..0x000BB040 -- CALL it. Making getNextToken inline lands the first group and breaks the second, so the fix is `Code/GameEngine/Source/Common/INI/ini_parsers.cpp` plus `reference/shims/ini_inline/Common/INI.h` (same header, getNextToken/getNextTokenOrNull/scanInt/MultiIniFieldParse::add inline, BFME's layout). ini.cpp keeps the ZH header and the other group. Functions that call each other must travel together: moving parseBitString32 out while leaving parseBitString8 behind broke parseBitString8, because with the callee's definition gone MSVC stopped reserving its `tmp` local. An inline that also needs its out-of-line COMDAT gets the `#pragma auto_inline(off)` forcing shim ascii_string.cpp already uses.
* BFME's INI layout, proven field by field by INI::INI (0x00851230) and re-confirmed by readLine: m_file +0x00, m_filename +0x04 (built from "None"), +0x08/+0x0c, m_buffer +0x10 running to +0x413 (INI_MAX_CHARS_PER_LINE with NO +1), m_seps +0x414 " \n\r\t=", m_sepsPercent +0x418, m_sepsColon +0x41c, m_sepsQuote +0x420 "\"\n=", m_blockEndToken +0x424 "END", a BFME-only "ENDSCRIPT" pointer at +0x428 where ZH has m_endOfFile, Bools at +0x42c (EOF) and +0x42d, and a vector at +0x834. ZH's 8K m_readBuffer does not exist: BFME pre-splits the file into that vector of 8-byte line records and readLine strncpy's line[m_lineNum] into m_buffer. Get m_seps wrong and every field parser misses by exactly the two bytes of that displacement.
* Two BFME-vs-ZH source drifts account for most INI near-misses. (1) ZH's bare `throw INI_INVALID_DATA` / `INI_INVALID_NAME_LIST` became `INIException(n, "message", ...)`; the message texts are recoverable from retail's string pool around 0x01130350..0x011307B0, and the leading integer is 2 for a message with no substitution and 3 for one with either one OR two %s -- transcribe it, it is not an argument count. (2) A number of sites `throw` a plain int 1 where ZH throws ERROR_BUG; the ThrowInfo at 0x012454C0 decodes to TypeDescriptor ".H" (int) and all 19 sites using it throw 1. ERROR_BUG is genuinely still 0xdead0001 -- an Errors.h shim renumbering the enum is WRONG and breaks parseScience/parseThingTemplate. Every numeric/bool scanner also first runs its token through a BFME-only macro table (helper 0x008524B0, buckets at 0x0130CE5C), which is the "or predefined macro" half of their messages.
* Ledger target_size can UNDERSTATE a function, and a short row still passes the gate because only the claimed bytes are compared. INI::parseRGBColor was recorded as 238B but int3 padding does not start until +241, and correcting the size was the difference between "3 bytes differ" and a match. INI::parseBitString32 is recorded as 297B against a 472-byte body, so 175 bytes are unclaimed and its MASM dump is truncated -- still outstanding. When a reconstruction is byte-identical over the whole claimed range and merely longer, suspect the row before the source.
* INIException's message strings are often COMPOSITES, which is why its leading integer looks wrong. INI::initFromINIMulti throws 0x01130678 = "Missing '%s' token.\n\nError parsing block '%s' in file '%s', line %i.\n" -- one sentence about the failure glued to a generic location suffix -- so a site that looks like it has one %s actually formats four. The counts observed so far are 1 (no substitution), 4, 5 and 8 in initFromINIMulti and 2/3 in the scanners, and they do not follow varargs+1 across all of them (scanLookupList passes 3 for a single-%s message and byte-verifies). Transcribe the integer AND the argument list from the call site; do not derive either from the format string.
* Unresolved, and worth settling before anyone writes SubsystemInterfaceList's ctor: `??0SubsystemInterfaceList@@QAE@XZ` is claimed at 0x0040D800 (22B) from the ZH-tree SubsystemInterface.cpp, but that body zeroes SIX dwords while the one GameEngine::init actually calls -- 0x009A1E10, 16B -- zeroes FOUR, which is exactly BFME's layout (the m_subsystems vector's three pointers plus m_xfer). `SubsystemInterfaceList::SubsystemInterfaceList() : m_xfer(0) {}` against the native header byte-matches 0x009A1E10 exactly, so the two addresses are two different classes wearing one mangled name. Do not land the second row until the ZH-tree claim is audited. Note also that 0x009A1E10 is immediately followed, with no padding, by a 43-byte vector compaction loop, so a naive int3 scan reports one 59-byte body.
* INI::loadFile (0x00853A20, 259B, still unclaimed) is the entry point that matters most for the legend: GameEngine::init calls it six times for its hard-coded paths and SubsystemInterface::loadIniFilesFromLegend calls it for every InitFile. Its shape is fully read: setFPMode(); s_xfer = pXfer; load(...); then `while (!m_endOfFile) { readLine(); parseBlock(filename); }`, then an INLINED unPrepFile tail -- clear the +0x834 line vector, m_filename.set("None", 4), zero m_loadType/m_lineNum/m_endOfFile, s_xfer = NULL. Two things block writing it: the +0x834 line-record vector needs a type (its ctor/dtor/clear are 0x009CBDC0 / 0x009CBFE0 / 0x009CBF50 and its element is 8 bytes), and s_xfer is a file-static that currently lives in ini.cpp while every function that touches it -- load, readLine, prepFile -- is parser-arena and belongs in ini_parsers.cpp. Move that group first. The per-block dispatcher it drives is pinned as INI::parseBlock (0x00851350, provisional): it strtoks the line, looks the block keyword up via 0x00850820 and records m_curBlockStart.
* Moving the INI file-loading group into ini_parsers.cpp is more than a cut and paste, and it is the prerequisite for writing loadFile. readLine/prepFile/unPrepFile/load move cleanly with the s_xfer static, but the TU then also needs: a complete `File` type (the shim header only forward-declares it, so `m_file->close()` fails), `setFPMode`, the `findBlockParse` file-static, and -- for loadDirectory -- FileSystem's `set<AsciiString, less_than_nocase>` machinery, which is why loadDirectory should stay in ini.cpp until that is available. Attempted and reverted; none of those functions is matched yet, so a half-done move buys nothing.
* A short target_size on a C++ row hides a partially-correct body, and that is worse than the MASM case. Sixteen rows found by audit_short_rows turned out to be free wins -- the source already compiled to the whole retail function and only the row was short, so re-verifying at the true size landed them unchanged (StringBase find/reverseFind, four HLodClass LOD functions, HUFF_decompress, normAngle, WaterRenderObjClass::addVelocity and others). But five did NOT match at the true size: MemoryPool::_Init and _VerifyIntegrity, Animatable3DObjClass::Render and Get_Bone_Transform, and vector<unsigned char>::resize. Those bodies really are incomplete -- they reproduce a prefix of retail and the short row is what let them pass. Do not extend those rows; fix the source. The distinction is cheap to make: extend the size and re-verify, and the gate answers it for you.
* Extending a row and re-verifying is NOT sufficient proof on its own -- run the full gate. DIR32 slots are copied verbatim from the target, so extra bytes that are mostly relocations byte-"match" for free. FireWeaponNugget::parse appeared to match when extended 23B -> 55B and only the DIR32 consistency check caught it: the extra span made ??_7FireWeaponNugget@@6B@ resolve to two different bases (0x0 and 0x109f0dc). Reverted. Of the 45 rows extended this way, that was the only false positive, but it is the one that would have quietly wedged another agent's build.
* A common shape behind short rows is the jump-table switch: the row covers the dispatch and stops. DX8Wrapper::Get_DX8_Blend_Name was recorded as 17 bytes, which is exactly `mov eax,[esp+4]; dec; cmp; ja; jmp [eax*4+<table>]` -- the thirteen `mov eax,<string>; ret` cases and the table itself were unclaimed, and the real function is 156 bytes. Same for Get_DX8_Texture_Arg_Name, Get_DX8_Texture_Transform_Flag_Name, ParticleBufferClass::Render_Line_Group and others. Extending these is safe to CHECK but must be gated: the table entries are DIR32 relocations, so that part of the comparison is free. The full gate is what distinguishes a real extension from FireWeaponNugget's false positive, and it does so positively -- landing these seven moved DIR32 symbols 3585 -> 3641 and verified literals 1189 -> 1222, i.e. the new bytes carry real references that check out.

* MSVC 7.1 inline asm cannot express an absolute `[imm32]` memory operand -- all three spellings fail differently, so `__emit` the instruction's bytes. Bare `mov ecx, dword ptr [12F0898h]` assembles as `mov ecx, imm32` (B9, not 8B 0D); `ds:[12F0898h]` assembles correctly but prepends an explicit 3E segment-override byte; a named `extern "C"` global makes MSVC emit a relocation whose recorded offset points at the instruction start, so the build's DIR32 fill overwrites the opcode and shifts every following byte. This is why the existing native_* GameNetwork files __emit every global load/store. Store forms (`mov [imm32], eax`) additionally reject the ds: spelling outright with C2415.
* Three more MSVC 7.1 inline-asm shapes that must be `__emit`ed rather than written as mnemonics, all found while transcribing retail bodies: string instructions (`rep movsd` and friends) -- a disassembler prints their implicit `es:[edi], [esi]` operands and MASM rejects them, so write the mnemonic alone; x87 stack forms like `fdivp st(1)` -- MASM wants `fdivp st(1), st` and one-operand spellings are C2414; and MSVC's multi-byte alignment NOPs `lea ecx,[ecx+0]` (8D 49 00) / `lea esp,[esp+0]` (8D A4 24 00000000), which MASM re-encodes into the shorter `8D 09` form and silently shifts every following byte. The last one is the nastiest because it compiles clean and only shows up as a byte diff a few hundred bytes in.
* A fourth inline-asm re-encoding trap, and the one that needs tooling rather than a rule: two-register ALU ops are direction-ambiguous, so `test ebx, edx` is both `85 D3` and `85 DA` and MASM does not always pick retail's. There is no way to know which without assembling, so the workable loop is generate -> byte-compare -> find the instruction containing the first differing byte -> force just that one to `__emit` -> repeat. Two or three pinned instructions per few-hundred-byte function is typical; `mov`, `test`, `cmp`, `add`, `and`, `or` and `xor` between two DIFFERENT registers are the candidates (same-register forms like `xor eax,eax` are unambiguous).
